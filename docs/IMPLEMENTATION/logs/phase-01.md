# Log Phase 01 — Master Data Independen

| | |
|---|---|
| **Phase** | [`phases/phase-01.md`](../phases/phase-01.md) |
| **Milestone PRD** | `M1 (sebagian) · M5 (sebagian)` |
| **Status** | Lihat [`IMPLEMENTATION-STATUS.md`](../IMPLEMENTATION-STATUS.md) |
| **Mulai** | 15 September 2026 |
| **Selesai** | — |

Log ini mencatat **apa yang benar-benar terjadi** selama phase berjalan: keputusan yang diambil, hal yang berbeda dari rencana, dan angka hasil pengukuran. Ia bukan salinan rencana — rencananya ada di [`phases/phase-01.md`](../phases/phase-01.md).

Log tidak boleh memuat requirement, keputusan desain, maupun business rule baru. Bila selama phase muncul kebutuhan akan salah satunya, ia dinaikkan ke PRD atau SDD lebih dulu, dan log ini hanya mencatat bahwa hal itu terjadi.

---

## 1. Catatan harian

| Tanggal | Yang terjadi | PR terkait |
|---|---|---|
| 19 September 2026 | `PR-01-14` tergabung ([#58](https://github.com/HanzzzBD/SIGM4/pull/58)). `PR-01-17` dibuka di `feature/PR-01-17-impor-pengguna-asinkron` (dari `develop`): migration `0020` (`user_import_jobs` + enum `user_import_status`, `expand`), `UserImportService.submit/get/jalankan`, `GET /users/import/{id}`, pekerjaan `user-import` pada worker, dan event outbox `UserImportRequested`/`UserImportCompleted`. Docs tidak menjawab bentuk pencatatan, cakupan idempotensi, hak baca laporan, maupun nasib `NT-52` (modul notifikasi baru di Phase 02) — ditanyakan ke pemilik produk sebelum kode (keputusan 33). Pemeriksaan mutasi atas delapan logika inti: enam langsung memerahkan uji; dua (kunci advisory atas hash, larangan menimpa pekerjaan yang sudah berakhir) awalnya **lolos tanpa ada uji yang memerah**, sehingga ditambahkan satu uji khusus untuk masing-masing — uji konkurensi awal dengan `Promise.all` ternyata tidak menjebak kondisi balapnya. Uji ujung-ke-ujung (`worker-user-import.test.ts`) menjalankan submit → outbox → antrean BullMQ nyata → worker produksi → `SELESAI` terhadap Redis dan PostgreSQL nyata. Satu galat sendiri tertangkap uji: regex `\d` pada skema param kehilangan garis miring saat ditulis lewat skrip. | — |
| 19 September 2026 | `PR-01-13` tergabung ([#57](https://github.com/HanzzzBD/SIGM4/pull/57)). `PR-01-14` dibuka di `feature/PR-01-14-gerbang-persetujuan-wali` (dari `develop`): migration `0019` (`users.consent_guardian_at`, NULLABLE, `expand`) dan gerbang `DP-02`/`SL-06` di `UserService` untuk akun **Siswa/OSIS (`R-07`)**: `POST /users` dan impor (`consent_wali: true` wajib — akun lahir `AKTIF`, jadi membuat siswa = mengaktifkannya), `PATCH .../status → AKTIF`, dan `PUT /users/{id}` (merekam persetujuan; mengganti role menjadi Siswa tanpa penanda ditolak). Penanda diisi server dari `Clock`, direkam sekali, tidak dicabut, dan hanya untuk siswa (`DP-03`). Dua titik tidak terjawab docs dinaikkan ke pemilik produk sebelum kode (keputusan 32): bentuk gerbang, dan penanganan `NT-48`. Pemeriksaan mutasi: mematikan gerbang pengaktifan membuat satu uji merah, gerbang pembuatan membuat tiga. Satu uji lama (`user-import.schema.test.ts`) yang menyatakan `consent_wali` diabaikan "sampai PR-01-13/14" diperbarui. Sekaligus dirapikan: tiga baris kosong di tabel `phase-01/02/05.md` yang memutus tabel markdown (akibat sunting saya sebelumnya). | — |
| 19 September 2026 | Butir terbuka `PR-01-13` ditutup lewat pertanyaan terstruktur ke pemilik produk (keputusan 31): `SystemAuthContext` → `PR-02-32` (Phase 02), `SL-05` + `DP-04` → `PR-05-26` (Phase 05), rancangan PRD/SDD keputusan 29–30 disetujui apa adanya, dan tugas `PR-05-09` ditambah pendaftaran pemeriksa di proses API dan worker. Rencana 167 → **169** PR; `validate_impl.py` disesuaikan. Dikerjakan pada cabang `PR-01-13` yang masih terbuka. | [#57](https://github.com/HanzzzBD/SIGM4/pull/57) |
| 19 September 2026 | `PR-01-12` tergabung ([#56](https://github.com/HanzzzBD/SIGM4/pull/56)). `PR-01-13` dibuka di `feature/PR-01-13-siklus-akun-siswa` (dari `develop`): migration `0018` (`student_enrollments`, `expand`), `ClassPromotionService` + `POST /class-promotions` (`SL-02`: `NAIK` menetapkan kelas pada satu tahun ajaran, `LULUS` menandainya; laporan per siswa, transaksi per siswa, ≤ 200 siswa), `GraduationService` (`SL-03`: nonaktifkan lulusan bila `tanggal_selesai` < hari WIB ini; idempoten), dan titik ekstensi `StudentObligationRegistry` (`SL-04`) yang juga menjaga `PATCH /users/{id}/status` bagi akun Siswa/OSIS. Sebelum kode ditulis PRD terbukti diam soal tabel kelas-per-tahun, endpoint kenaikan kelas, dan pekerjaan kelulusan; rancangan penuh ditawarkan dan diterima pemilik produk (keputusan 30). **Temuan yang mengubah cakupan setelah persetujuan:** pekerjaan `student-graduation` tidak dapat dipasang — pelaku job adalah `SYSTEM` dan `SystemAuthContext` (`SDD-03`) belum ada, sedangkan `AuthContext.userId` wajib bilangan bulat (dipakai `updated_by` seluruh repository); membangunnya menyentuh lapisan `AuthContext` (tulang punggung, `BRANCHING-STRATEGY §3.1`). Ditanyakan lagi: service sekarang, pemasangan job menyusul PR tersendiri. Pemeriksaan mutasi: mematikan blok `SL-04` dan mengubah `<` menjadi `<=` pada batas akhir tahun ajaran masing-masing membuat satu uji merah. Catatan `AC-YR-02`: "pemicu" pergantian tahun ajaran adalah navigasi UI `P-71` → `P-64` (halaman `P-71`), bukan pekerjaan backend — `PR-01-18` cukup menukar tahun aktif. Skill `api-and-interface-design` dimuat sebelum kode (butir 3b `CLAUDE.md`). | [#57](https://github.com/HanzzzBD/SIGM4/pull/57) |
| 19 September 2026 | Pertanyaan pemilik produk atas dua butir terbuka `PR-01-11`/`PR-01-12` ("pertahankan atau tutup di Phase 01?"): dijawab dengan rekomendasi dan dua keputusan (keputusan 29). Catatan koreksi: PRD **diam** soal endpoint kalender akademik dan `work_units`, jadi kondisi sebelumnya bukan keputusan "sesuai PRD" melainkan celah — dan DoD Phase 01 ("empat modul dapat didemokan mandiri kepada Administrator", "kalender akademik terisi") tidak dapat lulus tanpa jalur pengisian. Yang dikerjakan: baris endpoint `academic-years`, `holidays`, `work-days`, `work-units` (14 baris) dan sebelas aksi log ditambahkan ke `m20-settings.md` §7/§11 (PRD lebih dulu); `PR-01-18` dicatat di rencana Phase 01 (`L`, 166 → **167** PR) TANPA dikerjakan; runbook [`runbooks/master-data-awal.md`](../runbooks/master-data-awal.md) menjembatani pengisian master dan pemetaan `map_users_unit_kerja()` sampai PR itu tergabung. SQL runbook diverifikasi di basis data dev dalam transaksi yang di-rollback. | [#56](https://github.com/HanzzzBD/SIGM4/pull/56) |
| 19 September 2026 | `PR-01-11` tergabung ([#55](https://github.com/HanzzzBD/SIGM4/pull/55)). `PR-01-12` dibuka di `feature/PR-01-12-work-units` (dari `develop` setelah `PR-01-11` tergabung): migration `0017` — tahap **expand** (`work_units`, `users.work_unit_id` NULLABLE, dua enum) dan **migrate** (`map_users_unit_kerja()`, fungsi SQL idempoten: cocok by `nama` atau `kode` dengan normalisasi huruf/spasi, hanya bila TEPAT SATU kandidat, tidak pernah menimpa `work_unit_id` yang sudah terisi, dan MENGEMBALIKAN yang tak terpetakan sebagai laporan). Kode `m02-users` beralih: `POST/PUT /users`, respons, dan `filter[work_unit_id]` memakai `work_unit_id` (id tak dikenal atau unit nonaktif → `VALIDATION_ERROR`, bukan `500` dari FK; unit nonaktif tetap sah bagi yang sudah memakainya), dan impor meresolusi `kode_unit_kerja` ke master (kode tak dikenal = baris gagal, `E.5.2`). `users.unit_kerja` lama tetap ada, tidak lagi ditulis (contract `PR-08-11`). Tiga titik dinaikkan ke pemilik produk sebelum kode ditulis (keputusan 28). Temuan saat menguji: helper CSV `m02-users-import.test.ts` tidak memuat kolom `kode_unit_kerja`, sehingga uji baru semula lolos-semu (semua baris sukses tanpa unit) — diperbaiki dengan menambah kolom pada `HEADER`. Pemeriksaan mutasi: fungsi pemetaan versi naif (menimpa dan menebak) membuat uji "tanpa penebakan" merah. Skill `api-and-interface-design` dimuat sebelum kode (butir 3b `CLAUDE.md`). | — |
| 19 September 2026 | `PR-01-10` tergabung ([#54](https://github.com/HanzzzBD/SIGM4/pull/54)). `PR-01-11` dibuka di `feature/PR-01-11-kalender-akademik` (dari `develop` setelah `PR-01-10` tergabung): migration `0016` (`expand` murni) — `academic_years`, `academic_terms`, enum `academic_term_name`, dan `holidays.academic_year_id` NULLABLE. Acceptance "tepat satu tahun ajaran aktif" ditegakkan **basis data** (`SDD-DB-18`): paling banyak satu lewat partial unique index, paling sedikit satu lewat constraint trigger `DEFERRED` (pergantian aktif = dua `UPDATE` dalam satu transaksi). Ditambah `EXCLUDE` agar tahun ajaran dan semester tidak beririsan. Sebelum kode ditulis ditemukan celah PRD dan dua titik dinaikkan ke pemilik produk (keputusan 27): tidak ada endpoint kalender akademik yang terdaftar di modul PRD mana pun, sehingga PR ini skema saja; dan kelompok Bab 11.3 "Nama Semester" ditambahkan. Uji: 8 uji PostgreSQL nyata dengan pemeriksaan mutasi — mencabut trigger dan index membuat empat uji merah. Skill `api-and-interface-design` dimuat sebelum kode (butir 3b `CLAUDE.md`); PR ini tanpa kontrak HTTP baru, sehingga tidak ada temuan darinya. | — |
| 19 September 2026 | `PR-01-09` tergabung ([#53](https://github.com/HanzzzBD/SIGM4/pull/53)). `PR-01-10` dibuka di `feature/PR-01-10-system-settings` (dari `develop` setelah `PR-01-09` tergabung): modul `m20-settings` lahir pertama kali — migration `0015` (`system_settings`, enum `setting_group`/`setting_type`, seed enam parameter), `GET /settings` (terpaginasi, `filter[kelompok]`) dan `PUT /settings` (`FR-20.1`). Katalog kunci, nilai bawaan, dan rentang ditetapkan di SDD lebih dulu (`SDD-05 §4.7a`, `SDD-DB-17`) lalu di-seed — sesuai acceptance `phase-01.md` §7. Tiap baris `system_settings` mendeskripsikan dirinya sendiri (`tipe`, `nilai_bawaan`, `nilai_min`/`nilai_maks`) sehingga validasi rentang (`FR-20.1 A1`) membaca baris, bukan daftar di kode; `PUT` atomik (satu nilai tidak sah menolak seluruh permintaan, dengan daftar per kunci beserta batasnya), hanya nilai yang berubah ditulis, dan satu entri `SETTING_UPDATED` memuat nilai lama/baru. Dua titik dinaikkan ke pemilik produk SEBELUM kode ditulis (keputusan 26): cakupan katalog dan dua kelompok baru di PRD Bab 11.3. Ditemukan setelah kode jadi: `CLAUDE.md` memperoleh butir 3b (muat skill `api-and-interface-design` untuk `apps/api`) di pohon kerja; skill dimuat SETELAH kode ditulis lalu kontraknya diperiksa, dan satu perbaikan lahir darinya: `GET /settings` semula mengembalikan seluruh katalog tanpa paginasi (melanggar `SDD-PERF-04`), kini terpaginasi, dan `PUT` hanya mengembalikan parameter yang diminta. | — |
| 19 September 2026 | `PR-01-08` tergabung ([#52](https://github.com/HanzzzBD/SIGM4/pull/52)). `PR-01-09` dibuka di `feature/PR-01-09-ekspor-activity-log` (dari `develop` setelah `PR-01-08` tergabung): `GET /activity-logs/export` (`FR-18.2` langkah 5, `AL-10`) — filter SAMA seperti daftar (`ExportActivityLogsQuerySchema`, tanpa `page`/`per_page`), hasil digenerate `exceljs` sebagai satu berkas XLSX (header Bahasa Indonesia, `21.2` struktur entri log) dan di-stream langsung sebagai respons HTTP. Dua titik cakupan tidak dapat diterapkan dari docs apa adanya dan dinaikkan ke pemilik produk SEBELUM kode ditulis (keputusan 25): format (XLSX saja, PDF ditunda) dan mode eksekusi (sinkron, bukan `SDD-07 §3` "Ekspor & PDF: Asinkron") — `stored_files` (`SDD-09`) dan handler worker ekspor belum ada, pola sama `PR-01-06`/`PR-01-07`. Batas `BATAS_EKSPOR = 5000` baris ditolak SEBELUM diekspor (`VALIDATION_ERROR`, `detail.rule: "FR-18.2-A1"`) — menjaga `SDD-PERF-06` ("> 5 detik wajib asinkron") tanpa infrastruktur asinkron itu. Aksi ekspor itu sendiri tercatat sebagai `ACTIVITY_LOG_EXPORTED` (`m18-activity-log.md` §11), pola identik `ACTIVITY_LOG_VIEWED` di `PR-01-08`. `RouteDefinition` mendapat `contentType?` opsional (`shared/http/route.ts`, dipakai `api/openapi.ts`) agar dokumen OpenAPI route ini mengiklankan `application/vnd.openxmlformats-officedocument.spreadsheetml.sheet`, bukan `application/json` bawaan — perbaikan mekanis kecil pada infrastruktur bersama, bukan requirement baru. | — |
| 18 September 2026 | `PR-01-07` tergabung ([#51](https://github.com/HanzzzBD/SIGM4/pull/51)). `PR-01-08` dibuka di `feature/PR-01-08-penelusuran-activity-log` (dari `develop` setelah `PR-01-07` tergabung): modul `m18-activity-log` lahir pertama kali — `GET /activity-logs` (`FR-18.2` langkah 2-4) dengan filter kombinasi (rentang tanggal, pengguna, role, modul, aksi, entitas+entitas_id) langsung memakai keempat indeks `activity_logs` yang sudah dibuat `PR-00-13` (`waktu`, `user_id`, `entitas`, `modul`+`aksi`) — tidak ada migration baru. Repository mengueri `activity_logs` LANGSUNG (tabel itu ditulis `shared/audit`, bukan modul manapun). Akses terhadap log itu sendiri tercatat sebagai `ACTIVITY_LOG_VIEWED` (`m18-activity-log.md` §11) — meski operasi utamanya BACA, `AuditLogger.write()` menuntut `TransactionScope`, sehingga pembacaan dibungkus `withTransaction` sama seperti operasi tulis. Kriteria "≤3 detik" (`FR-18.2` AC) tidak diuji lewat timing assersi — volume data uji terlalu kecil untuk bermakna; diperiksa saat gerbang keluar phase dengan data staging. | [#52](https://github.com/HanzzzBD/SIGM4/pull/52) |
| 18 September 2026 | `PR-01-06` tergabung ([#50](https://github.com/HanzzzBD/SIGM4/pull/50)). `PR-01-07` dibuka di `feature/PR-01-07-daftar-aset-per-lokasi` (dari `develop` setelah `PR-01-06` tergabung). **Kesalahan ditemukan dan diperbaiki SEBELUM tergabung** (keputusan 23): implementasi pertama menaruh `GET /rooms/{id}/assets` di modul `m03-locations` dengan permission `location.view` — `audit_docs.py` menolak (`Endpoint duplikat`) karena baris itu SUDAH dimiliki `m04-assets.md` §7 dengan permission `asset.view`. Modul `m04-assets` (belum termasuk "Modul PRD" Phase 01 — lihat header `phases/phase-01.md`) dibangun PERTAMA kali, KERANGKA satu endpoint saja, sesuai kepemilikan dokumen yang sebenarnya. | [#51](https://github.com/HanzzzBD/SIGM4/pull/51) |
| 18 September 2026 | `PR-01-05` tergabung ([#49](https://github.com/HanzzzBD/SIGM4/pull/49)). `PR-01-06` dibuka di `feature/PR-01-06-pohon-lokasi-penonaktifan-berjenjang` (dari `develop` setelah `PR-01-05` tergabung): `GET /locations/tree` (FR-03.1 langkah 1) dan `PATCH /buildings/{id}/status` · `/rooms/{id}/status` (BR-015). **Cakupan BR-015 disempitkan ke pemilik produk sebelum kode ditulis** (keputusan 22): tabel `assets` baru lahir `PR-02-10` (Phase 02) dan reservasi baru lahir Phase 02 juga, sehingga "lokasi bermuatan aset tidak dapat dinonaktifkan" tidak dapat ditegakkan sungguhan hari ini. Yang dibangun adalah KERANGKA hierarki: gedung ditolak dinonaktifkan selagi masih memiliki ruangan AKTIF di bawahnya (proxy struktural, bukan pemeriksaan aset); ruangan sendiri TANPA pemeriksaan (tidak ada yang bisa diperiksa) — keduanya diserahkan ke `PR-02-10` (§10). `m03-locations.md` §7 disunting menambah dua endpoint `PATCH .../status` yang belum terdaftar. | [#50](https://github.com/HanzzzBD/SIGM4/pull/50) |
| 18 September 2026 | `PR-01-04` tergabung ([#48](https://github.com/HanzzzBD/SIGM4/pull/48)). `PR-01-05` dibuka di `feature/PR-01-05-skema-lokasi-crud` (dari `develop` setelah `PR-01-04` tergabung): modul `m03-locations` lahir pertama kali — skema `buildings`→`areas`→`rooms` (`0014`, expand) dan `POST /buildings`·`/areas`·`/rooms` + `PUT /rooms/{id}` (`FR-03.1`, BR-013, BR-014). `PUT /rooms/{id}` SENGAJA tanpa field `status` — penonaktifan berjenjang (`BR-015`, `FR-03.1 A2/A3`) tetap milik `PR-01-06`. Ditemukan saat menulis migration: Bab 11.3 tidak mendaftarkan status lokasi (Aktif/Nonaktif) meski `m03-locations.md` §8 mencantumkannya sebagai atribut `buildings`/`rooms` — pola identik keputusan 1 (`user_status`) di `PR-01-01`. Satu keputusan teknis (21). | [#49](https://github.com/HanzzzBD/SIGM4/pull/49) |
| 17 September 2026 | `PR-01-03` tergabung ([#47](https://github.com/HanzzzBD/SIGM4/pull/47)). `PR-01-04` dibuka di `feature/PR-01-04-matriks-permission-role-version` (dari `develop` setelah `PR-01-03` tergabung): endpoint `GET /roles` + `PUT /roles/{id}/permissions`, kolom `role_version` (`0013`, expand), dan `PermissionCache` (`shared/auth/permission-cache.ts`) berkunci `perm:{user_id}:{role_version}` TTL 60 detik. Penguncian permission inti (`SDD-AUTH-10`) memakai kolom `permissions.inti` yang sudah ada sejak seed `PR-00-16`, bukan daftar statis baru — dan berlaku KHUSUS role Administrator (`R-01`) sesuai `SDD-03 §4.7`, dibuktikan lewat uji yang secara eksplisit mencabut permission inti dari role lain (R-03) dan berhasil. Satu keputusan teknis (20). | [#48](https://github.com/HanzzzBD/SIGM4/pull/48) |
| 16 September 2026 | `PR-01-02` tergabung ([#46](https://github.com/HanzzzBD/SIGM4/pull/46)). `PR-01-03` dibuka di `feature/PR-01-03-impor-massal-pengguna`. Sebelum kode ditulis, tiga titik tidak dapat diterapkan dari docs apa adanya dan dinaikkan ke pemilik produk (keputusan 19): `IMPT-04` merujuk `NT-42` yang ternyata milik `m16-analytics` untuk peristiwa berbeda (ekspor siap diunduh) — PRD diperbaiki, `NT-52` baru dibuat milik `m02-users`; tidak ada mekanisme unggah berkas di API ini (`stored_files` eksplisit Phase 03) — dipilih body JSON `base64`, bukan `multipart/form-data`; laporan `IMPT-02` bagi jalur asinkron `IMPT-04` menuntut entitas baru (tabel + endpoint pengambilan) yang tidak ada di model data PRD — cakupan PR ini dipersempit ke impor SINKRON ≤ 200 baris saja (batas `IMPT-04` sendiri), sisanya (`IMPT-03`, `IMPT-04`, `NT-52`) dipindah ke `PR-01-17` baru (rencana 165 → **166** PR). | — |
| 16 September 2026 | `PR-01-02` dibuka di `feature/PR-01-02-crud-pengguna-soft-delete` (dari `develop` setelah `PR-01-15` tergabung, [#45](https://github.com/HanzzzBD/SIGM4/pull/45)). Modul `m02-users` lahir pertama kali: `GET/POST /users`, `GET/PUT /users/{id}`, `PATCH /users/{id}/status` — impor massal (`PR-01-03`), matriks permission (`PR-01-04`), reset password/2FA tidak termasuk (tidak terjadwal PR ini). Password sementara dibangkitkan server (bukan diterima klien), lolos `checkPasswordPolicy` (`PR-01-16`) dengan konstruksi. Dua keputusan pemilik produk (16–17) dan satu masalah tooling ditemukan dan diperbaiki pada PR yang sama (§7): `scripts/check_import_boundaries.mjs` menghapus seluruh folder modul contoh bernama `m01-auth`/`m02-users` di akhir tiap jalan — nama itu sekarang modul PRD sungguhan. | — |
| 16 September 2026 | `PR-01-15` dibuka di `feature/PR-01-15-middleware-otorisasi`. `authorize()` menegakkan permission atas `AuthContext` yang sudah ada di `res.locals`; `authenticate` (verifikasi token) tetap di luar cakupan — menyusul `PR-02-02`. `/health` ringkasan dipasang `healthSummaryRouter` di belakang otorisasi dan terdaftar ke registri; dua uji `health-http.test.ts` yang mengklaim ringkasan TIDAK terpasang diperbarui ke perilaku barunya (404 → 401). Uji SEC-T-01 dibangun tergenerate dari `registry.guarded()`, bukan daftar route yang ditulis manual. Tiga keputusan teknis (13–15) dan satu koreksi rujukan (§7). | [#45](https://github.com/HanzzzBD/SIGM4/pull/45) |
| 16 September 2026 | `PR-01-16` tergabung ([#43](https://github.com/HanzzzBD/SIGM4/pull/43)), **dengan *squash*** — `BRANCHING §3` kembali ditaati setelah #41 dan #42 memakai *merge commit*. Skema label PR ditetapkan dan langsung diterapkan (keputusan 12): 13 label phase dan jenis dibuat, sembilan label bawaan GitHub dihapus setelah dipastikan tidak dipakai satu pun issue maupun PR. Pertanyaannya datang dari pemilik produk — label memang tersedia di repositori tetapi tidak pernah dipakai di 43 PR, dan ternyata tidak pernah diminta docs mana pun. | [#43](https://github.com/HanzzzBD/SIGM4/pull/43) |
| 16 September 2026 | `PR-01-01` tergabung ([#42](https://github.com/HanzzzBD/SIGM4/pull/42)). `PR-01-16` dibuka di `feature/PR-01-16-hash-password`. Empat keputusan pemilik produk diambil sebelum kode ditulis (7–10): letak kode, pustaka Argon2id, cakupan `NFR-S-03a`, dan letak parameternya. Temuan saat menurunkan rujukan: baris rencana `PR-01-16` — warisan `PR-02-01` — menyebut `NFR-S-01`, yang sebenarnya soal HTTPS/TLS (§7). | [#43](https://github.com/HanzzzBD/SIGM4/pull/43) |
| 15 September 2026 | `PR-01-01` dibuka di `feature/PR-01-01-skema-users`. Sebelum migration ditulis, empat titik ternyata tidak dapat diterapkan dari docs apa adanya dan dinaikkan ke pemilik produk (keputusan 1–4): Bab 11.3 tidak mendaftarkan status pengguna, penanda 2FA berejaan tiga, akun berpassword sementara lahir sebelum hashing Argon2id tersedia, dan foto profil menuntut `stored_files` yang belum ada. | [#42](https://github.com/HanzzzBD/SIGM4/pull/42) |
| 15 September 2026 | Rencana disunting sebelum phase dimulai: middleware otorisasi `PR-02-09` dipindah menjadi `PR-01-15` dan mendahului seluruh PR endpoint (keputusan 63). Phase 01 boleh dimulai selagi Phase 00 `In Review`, lewat `DELIVERY-PLAN §10` butir 1 (keputusan 62). Keduanya dicatat di [log phase-00 §2](phase-00.md). | — |

## 2. Keputusan yang diambil

Keputusan teknis yang tidak berasal dari PRD maupun SDD, dan alasannya. Bila sebuah keputusan ternyata menyentuh requirement atau desain, ia **wajib** dinaikkan ke PRD/SDD, bukan diselesaikan di sini.

| # | Keputusan | Alasan | Menaikkan ke PRD/SDD? |
|:---:|---|---|:---:|
| 33 | (a) **Semua impor dicatat** di tabel baru `user_import_jobs` (sinkron ≤ 200 baris maupun asinkron), dan idempotensi `IMPT-03` **global per hash**: berkas identik dalam 24 jam, dari Administrator mana pun, mengembalikan pekerjaan sebelumnya; pekerjaan `GAGAL` tidak di-*replay*; pencarian+penyisipan dilindungi `pg_advisory_xact_lock` atas hash. (b) **Respons `POST /users/import` diseragamkan** menjadi representasi pekerjaan `{id, status, total, terproses, sukses, gagal, laporan_gagal[], pesan_galat, selesai_pada, dibuat_pada}` — `200` bila berakhir, `202` bila `MENUNGGU`/`BERJALAN`, `meta.idempotent_replay` — dan menggantikan `baris[]` (semua baris) milik `PR-01-03` dengan `laporan_gagal[]` (hanya baris gagal). (c) `GET /users/import/{id}` berpermission `user.create`, terbuka bagi semua Administrator. (d) Penjadwalan lewat **outbox**: `UserImportRequested` terbit dalam transaksi yang sama dengan baris pekerjaan; handler pada worker memasukkan pekerjaan `user-import` ke antrean `sigm4-jobs` dengan `jobId` tetap; `JobDefinition.cron` menjadi opsional (pekerjaan sesuai permintaan). (e) **Pelaku = pengunggah**: worker membangun ulang `AuthContext`-nya dari basis data (`PermissionCache`); akun harus `AKTIF` dan masih memegang `user.create`, bila tidak pekerjaan `GAGAL`. Bukan `SYSTEM`. (f) `JOB-06`: titik lanjut `baris_terproses` per baris; percobaan terakhir yang masih gagal menutup pekerjaan `GAGAL`. (g) `NT-52`: event `UserImportCompleted` **terbit sekarang** (hanya jalur asinkron), konsumennya dipasang `PR-02-25`. (h) `berkas` dikosongkan saat pekerjaan berakhir (`DP-03`). (i) Aksi log baru `USER_IMPORT_REQUESTED`; `USER_IMPORTED` juga menutup pekerjaan `GAGAL`. | (a) `IMPT-03` tidak membedakan ukuran berkas; pencatatan hanya jalur asinkron membiarkan impor kecil — jalur terlazim — tanpa idempotensi. Global (bukan per pengunggah) karena hanya role Administrator yang memegang `user.create` dan pengunggahan ulang oleh Administrator lain adalah kasus yang sama; `GAGAL` dikecualikan agar berkas yang ditolak dapat diunggah ulang. Kunci advisory karena pencarian lalu penyisipan tanpa kunci membiarkan dua unggahan bersamaan sama-sama lolos. (b) Dua bentuk respons per endpoint memaksa klien menebak; `IMPT-02` hanya menuntut alasan galat per nomor baris, dan menyimpan setiap baris sukses pada `jsonb` yang ditambah per baris membuat penulisan kuadratik. Tidak ada klien (`apps/web` belum dibangun). (c) Permission yang sama dengan pengunggahan; hak baca terpisah tidak diminta dan replay lintas-Administrator menuntutnya. (d) `CLAUDE.md`/`SDD-EVT-04`: tidak ada efek samping setelah `COMMIT` tanpa outbox — memasukkan ke BullMQ langsung dari API membuka jendela pekerjaan hilang; `jobId` tetap karena outbox at-least-once (`SDD-EVT-07`). (e) `SystemAuthContext` baru lahir di `PR-02-32`, dan impor memang tindakan Administrator yang harus tercatat atas namanya (`AL-01`); membaca ulang saat berjalan menghormati pencabutan role/penonaktifan di antara unggah dan proses (`PM-05`). (f) Pemrosesan baris tidak idempoten (membuat pengguna); tanpa titik lanjut, percobaan ulang melaporkan baris yang sudah dibuat sebagai "sudah digunakan". (g) Acceptance meminta notifikasi saat selesai, tetapi `M-17` belum ada; menerbitkan event sekarang aman (dalam transaksi penutupan) dan membuat `PR-02-25` hanya perlu konsumen — berbeda dari `NT-48` yang terbit dari transaksi yang ditolak. (h) `DP-03`: berkas memuat NIS/NIP dan email. (i) `AL-01` mewajibkan setiap penulisan tercatat; `USER_IMPORTED` sudah bermakna "impor beserta ringkasan hasil". | Ya — PRD: `m02-users.md` (FR-02.1 A4, §7 endpoint baru, §8 entitas, §11 aksi), `data-model.md` (entitas + Bab 11.3 `Status Impor Pengguna`); SDD: `SDD-05` (`SDD-DB-22`, §4.7f), `SDD-07 §4.3` (dua event); `phase-02.md` `PR-02-25`. (a), (b), (c), (g) dikonfirmasi pemilik produk (19 September 2026), begitu pula ukuran `L` (tidak dipecah) dan penundaan masa simpan laporan sebagai butir terbuka; (d)–(f), (h), (i) pelaksanaan. |
| 32 | (a) **Gerbang menolak** (bukan "lahir NONAKTIF"): pembuatan dan impor akun siswa tanpa `consent_wali: true`, pengaktifan kembali tanpa penanda, dan penggantian role menjadi Siswa tanpa penanda ditolak `VALIDATION_ERROR` (`rule: DP-02`). (b) **`NT-48` ditunda ke `PR-02-25`** (M-17): penolakan dikembalikan langsung kepada pemanggil dan tercatat di log; tidak ada event tanpa konsumen. (c) Penanda: cap waktu dari `Clock` (klien hanya menyatakan `consent_wali`), direkam **sekali**, **tidak dicabut** dan tidak ditimpa, dan **hanya** untuk akun R-07. (d) Gerbang di service, **bukan trigger** basis data. (e) Menyunting siswa yang sudah ada tanpa penanda (mis. dibuat sebelum gerbang) **tidak diblokir** selama role tidak berubah. (f) `UserService` menerima `Clock` sebagai parameter keempat (bawaan `SystemClock`) dan `UsersModuleDeps.clock` opsional. | (a) `E.5.2` sudah mewajibkan `consent_wali = true` bagi siswa saat impor, dan pilihan itu tidak mengubah "lahir Aktif" `FR-02.1` langkah 4; pilihan lain menuntut PRD disunting dan status baru. (b) Modul notifikasi baru ada di Phase 02, dan penolakan me-rollback transaksinya sehingga event outbox di dalamnya hilang (`SDD-EVT-04`); event tanpa konsumen ditandai selesai tanpa efek oleh dispatcher. (c) `DP-03` (minimisasi) dan `SDD-SYS-07`; pencabutan persetujuan tidak didefinisikan `DP-02`. (d) Gerbang bergantung pada role (lintas tabel) dan trigger menambah cakupan di luar acceptance; ditegakkan pada tiga titik service yang teruji. (e) Memblokir penyuntingan akun lama menghukum data yang sudah ada tanpa manfaat perlindungan. (f) Waktu wajib dari `Clock` yang di-inject. | Ya — PRD: `data-model.md` §11.1 + ERD dan `m02-users.md` §8 (atribut `consent_guardian_at`), `m02-users.md` `FR-02.1` A5; SDD: `SDD-05` (`SDD-DB-21`, §4.7e). (a) dan (b) dikonfirmasi pemilik produk (19 September 2026). |
| 31 | Tiga butir terbuka `PR-01-13` diputuskan pemilik produk (19 September 2026) lewat pertanyaan terstruktur: (a) **`SystemAuthContext`** menjadi **`PR-02-32`** di Phase 02, bergantung `PR-02-02`, tinjauan arsitek, sekaligus memasang `student-graduation`. (b) **`SL-05` + `DP-04`** menjadi **`PR-05-26`** di Phase 05, setelah `PR-05-09`. (c) **Rancangan PRD/SDD yang ditulis sebelum kode disetujui apa adanya**: baris endpoint kalender akademik dan unit kerja (keputusan 29, untuk `PR-01-18`); `student_enrollments`, `POST /class-promotions`, tiga aksi log `STUDENT_*`, baris pekerjaan `student-graduation` (keputusan 30); `SDD-DB-17` … `SDD-DB-20`. (d) Tugas `PR-05-09` ditambah: pemeriksa kewajiban didaftarkan di proses API **dan** worker. Rencana 167 → **169** PR. | (a) `AuthContext` diselesaikan saat autentikasi nyata (`PR-02-02`) masuk; merancang pelaku SYSTEM sebelum itu berisiko dirancang dua kali. (b) `DP-10` mengecualikan akun berkewajiban, dan definisinya baru ada di `PR-05-09`. (c) Keputusan 29 dan 30 meminta peninjauan sebelum `PR-01-18`/`PR-05-09` dikerjakan; ini penutupnya. (d) Registri hidup per proses, sedangkan penonaktifan lulusan berjalan di worker. | Ya — rencana: `phases/phase-02.md` §7/§8, `phases/phase-05.md` §7/§8, `DELIVERY-PLAN.md`, `IMPLEMENTATION-STATUS.md`, `README.md`, `IMPLEMENTATION/README.md`, `CLAUDE.md`, `scripts/validate_impl.py`. Tidak ada perubahan PRD/SDD baru; (c) hanya mencatat persetujuan. |
| 30 | (a) `PR-01-13` mengambil **rancangan penuh**: tabel `student_enrollments` (`user_id`, `academic_year_id`, `kelas_id` → `work_units` berjenis `KELAS`, `lulus`; `UNIQUE (user_id, academic_year_id)`), `POST /class-promotions` (`user.update`), `GraduationService`, dan `StudentObligationRegistry`. (b) Menandai `LULUS` hanya menandai baris tahun ajaran itu; **penonaktifan menunggu `tanggal_selesai` tahun itu lewat** (hari WIB, `CAL-03`), diblokir oleh kewajiban (`SL-04`). (c) `SL-04` berlaku bagi akun **Siswa/OSIS (`R-07`)**, di dua jalur: `PATCH .../status` dan penonaktifan lulusan; penandaan `LULUS` menampilkan kewajiban tanpa menggagalkan penandaan. (d) Registri kewajiban **kosong** sampai `PR-05-09`; kosong berarti "belum ada definisi", bukan "tanpa kewajiban". (e) Pekerjaan `student-graduation` **tidak dipasang** ke worker; barisnya tetap masuk PRD Bab 26.7. (f) Batas 200 siswa per permintaan sinkron (pola `IMPT-04`). (g) `NAIK` atas baris yang sudah ada mengganti kelas dan mencabut tanda lulus. | (a) Dikonfirmasi pemilik produk (19 September 2026) — PRD tidak mendefinisikan satu pun dari empat komponen itu. (b) `SL-03` menyebut "otomatis dinonaktifkan pada akhir tahun ajaran"; menandai sebelum tahun berakhir tidak boleh menonaktifkan seketika. (c) `SL-04` ada di siklus akun siswa (E.4); menerapkannya ke semua role melampaui requirement. (d) `SL-04` menuntut "jangan hardcode 'tidak ada kewajiban'" (`phase-01.md` §8): registri menyediakan tempat pendaftaran, definisi milik Phase 05. (e) Pelaku job `SYSTEM` (`AL-06`) membutuhkan `SystemAuthContext` yang belum ada; `AuthContext.userId: number` dipakai `updated_by` pada seluruh repository — mengubahnya adalah perubahan tulang punggung yang memerlukan tinjauan arsitek. (f) Klien membagi kelompok yang lebih besar; sinkron menjaga `SDD-PERF-06`. (g) Koreksi kelas adalah kebutuhan nyata; log memuat nilai lama dan baru. | Ya — PRD: `data-model.md` §11.1, `m02-users.md` §7/§8/§11, `availability-concurrency.md` §26.7 (baris pekerjaan), dan `_generated/`; SDD: `SDD-05` (`SDD-DB-20`, §4.7d). (a) dan (e) dikonfirmasi pemilik produk. Rancangan baris PRD ditulis di sini — peninjau PRD wajib memeriksanya. |
| 29 | (a) Celah endpoint **ditutup di Phase 01**: `PR-01-18` baru (endpoint tahun ajaran & semester, hari libur, hari kerja, unit kerja), baris endpoint lebih dulu di PRD; rencana 166 → **167** PR. (b) Pemetaan `map_users_unit_kerja()` **tetap langkah operasional** — tanpa endpoint, permission, atau aksi log baru — didokumentasikan sebagai runbook. (c) Runbook `master-data-awal.md` berlaku sebagai jembatan sampai `PR-01-18` tergabung, tanpa mengubah scope PRD. (d) Rancangan baris endpoint (path, permission `setting.view`/`setting.manage` sesuai `P-71`/`P-72`, aksi log) adalah **rancangan awal yang ditulis di sini** — peninjau PRD wajib memeriksanya sebelum `PR-01-18` dikerjakan. | (a) DoD Phase 01 menuntut kalender terisi dan empat modul dapat didemokan mandiri kepada Administrator; jalur SQL tidak memenuhinya dan penulisan SQL tidak tercatat di `activity_logs` (`AL-01`). Baris endpoint wajib masuk PRD dulu (satu baris, satu pemilik). (b) Belum ada data produksi; fungsinya idempoten dan dapat diulang; mekanisme resmi baru berguna bersama layar laporan tak-terpetakan yang bukan bagian requirement mana pun. (c) Tanpa runbook, pengisian master hari ini hanya diketahui penulis migration. (d) `P-71`/`P-72` menetapkan permission dan halaman, bukan path. | Ya — (a): `m20-settings.md` §7, §8, §11 (dan indeks `_generated/` yang diturunkan), `phases/phase-01.md` §7/§8, `DELIVERY-PLAN.md`, `IMPLEMENTATION-STATUS.md`, `README.md`, `IMPLEMENTATION/README.md`, `CLAUDE.md`, `scripts/validate_impl.py`; dikonfirmasi pemilik produk (19 September 2026). (b) dan (c) pelaksanaan. |
| 28 | (a) Dua kelompok ditambahkan ke PRD Bab 11.3: **Jenis Unit Kerja** (`work_unit_type`) dan **Status Unit Kerja** (`work_unit_status`). (b) Migrasi data lewat fungsi SQL idempoten `map_users_unit_kerja()` (`SDD-DB-19`), tanpa membuat master dari teks. (c) `work_unit_id` MENGGANTIKAN `unit_kerja` pada kontrak API pengguna (body, respons, filter, impor) — tanpa masa transisi dua-field. (d) TANPA endpoint `work_units` (pola keputusan 27). (e) Atribut `users` pada `m02-users.md` §8, `data-model.md` §11.1, dan ERD diselaraskan dari `unit_kerja` ke `work_unit_id`. (f) Keunikan `kode`/`nama` unit ditegakkan pada `lower(btrim(...))`, dan FK tanpa `ON DELETE` (RESTRICT) menegakkan `WU-02`. (g) Validasi "unit ada dan aktif" di `UserService`, dan hanya bila nilainya baru/berubah; `UserRepository` membaca `work_units` langsung (pola keputusan 23). | (a) `SDD-DB-02` + `tests/enums.test.ts`, pola keputusan 1/21/26/27. (b) `work_units` lahir KOSONG — pemetaan hanya bermakna setelah Administrator mengisinya, jadi harus dapat dijalankan ulang; membuat unit dari teks butuh `jenis` yang tidak dapat ditebak (`WU-01`, risiko `phase-01.md` §10: "tidak ada penebakan"). (c) `WU-01` berbunyi "menggantikan"; belum ada klien web/mobile sehingga tidak ada konsumen yang rusak, dan dua sumber kebenaran sampai contract adalah persis yang dihindari pola *expand → contract*. (d) `m20-settings.md` §7 tidak mendaftarkan endpoint unit kerja meski halaman `P-72` ada; baris endpoint baru wajib masuk PRD dulu. (e) `WU-01` sudah memutuskannya; daftar atribut hanya tertinggal. (f) Pencocokan pemetaan dan impor memakai bentuk ternormalisasi yang sama — tanpa keunikan yang sama satu teks dapat cocok dua unit; FK RESTRICT lebih ketat dari "pengguna aktif" sehingga pengguna nonaktif tidak kehilangan unitnya diam-diam. (g) Unit yang dinonaktifkan tidak boleh memblokir penyimpanan ulang pengguna yang sudah memakainya (`WU-02`: dinonaktifkan, bukan dilepas). | Ya — (a) dan (e): PRD Bab 11.3, `m02-users.md` §8, `data-model.md`, `SDD-05` (`SDD-DB-19`, §4.7c); (a)–(c) dikonfirmasi pemilik produk (19 September 2026) sebelum kode ditulis. (d) meninggalkan butir terbuka di §10. |
| 27 | (a) `PR-01-11` **skema saja** — migration + tipe Kysely + invariant basis data + uji; TANPA service/endpoint. (b) Kelompok **Nama Semester** (Ganjil, Genap) ditambahkan ke PRD Bab 11.3 → enum `academic_term_name`. (c) "Tepat satu tahun ajaran aktif" ditegakkan basis data: partial unique index + constraint trigger `DEFERRABLE INITIALLY DEFERRED` (`SDD-DB-18`); tabel kosong sah. (d) `academic_years`/`academic_terms` memakai kolom baku `SDD-05 §4.2` (entitas domain milik Administrator). (e) "Semester berada di dalam rentang tahun ajarannya" TIDAK ditegakkan basis data — diserahkan ke service kalender akademik. | (a) Judul rencana hanya menyebut skema, dan `m20-settings.md` §7 hanya mendaftarkan `/settings` dan `/health` — baris endpoint baru wajib lebih dulu masuk PRD (satu baris, satu pemilik), jadi membangun endpoint di sini berarti requirement lahir saat implementasi. (b) `tests/enums.test.ts` + `SDD-DB-02`, pola keputusan 1/21/26. (c) Constraint basis data tidak dapat dilewati service yang keliru (alasan `SDD-DB-14`); deferred karena pergantian aktif sesaat tanpa tahun aktif. Tidak dapat `CHECK` (lintas baris). (d) `SDD-05 §4.2`: hanya master data acuan (`holidays`, `work_days`) yang dikecualikan. (e) Menegakkannya di basis data menuntut trigger dua arah (semester dan tahun) tanpa pemilik logika yang jelas; tidak tercakup acceptance PR ini. | Ya — (b): PRD Bab 11.3, dan `SDD-05` (`SDD-DB-18`, §4.7b); (a) dan (b) dikonfirmasi pemilik produk (19 September 2026) sebelum kode ditulis. Celah endpoint dicatat di §10 — memerlukan keputusan pemilik produk, tidak dinaikkan sendiri. |
| 26 | (a) Katalog `system_settings` `PR-01-10` HANYA memuat enam parameter yang nilai bawaannya tertulis eksplisit di `FR-20.1` (`peminjaman.batas_perpanjangan` 1, `denda.cap_persen` 30, `reservasi.horizon_hari` 90, `reservasi.ttl_tentative_jam` 48, `reservasi.kuota_tertunda_guru_staf` 5, `reservasi.kuota_tertunda_siswa_osis` 2); kunci lain ditambah PR konsumennya sebagai baris seed tanpa ubah skema. (b) Dua kelompok ditambahkan ke PRD Bab 11.3: `Kelompok Parameter Sistem` (`setting_group`, sepuluh tab P-70) dan `Tipe Parameter Sistem` (`setting_type`: `BILANGAN_BULAT`/`DESIMAL`/`BOOLEAN`/`TEKS`). (c) Rentang tiap parameter adalah pagar kewajaran teknis yang ditulis di `SDD-05 §4.7a` (mis. cap 1–100, TTL 1–168 jam), BUKAN business rule. (d) Kalender Akademik (`academic_years`, `PR-01-11`), Satuan Bahan (`material_units`), dan logo (`stored_files`) tidak masuk `system_settings`. (e) Tanpa cache 60 detik `SDD-14` sampai ada konsumen pertama; `GET` langsung ke basis data. | (a) Sisa parameter FR-20.1 (jam operasional, tarif denda, durasi sesi, dst.) tidak punya angka di PRD — mengarangnya berarti keputusan produk oleh tim teknis, sementara `SDD-05 §4.6` menyerahkan katalog ke PR ini tanpa isi. (b) `tests/enums.test.ts` mewajibkan setiap enum migration terdaftar di Bab 11.3 — pola identik keputusan 1 dan 21; `text` + `CHECK` ditolak `SDD-DB-02`. Kode tipe memakai istilah Indonesia agar turunan mekanis dari label PRD (`kodeTeknis`). (c) PRD hanya menulis "rentang nilai yang wajar" tanpa angka; rentang tinggal di kolom baris seed sehingga menyesuaikannya cukup migration seed, tanpa kode. (d) Ketiganya entitas/kolom berbeda yang `FR-20.1` sekadar kelompokkan di layar. (e) Belum ada pembaca parameter mana pun; cache tanpa konsumen adalah kompleksitas tanpa manfaat terukur, dan pembacaan langsung sudah memenuhi "berlaku pada transaksi berikutnya". | Ya — (b): PRD Bab 11.3 (`data-model.md`) dan `SDD-05` (`SDD-DB-17`, §4.6, §4.7a); (a) dan (b) dikonfirmasi pemilik produk (19 September 2026) sebelum kode ditulis. (c)–(e) pelaksanaan, tidak dinaikkan. |
| 25 | Cakupan `PR-01-09` dipersempit sebelum kode ditulis: (a) format ekspor **XLSX saja** — PDF ditunda; (b) eksekusi **sinkron** (respons HTTP langsung, dibatasi `BATAS_EKSPOR = 5000` baris), bukan lewat outbox/worker. | (a) `exceljs` (XLSX) sudah dependency terpakai (`PR-01-03`); PDF sistem eksplisit milik pipeline Playwright/Chromium (`SDD-FS-12`) yang belum dibangun PR manapun — menambah pustaka PDF lain sekarang menyimpang dari desain yang sudah ditetapkan. (b) `SDD-07 §3` mengklasifikasikan "Ekspor & PDF" asinkron, tetapi jalur itu menuntut `stored_files` (`SDD-09`, eksplisit ditunda `PR-03-04`) dan handler worker ekspor — keduanya tidak ada di Phase 01. Pola sama `PR-01-06`/`PR-01-07` ("kerangka" menunggu infrastruktur phase lain): batas baris menjaga `SDD-PERF-06` ("> 5 detik wajib asinkron") tetap terpenuhi tanpa infrastruktur itu, dan A1 ("hasil filter sangat besar... ekspor diproses asinkron") diterjemahkan menjadi penolakan `VALIDATION_ERROR` yang menyarankan penyempitan filter — sejalan semangat A1 tanpa perlu antrean sungguhan. | Tidak — pembatasan cakupan implementasi terhadap infrastruktur yang belum ada, pola identik keputusan 22/23; dikonfirmasi pemilik produk (19 September 2026) sebelum kode ditulis. |
| 24 | `GET /activity-logs` (`PR-01-08`) mengembalikan `nilai_sebelum`/`nilai_sesudah` LANGSUNG pada setiap baris daftar — tidak ada endpoint detail (`GET /activity-logs/{id}`) terpisah, karena `m18-activity-log.md` §7 tidak mendaftarkannya. `entitas_id` ditambahkan sebagai filter opsional berpasangan dengan `entitas` (tidak disebut eksplisit di Main Flow langkah 3, tetapi diperlukan A2 "Melihat riwayat satu entitas"). Operasi BACA ini dibungkus `withTransaction` semata agar `AuditLogger.write()` dapat mencatat `ACTIVITY_LOG_VIEWED` (`m18-activity-log.md` §11) — katalog itu eksplisit mewajibkannya untuk AKSES, bukan hanya perubahan data (beda dari `BR-071` yang bicara soal tulis). Kriteria "≤3 detik" (`FR-18.2` AC) TIDAK diuji lewat assersi waktu — volume data uji (belasan baris) tidak bermakna dibanding target (rentang 1 bulan data produksi); repository memakai keempat indeks `activity_logs` yang sudah ada (`PR-00-13`) sebagai bukti tidak langsung, diperiksa sungguhan saat gerbang keluar phase. | `FR-03.2` sudah menunjukkan pola FR dan endpoint boleh dimiliki modul berbeda (keputusan 23) — di sini sama: satu-satunya tempat pengguna "membuka detail entri" (langkah 4) tanpa endpoint kedua adalah data yang sudah ada di baris daftar. `entitas_id` tanpa `entitas` tidak berguna (banyak modul memakai id berurutan sendiri-sendiri), jadi keduanya selalu dipasangkan. Uji timing pada data kecil akan SELALU hijau tanpa membuktikan apa pun — pola yang sama seperti kriteria "≤2 detik" pada modul lain yang diperiksa di gerbang phase, bukan per PR. | Tidak — pelaksanaan langsung dari `m18-activity-log.md` §7/§11 dan pola `FR-03.2`/`PR-01-07`. |
| 23 | `GET /rooms/{id}/assets` (FR-03.2, kerangka) dibangun di modul BARU `apps/api/src/modules/m04-assets` dengan permission `asset.view` — BUKAN di `m03-locations` dengan `location.view` seperti percobaan pertama. `AssetRepository.roomExists()` mengueri tabel `rooms` LANGSUNG (bukan lewat repository `m03-locations`, dilarang lintas modul) — sah karena `m04-assets.md` §13 menyatakan M-04 bergantung M-03 justru pada data ini. Modul M-04 belum masuk "Modul PRD" Phase 01 (`phases/phase-01.md` header: M-02, M-03, M-18, M-20) — `phases/phase-01.md` TIDAK disunting karena satu endpoint kerangka baca-saja tidak mengubah cakupan modul phase secara substansial; dicatat cukup di sini. | Percobaan pertama (path sama, permission `location.view`, ditaruh di `m03-locations.md` §7) ditolak `scripts/audit_docs.py` ("Endpoint duplikat... ada di m03-locations.md, m04-assets.md") — `GET /rooms/{id}/assets` SUDAH dimiliki `m04-assets.md` §7 dengan permission `asset.view` sejak PRD ditulis (bersanding dengan `/assets`, `/assets/{id}`, dst.). FR-03.2 (alur pengguna, dimiliki M-03) dan endpoint yang memenuhinya (dimiliki M-04) adalah dua hal berbeda — bukan pertentangan, sekadar kesalahan penulis mengasumsikan tanpa memeriksa `grep` ID lebih dulu (persis yang diperingatkan `CLAUDE.md` §Workflow AI butir 1). Diperbaiki SEBELUM commit pertama PR ini dibuat — tidak ada kode yang sempat tergabung salah tempat. | Tidak — koreksi kepemilikan endpoint terhadap dokumen yang SUDAH ada, bukan keputusan baru. |
| 22 | Cakupan `BR-015`/`FR-03.1 A2/A3` di `PR-01-06` dipersempit sebelum kode ditulis. Dibangun: `GET /locations/tree` (pohon lengkap, tanpa penyaringan status) dan `PATCH /buildings/{id}/status` dengan KERANGKA hierarki — gedung ditolak (`VALIDATION_ERROR`, `detail.rule: "BR-015"`) selagi memiliki ruangan berstatus `AKTIF` di bawahnya (query `rooms` × `areas`, TANPA menyentuh `assets`). `PATCH /rooms/{id}/status` TANPA pemeriksaan apa pun — BR-015 di tingkat ruangan (memuat aset) dan `FR-03.1 A2` (reservasi mendatang) sama sekali tidak ditegakkan hari ini. Dua endpoint `PATCH .../status` ditambahkan ke `m03-locations.md` §7 (belum terdaftar sebelumnya). `m03-locations.md` §8 tidak disunting: kolom `status` sudah ada sejak `PR-01-05`. | Tabel `assets` baru lahir `PR-02-10` (Phase 01 tidak memilikinya sama sekali) dan reservasi (`booking_slots`) juga Phase 02 — BR-015 sebagaimana tertulis ("lokasi bermuatan aset") dan `FR-03.1 A2` (reservasi) TIDAK DAPAT ditegakkan sungguhan pada PR ini. Opsi "lewati seluruh penonaktifan" ditolak: judul rencana phase eksplisit "penonaktifan berjenjang", dan hierarki gedung→ruangan sudah cukup untuk memberi makna nyata pada "berjenjang" (bottom-up: ruangan dinonaktifkan lebih dulu) tanpa menunggu Phase 02 — pola identik `PR-01-07` ("kerangka; data menyusul Phase 02") yang sudah diterima rencana phase untuk masalah yang sama persis (fitur menunggu tabel `assets`). Dipilih & dikonfirmasi pemilik produk SEBELUM implementasi (bukan ditemukan di tengah jalan) — lihat §10 untuk item yang diserahkan ke `PR-02-10`. | Tidak — pelaksanaan pembatasan cakupan yang sudah tersirat rencana phase (`PR-01-07` pola sama); dikonfirmasi pemilik produk (18 September 2026) sebelum kode ditulis. |
| 21 | Kelompok **Status Lokasi** (Aktif, Nonaktif) ditambahkan ke Bab 11.3 (`data-model.md` §11.3); `buildings.status`/`rooms.status` bertipe enum `location_status` **dengan** nilai bawaan `AKTIF` (berbeda dari `user_status` — lihat alasan). Kode unik (`BR-014`) ditegakkan `UNIQUE (kode)` per tabel — GLOBAL pada tingkat itu, bukan bersyarat induk (`building_id`/`area_id`), sesuai bacaan harfiah "unik pada SETIAP TINGKAT hierarki". `PUT /rooms/{id}` (`m03-locations.md` §7) TIDAK menerima `status` — penonaktifan berjenjang (`BR-015`, `FR-03.1 A2/A3`) tetap `PR-01-06`. `penanggung_jawab_id` (FK ke `users`) TIDAK divalidasi eksistensinya di service — mengikuti pola `role_id` pada `CreateUserBodySchema` (`PR-01-02`), mengandalkan FK constraint sebagai jaring terakhir, bukan pemeriksaan lintas modul. | Bab 11.3 tidak mendaftarkan status lokasi meski `m03-locations.md` §8 mencantumkan `status` sebagai atribut `buildings`/`rooms` — pola identik keputusan 1 (`user_status`, `PR-01-01`). Nilai bawaan `AKTIF` (berbeda dari `user_status` yang sengaja tanpa bawaan): tidak ada aturan PRD manapun yang menuntut gedung/ruangan baru dinyatakan eksplisit sebelum aktif (berbeda dari `SL-06` yang menahan akun siswa). `BR-014` tidak menyebut "per gedung/area induk", dan menafsirkannya sebagai unik-per-induk berarti menebak konvensi penomoran sekolah yang tidak ditetapkan PRD manapun — dipilih bacaan yang tidak menuntut asumsi tambahan. `area_id`/`building_id` DIVALIDASI (bukan diserahkan ke FK) karena keduanya tabel MILIK modul sendiri (bukan lintas modul) dan pesan `VALIDATION_ERROR` yang jelas lebih berguna daripada `23503` mentah; `penanggung_jawab_id` menunjuk `users` — modul LAIN — dan `check_import_boundaries.mjs` melarang repository memanggil repository modul lain, sehingga divalidasi butuh query lintas tabel yang sama persis dengan yang sudah diterima diam-diam untuk `role_id` sejak `PR-01-02`. | Ya (Bab 11.3) — dinaikkan dan diputuskan pemilik produk (18 September 2026); `data-model.md` §11.3, `tests/helpers/bab113.ts` disunting. Sisanya pelaksanaan murni, tidak dinaikkan. |
| 20 | `GET /roles` mengembalikan matriks permission (kode+scope) DAN `jumlah_pengguna` per role dalam SATU respons — tidak ada endpoint detail (`GET /roles/{id}`) terpisah. `PermissionCache` (`shared/auth/permission-cache.ts`) membaca role_id+`role_version` pengguna FRESH setiap panggilan (kueri primer murah, indeks `users.role_id`), dan HANYA meng-cache hasil join `role_permissions`×`permissions` (kueri yang lebih mahal). Penguncian permission inti (`SDD-AUTH-10`) memakai kolom `permissions.inti` yang sudah ada sejak seed `PR-00-16`, bukan daftar statis baru di kode. | `m02-users.md` §7 hanya mendaftarkan `GET /roles` dan `PUT /roles/{id}/permissions`; `FR-02.2` langkah 2 menuntut UI melihat jumlah pengguna dan permission aktif per role SEBELUM matriks dibuka, dan satu-satunya tempat memuat data itu tanpa endpoint baru adalah `GET /roles`. `SDD-03 §4.5` menetapkan BENTUK kunci cache (`perm:{user_id}:{role_version}`), bukan query mana yang boleh selalu-fresh vs di-cache — pemisahan primer/mahal ini murni CARA memenuhi `PM-05` tanpa mengubah perilaku yang dijanjikan, tanpa business rule/permission/endpoint/aksi log baru, dan semata melayani requirement yang sudah ada — lolos uji tiga syarat `SDD-AUTH-11` persis seperti `role_version` sendiri. Memakai kolom `inti` yang sudah ada menghindari dua sumber kebenaran (daftar statis vs kolom basis data) yang dapat berbeda diam-diam. | Tidak — mekanisme teknis, lolos uji tiga syarat `SDD-AUTH-11`. |
| 19 | (a) `NT-52` baru dibuat milik `m02-users.md` §9 untuk "impor pengguna selesai"; `IMPT-04` (`conventions.md`) dan `SDD-14 §4.6` diperbaiki dari rujukan `NT-42` yang salah. (b) `POST /users/import` menerima berkas lewat `content_base64` di body JSON, bukan `multipart/form-data` — tidak ada dependensi baru, konsisten dengan API ber-JSON murni. (c) `exceljs` dipilih sebagai pustaka parsing CSV/XLSX. (d) Cakupan `PR-01-03` dipersempit ke impor SINKRON ≤ 200 baris; `IMPT-03` (idempotensi hash-berkas) dan `IMPT-04` (>200 baris asinkron + `NT-52`) dipindah ke `PR-01-17` baru. `IMPT-05` (unduh templat) murni kepentingan `apps/web`, di luar PR backend mana pun di Phase 01. | (a) `NT-42` berkelas "berkas ekspor siap diunduh" (`m16-analytics`), bukan "impor selesai" — dipakai untuk peristiwa yang jelas berbeda akan melanggar aturan satu-pemilik notifikasi dan salah kaprah bagi pembaca `m16-analytics.md`. (b) `stored_files`/alur *presigned upload* (`SDD-09`) eksplisit ditunda ke `PR-03-04`, dan berkas impor bersifat transien (tidak pernah menjadi dokumen bisnis tersimpan) — tidak cocok dipaksakan ke alur itu; berkas CSV/XLSX 200 baris berukuran kecil, overhead base64 tidak berarti. (c) `xlsx`(SheetJS) berjejak CVE (*prototype pollution*, ReDoS) atas masukan berkas tak dipercaya — persis kelas ancaman `POST /users/import`; `exceljs` menangani CSV dan XLSX dengan satu dependensi, terpelihara aktif, tanpa CVE terbuka signifikan. (d) `IMPT-02` (laporan per baris) bagi jalur asinkron menuntut tabel job impor + endpoint pengambilan — entitas yang tidak ada di §8 `m02-users.md`; membangunnya sekarang berarti requirement lahir saat implementasi, bukan dinaikkan ke PRD lebih dulu. Ambang 200 baris bukan angka baru — `IMPT-04` sendiri sudah menyebutnya sebagai batas sinkron/asinkron. | Ya — (a) dan (d) dinaikkan dan diputuskan pemilik produk (16 September 2026); `m02-users.md` §9, `conventions.md`, `SDD-14 §4.6`, `phases/phase-01.md` §7, `IMPLEMENTATION-STATUS.md` disunting; rencana 165 → 166 PR. (b) dan (c) pelaksanaan murni, tidak dinaikkan. |
| 18 | SAST `js/missing-rate-limiting` (CodeQL `security-extended`) dikecualikan lewat `.github/codeql/codeql-config.yml` (`query-filters: exclude`) — HANYA query ini, seluruh `security-extended` lain tetap menggerbang. `init` di `ci.yml` memakai `config-file:` alih-alih `queries: security-extended` langsung. | Query ini hanya mengenali beberapa paket npm rate-limiter baku, bukan `RedisRateLimiter`/`batasi()` milik proyek ini (`SDD-13 §4.3`). `healthRouter`/`healthSummaryRouter` (`PR-01-15`) memakai pola identik dan lolos SAST. **Dua percobaan sebelumnya gagal dan DIBATALKAN**: (1) komentar `codeql[rule-id]` — bukan sintaks CodeQL yang sah; (2) komentar `lgtm[rule-id]` (sintaks benar) + gerbang `jq` diperbaiki membaca `.suppressions` — tetap gagal karena `sarif/*.sarif` adalah keluaran LOKAL `codeql-action/analyze` SEBELUM diunggah; korelasi komentar-penekanan ke `.suppressions` hanya terjadi di server GitHub SETELAH unggah, tidak pernah tertulis balik ke berkas lokal itu — dibuktikan langsung, bukan diasumsikan. Pengecualian di-scope ke SATU query (bukan mematikan `security-extended` seluruhnya) agar route baru yang benar-benar lupa `batasi()` tetap tertangkap query lain yang relevan. | Tidak — pelaksanaan; diputuskan pemilik produk (16 September 2026), tiga putaran (dua percobaan komentar-penekanan terbukti tidak dapat bekerja secara arsitektural, putaran ketiga baru menemukan akar penyebabnya dan menutupnya dengan benar). PR modul berikutnya yang menulis data nyata akan mengalami temuan sama dan otomatis lolos lewat `codeql-config.yml` ini — tidak perlu pola apa pun diulang per PR. Bila proyek butuh CodeQL benar-benar mengenali `batasi()`, jalannya *data extension* (`codeql/*-models`), dicatat di `codeql-config.yml` sebagai catatan penutup. |
| 17 | `scripts/check_import_boundaries.mjs` — nama folder modul contoh diganti dari `m01-auth`/`m02-users` menjadi `__contoh_modul_a__`/`__contoh_modul_b__`. | Skrip menghapus (`rmSync` rekursif) seluruh folder modul contoh di blok `finally` setiap kali dijalankan. Namanya sengaja meniru modul PRD nyata sejak `PR-00-02`, dan tidak masalah selama modul itu belum ada — begitu `PR-01-02` membangun `m02-users` sungguhan, `npm run check:boundaries` (dan `npm run lint` lewat itu bila dipanggil terpisah) menghapus seluruh isi modul yang sedang dikerjakan. Ditemukan langsung: modul hilang total tiga kali berturut-turut di sesi yang sama sebelum akar masalahnya terlihat pada baris `modulContoh` skrip ini. | Tidak — bug perkakas, bukan requirement/desain. |
| 16 | Batas admin aktif yang ditegakkan `UserService.updateStatus` adalah **minimal dua**, bukan minimal satu. | `BR-068` (m02-users.md, floor 1) dan `BR-070a` (m01-auth.md, floor 2, `RS-19`) sama-sama milik PRD; `SDD-05 §4.3` sudah menggabungkan keduanya secara eksplisit ("BR-068 + BR-070a: minimal dua Administrator aktif — ditegakkan di service layer... SELECT ... FOR UPDATE") dan `PR-01-02` memang pemilik `BR-070a` (keputusan 34 log phase-00). Bukan pertentangan PRD/SDD: menegakkan floor 2 otomatis memenuhi floor 1: BR-068 terlihat lebih longgar hanya karena ditulis sendirian, tanpa menyebut BR-070a. Frasa acceptance `phase-01.md` §7 ("Administrator terakhir ditolak") diperjelas jadi "minimal dua" agar tidak terbaca floor 1. | Ya — dinaikkan dan diputuskan pemilik produk (16 September 2026); `phases/phase-01.md` §7 disunting. |
| 15 | `authorize()` melempar lewat `next(galat)` (`AuthError`/`ForbiddenError`), bukan menulis respons sendiri. | Menyamakan pola dengan seluruh service lain di basis kode: galat domain ditangkap satu tempat oleh `errorMapper` (`SDD-06 §4.4`), sehingga amplop, `request_id`, dan pemetaan kode tidak disalin ke middleware ini. | Tidak — mekanisme teknis. |
| 14 | `/health` ringkasan dipasang `healthSummaryRouter` — router terpisah dari `healthRouter`, bukan digabung. | Probe publik dan endpoint berpermission adalah dua kelas akses berbeda; menyatukannya ke satu router mengaburkan mana yang menuntut apa. | Tidak — mekanisme teknis. |
| 13 | Slot `AuthContext` per-permintaan adalah `res.locals["authContext"]` (akses kurung, `getAuthContext`/`setAuthContext`) — **bukan** augmentasi tipe global `Express.Request`. | Menyamakan pola dengan `res.locals["cspNonce"]` (`PR-00-15`); augmentasi tipe global akan membocorkan bentuk `AuthContext` ke setiap handler di seluruh basis kode meski kebanyakan tidak membutuhkannya. | Tidak — mekanisme teknis. |
| 12 | PR memakai **dua label wajib** — `phase-NN` dan jenis (`feature`/`fix`/`chore`) — ditambah `tinjauan-arsitek` bila berlaku; sembilan label bawaan GitHub dihapus. | Label bawaan (`bug`, `enhancement`, `good first issue`, …) adalah set otomatis GitHub yang tidak mencerminkan alur kerja proyek, dan tidak pernah dipakai di 43 PR. Yang berguna saat menyaring daftar PR justru phase dan jenis — keduanya sudah ada pada ID PR dan nama cabang, sehingga label hanya memindahkannya ke tempat yang dapat disaring. Sengaja **bukan gerbang**: tidak ada workflow yang membacanya, jadi PR tanpa label tidak tertahan. | Ya — `BRANCHING §4.2`, `GITHUB-CI-STATE §4`, `templates/PULL-REQUEST.md`; diputuskan pemilik produk (16 September 2026). |
| 11 | `algorithm: 2` (Argon2id) ditulis sebagai angka, bukan lewat `Algorithm.Argon2id`. | Enum paket bersifat `const enum`, yang tidak dapat diakses saat `verbatimModuleSyntax` aktif. Bahwa hash benar-benar `argon2id` tidak bersandar pada konstanta ini melainkan pada uji yang memeriksa keluarannya. | Tidak. |
| 10 | Parameter Argon2id tetap **konstanta di kode**, dijaga uji yang membandingkannya dengan angka pada `SDD-SESS-01`. | `SDD-04 §6` menyebut parameter dapat diturunkan lewat konfigurasi sebagai mitigasi beban CPU, bukan sebagai kewajiban sekarang; memindahkannya ke env hari ini menambah tiga variabel tanpa satu pun yang menyetelnya. Dipindah bila uji beban `NFR-P-09` menuntut (Phase 08). | Tidak — pelaksanaan; diputuskan pemilik produk (16 September 2026). |
| 9 | Cakupan `NFR-S-03a` dipecah: `PR-01-16` menegakkan panjang ≥ 12, komposisi huruf besar/kecil/angka, dan larangan memuat identitas; **daftar password bocor dan riwayat 3 password terakhir menjadi `PR-02-31`** di Phase 02. Rencana total 164 → **165** PR. | Kedua aturan itu menuntut sumber daftar bocor dan tabel riwayat yang tidak ada di SDD mana pun, dan tidak ada PR yang memilikinya — memasukkannya ke sini menaikkan PR `S` menjadi `M`/`L` sekaligus menyentuh migration. `FR-01.4` sudah milik Phase 02, jadi pemiliknya berdekatan dengan `PR-02-06`. | Tidak — rencana implementasi; diputuskan pemilik produk (16 September 2026); `phase-01.md` §2/§7, `phase-02.md` §2/§7. |
| 8 | Pustaka Argon2id **`@node-rs/argon2` 2.2.1**, ditulis sebagai `SDD-SEC-12`. | Tahap build `Dockerfile` berjalan di `node:22-alpine` tanpa `build-base` (`SDD-INF-02`), sehingga pustaka ber-`node-gyp` menuntut perkakas kompilasi ditambahkan hanya demi satu dependensi; `@node-rs/argon2` menyediakan biner napi `linux-x64-musl` siap pakai. `hash-wasm` ditolak karena lebih lambat pada parameter yang sama. | Ya — `SDD-13` (`SDD-SEC-12`); diputuskan pemilik produk (16 September 2026). |
| 7 | Hash password dan kebijakan kata sandi bermukim di *shared kernel* **`shared/security/`**, ditulis sebagai `SDD-SYS-15` sebelum satu baris kode. | Daftar `SDD-SYS-06` tertutup dan tidak memuatnya, sementara dua modul berbeda memakainya: M-02 membuat akun berpassword sementara dan M-01 memverifikasinya. Menaruhnya di salah satu modul membuat kontrol keamanan bergantung pada modul yang kebetulan lahir lebih dulu. | Ya — `SDD-00` §2 dan §4.1; diputuskan pemilik produk (16 September 2026). |
| 6 | Satu fungsi trigger `set_updated_at()` dipakai bersama tabel entitas domain; `must_change_password` juga **tanpa nilai bawaan**. | `SDD-05 §4.2` mewajibkan trigger pada setiap tabel entitas domain, dan satu fungsi menghindari salinan per tabel. Nilai bawaan pada kolom keamanan mengubah kelalaian pemanggil menjadi keadaan diam-diam — pola keputusan 3 log phase-00. | Tidak. |
| 5 | Keunikan email ditegakkan indeks unik `lower(email)`, bukan `UNIQUE (email)`. | Lampiran E.5.2 menuntut email unik sistem-wide; `Budi@…` dan `budi@…` adalah kotak surat yang sama, dan `UNIQUE` polos meloloskan duplikat yang menyalahi `FR-02.1 A1`. Tanpa ekstensi `citext`, yang tidak dipasang `0001`. | Tidak — cara menegakkan aturan yang sudah ada. |
| 4 | Foto profil **ditunda ke Phase 03**: `users.foto_file_id` → `stored_files(id)` ditambahkan (expand) bersama `PR-03-04`; bagian foto `PR-02-06` menunggu. | `SDD-FS-02` mewajibkan entitas merujuk `stored_files.id`, yang baru lahir di Phase 03; kolom tanpa FK tidak dapat diisi dengan sah sebelum itu. | Tidak — rencana implementasi; diputuskan pemilik produk (15 September 2026); `phase-02.md` §7, `phase-03.md` §7. |
| 3 | `PR-02-01` (hash Argon2id) **dipindah ke Phase 01** sebagai `PR-01-16` dan mendahului `PR-01-02`; ID `PR-02-01` dipensiunkan. `users.password_hash` `NOT NULL`. Phase 01: 16 PR, Phase 02: 28; total tetap 164. | `FR-02.1` langkah 4 membuat akun berpassword sementara di `PR-01-02`, sementara hashing baru ada di Phase 02 yang bergantung Phase 01 — bentuk yang sama dengan keputusan 63 log phase-00. Kolom nullable menyimpang dari langkah 4 selama Phase 01. | Tidak — rencana implementasi; diputuskan pemilik produk (15 September 2026); `phase-01.md` §2/§4/§5/§7, `phase-02.md` §3/§7. |
| 2 | Penanda 2FA adalah **`totp_enabled_at`** (`SDD-04 §4.1`, Phase 02) saja; `PR-01-01` tidak membuat kolom 2FA. Atribut `2fa_enabled`/`two_fa_enabled` di PRD diganti. | Tiga ejaan untuk satu fakta: `2fa_enabled` (tidak sah sebagai identifier SQL), `two_fa_enabled` (ERD), dan `totp_enabled_at`. Boolean dan waktu sekaligus berarti dua penanda yang wajib selalu sinkron tanpa manfaat tambahan. | Ya — PRD tabel entitas `users` dan ERD; diputuskan pemilik produk (15 September 2026). |
| 1 | Kelompok **Status Pengguna** (Aktif, Nonaktif) ditambahkan ke Bab 11.3; `users.status` bertipe enum `user_status` **tanpa nilai bawaan**. | ERD PRD menulis `enum status` dan `FR-02.1` memakai kedua nilai, tetapi Bab 11.3 tidak mendaftarkannya, sementara `SDD-DB-02` mewajibkan native enum — pola `activity_result`/`holiday_type`/`permission_scope`. Tanpa bawaan agar akun siswa tidak diam-diam aktif sebelum persetujuan wali (`SL-06`). | Ya — PRD Bab 11.3; diputuskan pemilik produk (15 September 2026). |

## 3. Penyimpangan dari rencana

| Yang direncanakan | Yang dikerjakan | Sebab | Dampak pada phase berikutnya |
|---|---|---|---|
| `PR-01-17` — Impor massal pengguna asinkron + idempotensi; kode `M` · uji `M`. | Kode **+739 / −105** baris (`L`, di atas perkiraan `M`): migration `0020` (61) + `user-import.repository.ts` (201) + `user-import.service.ts` (+189/−73) + `user-import.job.ts` (87) + worker/`scheduler.ts` (+59) + controller/skema/route/`schema.ts`/indeks (+~140). Uji **+682 / −24** baris (`L`): `user-import-async.test.ts` (420, 14 uji PostgreSQL nyata) + `worker-user-import.test.ts` (128, ujung-ke-ujung Redis+PostgreSQL) + `user-import-worker.test.ts` (92) + penyesuaian uji lama. | **Kode di atas rencana `M`.** Tidak dipecah karena idempotensi dan jalur asinkron bertumpu pada satu baris `user_import_jobs` dan satu refaktor service yang sama (jalur sinkron dan asinkron kini berbagi `proses`/`tutup`): memecahnya meninggalkan tabel dengan setengah kolomnya tak terpakai (`berkas`, `baris_terproses`) dan service yang direfaktor dua kali. Sebagian besar baris adalah repository standar dan komentar `WHY`, bukan logika. Skema respons `PR-01-03` berubah (keputusan 33b). | `PR-02-25` memasang konsumen `NT-52`. |
| `PR-01-14` — Gerbang persetujuan wali (`consent_guardian_at`); kode `S` · uji `S`. | Kode **~115** baris (`S`, sesuai rencana): migration `0019` (16) + `user.service.ts` (+~55, gerbang di `create`/`update`/`updateStatus`) + `user.repository.ts` (+~20) + skema/controller/impor (+~16) + `schema.ts` (+3), `routes.ts` (+4), `api/index.ts` (+1). Uji **~290** baris (`M`): `guardian-consent.test.ts` (~260, 9 uji PostgreSQL nyata, termasuk HTTP dan impor) + `student-lifecycle.unit.test.ts` (+~22) + pembaruan `users.test.ts`/`user-import.schema.test.ts`. | — (sesuai rencana untuk kode; uji lebih besar dari `S` karena gerbang bercabang tiga titik) | `PR-02-25` memasang `NT-48` dan `NT-40` dari M-02. |
| `PR-01-13` — Siklus akun siswa: kenaikan kelas massal, kelulusan; kode `M` · uji `M`. | Kode **~655** baris (`L`): migration `0018` (39) + modul `m02-users` — `class-promotion.service.ts` (190), `student-enrollment.repository.ts` (153), `graduation.service.ts` (85), `student-obligation-registry.ts` (49), `class-promotion.schema.ts` (37), `class-promotion.controller.ts` (36), perubahan `routes.ts` (+28), `user.service.ts` (+16), `index.ts` (+7) — plus `schema.ts` (+13) dan `api/index.ts` (+2). Uji **~470** baris (`L`): `student-lifecycle.test.ts` (393, 12 uji PostgreSQL nyata) + `student-lifecycle.unit.test.ts` (70) + `health-http.test.ts` (+5). Tidak dipecah — namun **dapat dipecah** (kenaikan kelas | penonaktifan lulusan + `SL-04`): dipertahankan satu PR karena rencana phase menyebut `SL-01`…`SL-04` sebagai satu PR dan acceptance ("siswa berkewajiban aktif tidak dapat dinonaktifkan") menyentuh keduanya. | Rencana `M` mengasumsikan tabel + logika saja; PRD ternyata tidak mendefinisikan endpoint dan pekerjaan sehingga keduanya ikut dibangun (endpoint) atau disiapkan (service pekerjaan), ditambah titik ekstensi `SL-04` yang sengaja dibuat eksplisit. | `PR-05-09` cukup mendaftarkan pemeriksa ke `studentObligations` (tanpa mengubah modul pengguna). PR `SystemAuthContext` memasang `student-graduation` dan menyesuaikan `GraduationService` ke `writeSystem`. |
| `PR-01-12` — `work_units` + migrasi `users.unit_kerja` → `work_unit_id`; kode `M` · uji `M`. | Kode **~235** baris (`M`, sesuai rencana): migration `0017` (113 — dua enum, tabel, dua indeks unik ternormalisasi, kolom `users`, fungsi pemetaan) + `schema.ts` (17) + perubahan `m02-users` — `user.service.ts` (+33), `user-import.service.ts` (+32), `user.repository.ts` (+26), `user.schema.ts`/`user-import.schema.ts`/`user.controller.ts` (~16). Uji **~275** baris (`M`): `work-units.test.ts` (143, 6 uji PostgreSQL nyata) + pembaruan `m02-users.test.ts` (+95/−21), `m02-users-import.test.ts` (+33), dan tiga berkas kecil. | — (sesuai rencana) | `PR-01-13`/`PR-01-14` menyentuh `users` yang sama; `PR-05-xx` (pengadaan) mengulang pola pemetaan untuk `procurements.unit_kerja`. |
| `PR-01-11` — Kalender akademik: `academic_years`, `terms`, + `academic_year_id` pada `holidays`; kode `M` · uji `M`. | Kode **~130** baris (`S`): migration `0016` (105 — dua tabel, enum, `EXCLUDE`, constraint trigger deferred, kolom `holidays`) + tipe Kysely `AcademicYearsTable`/`AcademicTermsTable`/`holidays.academic_year_id` (27). Uji **~150** baris (`S`): `academic-calendar.test.ts` (151, 8 uji PostgreSQL nyata). Lebih kecil dari rencana `M` karena cakupan dipersempit ke skema (keputusan 27) — tidak ada modul, service, maupun endpoint. | Tidak ada endpoint kalender akademik di PRD, jadi tidak ada yang dapat dibangun di atas skema ini tanpa keputusan pemilik produk lebih dulu. | Butir terbuka §10: tanpa endpoint, DoD phase "Kalender akademik terisi untuk tahun ajaran berjalan (`AC-YR-01`)" tidak dapat dipenuhi Administrator lewat aplikasi. |
| `PR-01-10` — `system_settings` + seed + endpoint baca/tulis + validasi rentang; kode `M` · uji `M`. | Kode **~520** baris (`L`): migration `0015` (78: dua enum + tabel + tiga `CHECK` + seed enam baris) + tipe Kysely `SystemSettingsTable` (30); modul `m20-settings` LAHIR pertama kali — `setting.repository.ts` (104), `setting.service.ts` (111), `setting.schema.ts` (74), `routes.ts` (73), `setting.controller.ts` (57), `setting-validator.ts` (49, fungsi murni), `index.ts` (5) — plus pemasangan di `api/index.ts` (17). Uji **~325** baris (`M`): `m20-settings.test.ts` (236, 12 uji PostgreSQL nyata termasuk seed-vs-katalog-SDD) + `setting-validator.test.ts` (89) + pembaruan `health-http.test.ts` dan `bab113.ts` (~7). Tidak dipecah — tabel, seed, mekanisme validasi, dan dua endpoint adalah SATU requirement (`FR-20.1`): seed tanpa validator menyimpan angka yang tidak dapat dijaga, validator tanpa baris tidak dapat diuji. | Modul pertama menanggung biaya scaffolding SDD-00 §4.1 (enam berkas), ditambah dua enum baru dan penyuntingan PRD/SDD (Bab 11.3, `SDD-DB-17`, §4.7a) yang memang milik PR ini menurut `SDD-05 §4.6`. | PR konsumen parameter berikutnya cukup menambah baris seed (`SDD-DB-17`) — tanpa kode validasi baru. `PR-01-11` mewarisi pola migration + tipe Kysely yang sama. |
| `PR-01-09` — Ekspor activity log + pencatatan aksi ekspor; kode `S` · uji `S`. | Kode **~225** baris bersih (304 sisipan, 79 hapus — `M`, bukan `S`): `activity-log.service.ts` (+109, `BATAS_EKSPOR` + `buatBufferXlsx` + `export()`), `activity-log.controller.ts` (+64/−19, `exportActivityLogsHandler` + ekstraksi `bacaFilterMentah`/`keFilterRepo` bersama `listActivityLogsHandler`), `activity-log.repository.ts` (+41/−20, `dasar()` diekstrak agar dipakai `list()` DAN `listForExport()`), `activity-log.schema.ts` (+46/−25, `ExportActivityLogsQuerySchema` + skema respons biner), `routes.ts` (+26), `shared/http/route.ts` (+2, `contentType?`) + `api/openapi.ts` (+5, memakainya) — perbaikan kecil pada infrastruktur bersama agar OpenAPI route ini tidak salah mengiklankan `application/json`, plus pemasangan `api/index.ts`+`index.ts` (~13). Uji **~130** baris bersih (133 sisipan — `S`): `m18-activity-log.test.ts` (+79, 4 uji PostgreSQL nyata termasuk penolakan `BATAS_EKSPOR`), `activity-log.schema.test.ts` (+32), `openapi.test.ts` (+18, `contentType`), `health-http.test.ts` (+4/−1). Tidak dipecah — endpoint, `BATAS_EKSPOR`, dan pencatatan `ACTIVITY_LOG_EXPORTED` adalah SATU requirement (`FR-18.2` langkah 5, `AL-10`) yang saling bergantung, persis pola `PR-01-08` di baris lain. | Tidak seperti `PR-01-07`/`PR-01-08` (modul sudah hidup, endpoint tambahan murah), PR ini menambah kemampuan BARU (generate berkas biner) pada modul yang sudah ada — filter builder yang sebelumnya privat di `list()` harus diekstrak agar dipakai ulang `listForExport()`, dan `RouteDefinition` perlu `contentType?` karena setiap route sebelumnya berasumsi JSON. Biaya itu bukan scaffolding modul pertama, melainkan generalisasi infrastruktur bersama untuk kelas kemampuan (respons biner) yang belum pernah ada di basis kode. | `PR-06-08` (ekspor laporan, Phase 06) dan PR ekspor lain mewarisi `contentType?` yang sudah ada di `RouteDefinition` — tidak perlu menemukan ulang cara mengiklankan respons biner di OpenAPI. |
| `PR-01-04` — Matriks permission + `role_version` + cache 60 detik; kode `M` · uji `M`. | Kode **550** baris (`L`): migration `0013` (11) + tipe Kysely `roles.role_version` (10); `shared/auth/permission-cache.ts` (87, `PermissionCache` — kueri peran fresh + cache hasil join role_permissions×permissions); modul `m02-users` — `role.repository.ts` (158), `role.service.ts` (124), `role.controller.ts` (38), `role.schema.ts` (65), perubahan `routes.ts`+`index.ts`+`api/index.ts` (~57). Uji **523** baris (`M`, mendekati `L`): `role.schema.test.ts` (52) + `m02-users-roles.test.ts` (262, kontrak `RoleService` terhadap PostgreSQL nyata) + `permission-cache.test.ts` (202, kontrak cache terhadap Redis nyata) + pembaruan `health-http.test.ts` (8). Tidak dipecah — endpoint matriks, `role_version`, dan cache adalah SATU requirement saling bergantung (`SDD-AUTH-04`/`SDD-AUTH-10`): `role_version` tidak berguna tanpa endpoint yang menaikkannya, dan cache tidak dapat diuji berarti tanpa keduanya, persis pola `PR-01-02` di §3 atas. | Modul `m02-users` sudah ada sejak `PR-01-02`/`PR-01-03` — biaya di sini murni domain baru (role/permission) di dalam modul yang sudah hidup, bukan scaffolding modul pertama. | `PR-01-05` dst. (domain lokasi, modul BARU) diperkirakan tetap menanggung biaya scaffolding modul pertama serupa `PR-01-02`, bukan mewarisi keringanan `PR-01-04` ini. |
| `PR-01-05` — Skema `buildings`/`areas`/`rooms` + CRUD; kode `M` · uji `M`. | **Prediksi kalibrasi baris di atas terbukti**: modul `m03-locations` lahir PERTAMA kali, menanggung biaya scaffolding SDD-00 §4.1 satu kali (sama seperti `PR-01-02` bagi `m02-users`). Kode **960** baris (`L`): migration `0014` (95) + tipe Kysely `Buildings/Areas/RoomsTable` (46) + modul `m03-locations` lengkap — `location.repository.ts` (230, tiga tabel dalam satu berkas karena hierarkinya selalu berubah bersama), `location.service.ts` (194), `location.schema.ts` (122), `location.controller.ts` (118), `routes.ts` (119), `index.ts` (11) — plus pemasangan `locationsRouter` di `api/index.ts` (25). Uji **555** baris (`L`): `m03-locations.test.ts` (434, 13 uji terhadap PostgreSQL nyata) + `location.schema.test.ts` (107, 12 uji) + pembaruan `health-http.test.ts` (14) + `helpers/bab113.ts` (1). Tidak dipecah — `buildings`→`areas`→`rooms` adalah SATU hierarki (`FR-03.1`, `BR-013`) yang lahir bersama pada modul manapun yang membangunnya lebih dulu; memecah per tabel akan memisahkan validasi silang (area butuh gedungnya, ruangan butuh areanya) yang justru membuktikan `BR-013`. | Modul pertama menanggung biaya satu kali (enam berkas SDD-00 §4.1 lahir bersama, migration + tipe Kysely tiga tabel sekaligus) — pola identik `PR-01-02`. | `PR-01-06` (pohon lokasi + penonaktifan berjenjang) mewarisi modul yang sudah hidup — diperkirakan `M` sesuai rencana, tanpa biaya scaffolding ini. |
| `PR-01-07` — Daftar aset per lokasi (kerangka); kode `S` · uji `S`. | Modul BARU `m04-assets` lahir (keputusan 23 — bukan `m03-locations` seperti diasumsikan rencana phase, sebab `GET /rooms/{id}/assets` dimiliki `m04-assets.md`). Kode **287** baris (`M`): `asset.schema.ts` (62), `asset.repository.ts` (31, satu metode `roomExists`), `asset.service.ts` (80), `asset.controller.ts` (46), `routes.ts` (53), `index.ts` (5) — plus pemasangan `assetsRouter` di `api/index.ts` (10). Uji **231** baris (`M`): `m04-assets.test.ts` (177, 4 uji PostgreSQL nyata) + `asset.schema.test.ts` (49, 6 uji) + perbaikan assersi longgar `m03-locations.test.ts` (2, §7 masalah). Tidak dipecah — satu endpoint baca, tidak ada yang bisa dipisah lebih jauh. | Modul pertama menanggung biaya scaffolding SDD-00 §4.1 (enam berkas) meski isinya cuma SATU endpoint — rasio biaya/manfaat lebih buruk daripada `PR-01-02`/`PR-01-05` yang scaffolding sama tapi endpoint jauh lebih banyak. | Kapan pun M-04 (Inventaris Aset) sungguhan dibangun `PR-02-10` dst., modul ini sudah hidup — PR itu mewarisi pola yang sama seperti `m02-users`/`m03-locations` mewariskan ke PR keduanya masing-masing. |
| `PR-01-01` — skema `users` dengan atribut PRD M-02 §8; kode `M` · uji `M`. | Tanpa penanda 2FA, kolom penguncian/TOTP, dan foto; PRD Bab 11.3 dan atribut 2FA disunting pada PR yang sama; `PR-02-01` dipindah menjadi `PR-01-16`. Kode **124** (`S`) · uji **241** (`M`). | Keputusan 1–4: tiga atribut PRD tidak dapat diterapkan apa adanya, dan hashing yang dituntut `PR-01-02` berada di phase sesudahnya. | `PR-01-16` wajib tergabung sebelum `PR-01-02`; foto profil `PR-02-06` menunggu `PR-03-04`. |
| `PR-01-02` — CRUD pengguna; kode `M` · uji `M`. | Kode **~934** baris (`L`): modul PERTAMA di basis kode (`m01-auth` s.d. `m22-materials` semuanya masih kosong) — `schemas/` 114, `repositories/` 225, `services/` 262 (`user.service.ts` + `temporary-password.ts`), `controllers/` 121, `routes.ts` + `index.ts` 149, plus pemasangan `express.json()` dan `AuditLogger` pertama di `api/chain.ts`+`api/index.ts` (~55) dan `requireAuthContext` di `shared/auth` (~17). Uji **742** baris (`M`→mendekati `L`): 20 uji HTTP+layanan terhadap PostgreSQL nyata + 15 uji skema/generator murni. Tidak dipecah — SDD-00 §4.1 menuntut keenam berkas modul (`index.ts`/`routes.ts`/`controllers/`/`services/`/`repositories/`/`schemas/`) lahir bersama pada modul manapun yang lahir pertama, dan FR-02.1 (CRUD + soft delete + aturan admin terakhir) adalah satu requirement yang saling bergantung — memecah status/admin-terakhir dari CRUD-nya akan memisahkan acceptance yang tidak dapat diuji terpisah. | Modul pertama menanggung biaya satu kali (scaffolding SDD-00 §4.1, `express.json()`, penyambungan `AuditLogger` ke `createApp`) yang tidak akan terulang di modul-modul berikutnya. | Modul-modul berikutnya (`m03-locations`, dst.) mewarisi pola yang sama tanpa biaya satu kali ini — perkiraan `M` pada rencana Phase 01+ kemungkinan lebih akurat untuk PR sesudah ini. |

## 4. PR di luar rencana

PR yang tidak ada pada daftar [`phase-01.md` §7](../phases/phase-01.md).

| ID | Judul | Mengapa tidak terencana |
|---|---|---|
| — | — | — |

Kolom ketiga adalah yang paling berharga di seluruh log ini. Pola yang berulang di sana menunjukkan di mana perencanaan phase berikutnya perlu diperbaiki.

## 5. Butir yang wajib tercatat pada phase ini

- [ ] Titik ekstensi `SL-04` — di mana ia ditinggalkan terbuka (`PR-01-13`); **ditutup di Phase 05 `PR-05-09`**
- [ ] Migrasi `users.unit_kerja` → `work_unit_id`: tahap expand & migrate selesai; **contract ditunda ke `PR-08-11`**
- [ ] Penerapan `SDD-AUTH-11` (uji tiga syarat) dan `SDD-EVT-10` (kartu dead letter baca-saja) — keduanya ditutup 25 Agustus 2026, sebelum phase dimulai

## 6. TBD yang tertutup

| TBD | Keputusan | Diputuskan oleh | Tanggal |
|---|---|---|---|
| — | — | — | — |

Setiap TBD yang tertutup wajib juga diperbarui di [`../../SDD/TBD-REGISTER.md`](../../SDD/TBD-REGISTER.md). Menutupnya hanya di sini membuat register menjadi salah.

## 7. Masalah yang ditemukan

| Masalah | Dampak | Penyelesaian | Terbuka? |
|---|---|---|:---:|
| Sel kolom opsional (`telepon`, `kode_unit_kerja`) yang dikosongkan pengunggah dibaca sebagai string kosong `""`, bukan "tidak diisi" — `min(1)` menolaknya sebagai baris gagal padahal kolomnya memang boleh kosong. Perilaku CSV dan XLSX berbeda: baris CSV berakhiran koma kosong kebetulan terbaca `null` (lolos), sel XLSX kosong eksplisit terbaca `""` (ditolak) — ditemukan lewat uji XLSX yang gagal, bukan lewat pembacaan kode. | Berkas templat CSV yang sepenuhnya sah — kolom opsional dikosongkan, pola paling umum saat mengisi spreadsheet — akan menolak baris yang seharusnya diterima, dan konsisten dengan format lain hanya secara kebetulan. | **Selesai 16 September 2026**: `bacaBerkas` menyamakan sel kosong (apa pun bentuknya) menjadi `undefined` sebelum divalidasi Zod, satu tempat untuk CSV dan XLSX. Uji regresi ditambahkan khusus kasus XLSX (`user-import.service.ts`, `m02-users-import.test.ts`). | tidak |
| `phase-01.md` §5 menulis `SDD-AUTH-05` (`GET /me`) berada dalam kelompok "(middleware otorisasi, `PR-01-15`)", padahal `GET /me` menuntut token terverifikasi nyata dan tercatat sebagai endpoint `m01-auth.md` (Phase 02) — PR-01-15 sendiri bergantung `PR-01-01` saja, tanpa autentikasi. | Pembaca dapat menyimpulkan `PR-01-15` wajib membangun `/me`, padahal mustahil sebelum Phase 02 punya JWT. | **Selesai 16 September 2026**: baris §5 diperbaiki, `SDD-AUTH-05` dipisah dari kelompok dan ditandai milik Phase 02. | tidak |
| Baris rencana `PR-01-16` (warisan `PR-02-01`) merujuk `NFR-S-01`, yang isinya HTTPS/TLS — bukan `NFR-S-02` (hash password) maupun `NFR-S-03a` (kebijakan kata sandi). Rujukan itu ikut terbawa saat PR dipindah ke Phase 01. | PR dapat selesai dan lulus tinjauan tanpa pernah menguji requirement yang sebenarnya dilayaninya; `NFR-S-03a` bahkan tidak dimiliki PR mana pun sampai keputusan 9. | **Selesai 16 September 2026**: `phase-01.md` §4 dan §7 diperbaiki ke `NFR-S-02` dan `NFR-S-03a`, dan sisa `NFR-S-03a` memperoleh pemilik (`PR-02-31`). | tidak |
| `tests/shared/clock-lint.test.ts` merah satu kali pada run penuh (timeout 6,1 detik), lalu hijau saat berkasnya dijalankan sendiri. | Uji yang kadang merah tanpa sebab kode membuat orang mengulang run alih-alih membacanya, dan lama-lama mengabaikan pipeline. | Dibuktikan bukan cacat aturan: memanggil ESLint langsung atas berkas probe tetap menghasilkan `no-restricted-syntax`. Penyebabnya ESLint dijalankan di bawah 27 worker paralel. Dibiarkan apa adanya pada PR ini; bila berulang, yang disesuaikan adalah jumlah worker atau `testTimeout` uji itu, bukan ujinya dihapus. | **ya** |
| Mutation-check pertama atas `MISSING_DIGIT` tidak menyentuh berkas: pola `perl` gagal karena `\d` ikut ditafsirkan shell. | Pengaman itu sempat tampak terbukti padahal ujinya tidak pernah dijalankan terhadap kode yang berubah. | Terlihat karena skrip membandingkan berkas sebelum dan sesudah mutasi lalu berteriak; mutasi diulang dengan pola lain dan memerah. Pelajaran yang sama dengan putaran mutasi `PR-01-01`: mutasi yang tidak mengubah berkas wajib gagal berisik, bukan lolos. | tidak |
| `scripts/check_import_boundaries.mjs` menghapus (`rmSync` rekursif, blok `finally`) folder modul contoh bernama `apps/api/src/modules/m01-auth` dan `m02-users` di akhir SETIAP jalan — termasuk `npm run check:boundaries` dan `npm run lint` (root) bila memanggilnya. Sejak `PR-00-02` nama itu memang dipilih meniru modul PRD nyata, aman selama keduanya belum ada. `PR-01-02` membangun `m02-users` sungguhan — modul hilang total (seluruh isi `schemas/`, `repositories/`, `services/`, `controllers/`, `routes.ts`, `index.ts`) tiga kali di sesi yang sama setiap kali skrip ini (atau `npm run lint` yang memuatnya) dijalankan, sebelum akar masalahnya terlihat. | Siapa pun yang menjalankan `check:boundaries`/`lint` sambil mengerjakan modul pertama kehilangan seluruh pekerjaan tersimpan yang belum di-commit, tanpa satu pun pesan galat. Berulang bagi modul PRD berikutnya yang kebetulan lahir dengan nama sama dengan fixture (tidak ada lagi — lihat penyelesaian). | **Selesai 16 September 2026** (keputusan 17): nama folder contoh diganti `__contoh_modul_a__`/`__contoh_modul_b__`, tidak pernah bertabrakan dengan `mNN-*` PRD nyata. Modul direkonstruksi dari isi yang sama persis (ditulis ulang tiga kali; typecheck+uji dibandingkan hijau setiap kali). | tidak |
| Uji integrasi yang menaikkan `role_version` lewat `RoleService.updatePermissions()` menulis `roles.updated_by` = pelaku PUT (perilaku produksi yang benar, `SDD-05 §4.2`). Uji yang hanya memulihkan `role_permissions`+`role_version` di `finally` (bukan `updated_by`) meninggalkan `roles.updated_by` menunjuk ke akun uji sekali-pakai. Berkas uji LAIN yang membersihkan `users` (mis. `m02-users.test.ts`, `m02-users-import.test.ts`) lalu gagal dengan `roles_updated_by_fkey` karena baris yang dihapusnya masih dirujuk. Ditemukan lewat jalan penuh `vitest.integration.config.ts`, bukan lewat menjalankan berkas baru sendirian — kedua berkas lolos sendirian dan baru bertabrakan pada urutan penuh. | Kegagalan lintas-berkas yang tampak acak — berkas yang gagal (`users.test.ts`, `m02-users-import.test.ts`) bukan berkas yang menyebabkan (`m02-users-roles.test.ts`, `permission-cache.test.ts`), sehingga penyebabnya mudah dicari di tempat yang salah. | **Selesai 17 September 2026**: fungsi `pulihkan()` kedua berkas uji memulihkan `updated_by` (dan `role_version`) ke nilai SEBELUM diuji, bukan hanya `role_permissions`. Dibuktikan: jalan penuh `vitest.integration.config.ts` hijau (selain dua kegagalan lingkungan `sigm4_app` yang sudah ada sebelum PR ini — lihat baris di bawah). | tidak |
| Dua kegagalan `tests/integration/users.test.ts` dan `activity-log.test.ts` ("AL-03b") pada volume Docker dev yang BARU dibuat: `password authentication failed for user "sigm4_app"`. `0007_app_role.sql` membuat `sigm4_app` TANPA sandi (`CREATE ROLE sigm4_app LOGIN`, mengandalkan `trust`/`scram` lokal), sementara `.env` pengembangan menulis `APP_DATABASE_URL` dengan sandi `sigm4_app_dev` yang tidak pernah disetel pada peran itu. | Kedua uji `AL-03b` (hak akun aplikasi tidak dapat menulis `activity_logs`) gagal pada image `postgres:15-alpine` bawaan `docker-compose.yml` bila `pg_hba.conf`-nya menuntut sandi. | **Dikonfirmasi PRA-ADA**, bukan regresi PR ini: direproduksi identik pada `develop` (`git stash` atas seluruh perubahan PR-01-04, jalan penuh terhadap kontainer yang sama). Dibiarkan terbuka — perbaikannya (menyamakan `pg_hba.conf`/sandi kontainer dev dengan `APP_DATABASE_URL`) di luar cakupan `PR-01-04`. | **ya** |
| `CREATE INDEX CONCURRENTLY cannot run inside a transaction block` pada `tests/integration/migration-runner.test.ts` saat jalan penuh `vitest.integration.config.ts`. | Satu berkas uji migration-runner gagal di tengah jalan penuh. | **Dikonfirmasi PRA-ADA** dengan cara yang sama (`git stash` + jalan ulang terhadap kontainer yang sama) — tidak tersentuh `PR-01-04`, yang tidak menulis migration ber-`CONCURRENTLY` apa pun (`0013` murni `ALTER TABLE ADD COLUMN`). Di luar cakupan. | **ya** |
| Sandbox lokal sesi ini menolak (`Security Test Removal`) menjalankan uji sementara guard `role.kode === KODE_ROLE_ADMINISTRATOR` (`SDD-AUTH-10`) dinonaktifkan untuk mutation-check langsung. | Tidak ada bukti EKSEKUSI langsung bahwa mengangkat guard membuat uji terkait merah — hanya penalaran konstruksi (assersi `CORE_PERMISSION_LOCKED` pada kondisi persis itu). | Kode dikembalikan seketika (`git diff` bersih sebelum lanjut); TIDAK dipaksakan lewat jalur lain. Dicatat apa adanya di §8, bukan diklaim terbukti. | **ya** |
| Implementasi pertama `PR-01-07` menaruh `GET /rooms/{id}/assets` di `m03-locations` (`location.view`) tanpa memeriksa lebih dulu apakah endpoint itu sudah dimiliki modul lain. `scripts/audit_docs.py` menolaknya: baris itu SUDAH ada di `m04-assets.md` §7 dengan permission `asset.view`, bersanding `/assets`, `/assets/{id}`, dst. | Nyaris membangun dan menggabungkan endpoint di modul yang salah beserta permission yang salah, bertentangan dengan `CLAUDE.md` §Workflow AI butir 1 ("Temukan ID-nya lebih dulu"). | **Selesai 18 September 2026** (keputusan 23): baris `m03-locations.md` dikembalikan, kode dipindah utuh ke modul BARU `m04-assets` dengan permission `asset.view`. `audit_docs.py` hijau sebelum commit pertama dibuat. Pelajaran: `grep` ID endpoint di SELURUH `docs/PRD/02-modules/*.md` sebelum menambah baris katalog baru, bukan hanya di modul yang sedang dikerjakan. | tidak |
| `tests/integration/m03-locations.test.ts` (katalog `PR-01-05`) memakai `path.startsWith("/rooms")` untuk memfilter route ber-permission `location.manage` — longgar terhadap route BARU apa pun yang kebetulan diawali `/rooms` tetapi bukan milik `m03-locations`. Ketahuan DUA KALI: pertama oleh `PATCH /rooms/{id}/status` (`PR-01-06`, sebenarnya `location.manage` — kebetulan tidak memerah), lalu oleh `GET /rooms/{id}/assets` (`PR-01-07`, `asset.view` — memerah). | Uji katalog yang longgar gagal acak setiap kali modul LAIN mendaftarkan route berawalan path yang sama, menuding modul yang salah sebagai penyebab. | **Selesai 18 September 2026**: assersi diganti memakai `rute` — daftar yang SUDAH dicocokkan persis terhadap keempat path `m03-locations` sendiri (baris di atasnya), bukan `startsWith` longgar. Tidak akan bereaksi lagi terhadap route modul lain. | tidak |
| Draf pertama `m18-activity-log.test.ts` memakai penghitung `urut` (pola `PR-01-02`/dst.) untuk penanda unik (`modul`/`aksi`) yang disisipkan ke `activity_logs` — tabel yang SENGAJA TIDAK PERNAH dibersihkan (append-only, `AL-03b`). Jalan ulang berkas ini SENDIRIAN setelah kegagalan lain (bug rentang tanggal, lihat kalibrasi §8) menghasilkan penanda yang SAMA PERSIS dengan baris yang ditinggalkan jalan sebelumnya (`urut` mulai dari 0 lagi tiap proses baru) — jumlah baris yang cocok pada filter ganda (2→4, 1→2, 5→10), bukan sekali. | Penghitung sekuensial aman untuk tabel yang dibersihkan `beforeEach` (`users`, `buildings`, dst.) tetapi TIDAK PERNAH aman untuk tabel yang sengaja permanen — pelajaran yang sama seperti `randomUUID` pada `m02-users-roles.test.ts`/`permission-cache.test.ts` (`PR-01-04`) kini terulang di tabel berbeda karena polanya tidak digeneralisasi saat itu. | **Selesai 18 September 2026**: `kodeUnik`/`emailUnik`/`nipUnik` diganti `crypto.randomUUID()`. Aturan umum untuk PR berikutnya: uji yang menulis ke tabel APA PUN yang tidak dibersihkan di `beforeEach`/`afterAll` berkas itu sendiri WAJIB memakai UUID untuk penanda unik, tidak pernah penghitung — apa pun alasannya tabel itu tidak dibersihkan (append-only, riwayat bersama, atau sekadar belum sempat). | tidak |

## 8. Hasil pengukuran

Angka nyata, bukan perkiraan. Kosongkan bila belum diukur — jangan diisi tebakan.

| Yang diukur | Target | Hasil | Rujukan |
|---|---|---|---|
| Acceptance `PR-01-01` terhadap PostgreSQL 15 nyata | naik-turun bersih; `role_id` merujuk role hasil seed | **Terbukti** — 14 uji `users.test.ts`: FK ke role `R-07` hasil seed; role tak ada `23503`; tanpa role `23502`; email beda huruf besar `23505`; `nip_nis` ganda `23505`; `status` dan `must_change_password` tanpa bawaan `23502`; status di luar enum `22P02`; kolom persis 15; trigger menimpa `updated_at` yang dipaksakan pada `users` dan `roles`; `roles.created_by` → `users`; akun `sigm4_app` menulis tanpa GRANT susulan. `down` mencabut tabel, 4 kolom baku, fungsi, dan tipe tanpa menyentuh 7 role dan grant seed; `up` memulihkannya | `FR-02.1`, `BR-066`, `SDD-05 §4.2/§4.7`, `CD-05` |
| Mutation-check `0012` | setiap pengaman merah bila dicabut | trigger `users` dicabut → 1 uji merah · trigger `roles` → 1 · indeks `lower(email)` menjadi `(email)` → 1 · `status DEFAULT 'AKTIF'` → 1 · `down` tanpa pencabutan kolom `roles` → 1. Putaran pertama **tidak sah**: `migrate.mjs` dipanggil dari direktori yang salah sehingga tiap putaran menguji mutasi sebelumnya; diulang terisolasi dengan `0012` dibersihkan di antara mutasi | `SDD-05 §4.2`, `SL-06`, `FR-02.1 A1` |
| Gerbang CI lokal (`test:ci`) | cakupan ≥ 70%, 0 ter-skip | **41 berkas / 441 uji**, 0 ter-skip; **97,96%** statements · **98,13%** lines · 91,70% branches · 97,61% functions. Uji berkas 306/306; lint, typecheck, `check:boundaries` hijau | `CD-01`, `CD-02` |
| Baris kode produksi vs uji, `PR-01-01` | kode `M` · uji `M` (§7) | **124** (`S`: migration 85, tipe Kysely 39) / **241** (`M`) — kode meleset satu kelas ke bawah karena kolom autentikasi dan foto tidak masuk (keputusan 2, 4) | kalibrasi §7 |
| Acceptance `PR-01-15`: tanpa `AuthContext` → 401, tanpa permission → 403, keduanya sebelum controller | keduanya | **Terbukti** — 10 uji `middleware.test.ts` (unit) + 3×1 uji `sec-t-01.test.ts` (tergenerate atas `registry.guarded()`, saat ini 1 route: `GET /health`) + 1 uji HTTP penuh lewat `createApp()` tanpa konteks apa pun → `401 UNAUTHENTICATED`. Mutation-check: pemeriksaan `ctx===undefined` dicabut → 2 uji merah · pemeriksaan `ctx.can` dicabut → 3 · kode galat ditukar ke `FORBIDDEN` → 2 · `allowedFields` selalu buka `extra` → 2 uji merah | `PM-02`, `SDD-03 §4.4`, `SEC-T-01` |
| Baris kode produksi vs uji, `PR-01-15` | kode `M` · uji `M` (§7) | **124** (`S`: `middleware.ts` 45, `fields.ts` 29, perubahan `health.ts`+`index.ts`+`auth/index.ts` 50) / **313** (`L`) — kode meleset satu kelas ke bawah: tidak ada `authenticate` maupun cache permission di PR ini (keputusan 63 log phase-00 sudah membatasi cakupannya) | kalibrasi §7 |
| Acceptance `PR-01-02` — soft delete, admin terakhir, activity log | BR-067 (tanpa `DELETE /users`), BR-068+BR-070a (floor 2, keputusan 16), AL-01 (`USER_CREATED/UPDATED/DEACTIVATED/REACTIVATED`) | **Terbukti** — 20 uji `m02-users.test.ts` (PostgreSQL + Redis nyata, HTTP penuh lewat `createApp`-setara): registri tanpa `DELETE /users`; pembuatan → `AKTIF`+`must_change_password`+password lolos `checkPasswordPolicy`+`USER_CREATED` tercatat; email/nip_nis duplikat (tanpa peduli huruf besar) → `DUPLICATE_CODE`; update → `USER_UPDATED` dengan nilai sebelum/sesudah, tidak salah anggap diri sendiri duplikat; nonaktif/aktif kembali → `USER_DEACTIVATED`/`USER_REACTIVATED` dengan alasan; 3 admin aktif → nonaktifkan 1 berhasil (sisa 2); 2 admin aktif → nonaktifkan ditolak `VALIDATION_ERROR` (sisa tetap 2); paginasi+filter `role_id`/`unit_kerja`; tanpa `AuthContext` → 401. Plus 15 uji murni skema Zod (`CreateUserBodySchema`, `UpdateUserStatusBodySchema` menuntut alasan, `ListUsersQuerySchema`) dan generator password sementara (lolos kebijakan 200× berturut-turut, ≥ panjang minimal, tidak memuat identitas, tidak pernah identik dua kali) | `FR-02.1`, `BR-067`, `BR-068`, `BR-070a`, `AL-01`, `SDD-05 §4.3` |
| Mutation-check guard admin terakhir (`adminAktif.length <= AMBANG_ADMIN_AKTIF`) | pengaman merah bila dicabut | Kondisi diganti `false && ...` (guard tidak pernah aktif): uji layanan "hanya tersisa 2 aktif → ditolak" merah, uji HTTP "admin terakhir → 422" merah — keduanya persis seperti diharapkan. Dikembalikan dan dibandingkan hijau kembali (`tsc` + 16/16 uji) sebelum lanjut | `BR-068+BR-070a`, `SDD-05 §4.3` |
| Gerbang CI lokal (`test:ci`, coverage) | cakupan ≥ 70%, 0 ter-skip | **48 berkas / 518 uji** (2 berkas migration-cycling dikecualikan sementara — lihat catatan lingkungan di bawah); cakupan keseluruhan **97,33%** statements · **89,61%** branch · **98,1%** functions · **97,85%** lines. Modul baru: `controllers` 100% stmt/line, 71,4% branch · `repositories` 90,9% stmt/line, 66,7% branch · `services` 92,95% stmt, 80,55% branch. `check:boundaries`, lint, `tsc -p tsconfig.test.json` hijau | `CD-01`, `CD-02` |
| Baris kode produksi vs uji, `PR-01-02` | kode `M` · uji `M` (§7) | **~934** (`L`, lihat §3) / **742** (mendekati `L`) — kode meleset satu kelas ke atas: modul pertama menanggung biaya scaffolding SDD-00 §4.1 satu kali | kalibrasi §3, §7 |
| Acceptance `PR-01-03` — impor CSV/XLSX sinkron, IMPT-01/02 | baris gagal tidak menggagalkan berkas; laporan per baris | **Terbukti** — 9 uji `m02-users-import.test.ts` (PostgreSQL nyata, HTTP penuh): CSV valid seluruh baris sukses + `USER_CREATED` per baris + `USER_IMPORTED` ringkasan; 1 baris tidak sah di antara baris sah lain → baris sah tetap sukses (IMPT-01); email duplikat DI DALAM satu berkas → baris pertama sukses baris kedua `DUPLICATE_CODE`; `kode_role` tak dikenal → pesan jelas menyebut kodenya; XLSX (ditulis `exceljs`, dibaca balik) diproses sama seperti CSV; kolom opsional dikosongkan XLSX tidak ditolak (regresi — lihat §7); header tanpa kolom wajib → 400; > 200 baris → 422 SEBELUM satu baris pun diproses; tanpa `AuthContext` → 401. Plus 3+8 uji murni skema `ImportUsersBodySchema`/`ImportUserRowSchema` | `FR-02.1 A4`, `IMPT-01`, `IMPT-02` |
| Mutation-check batas 200 baris (`BATAS_BARIS_IMPOR`) | pengaman merah bila dicabut | Kondisi diganti `false && ...`: uji "> 200 baris → 422" memerah (200 diterima, seluruhnya diproses dalam ~5 detik alih-alih ditolak). Dikembalikan dan hijau kembali | `IMPT-04` |
| Baris kode produksi vs uji, `PR-01-03` | kode `M` · uji `M` (§7) | **392** (`M`, persis dalam ambang) / **487** (`M`) — cakupan disempitkan (keputusan 19) membuat perkiraan rencana akhirnya akurat, berbeda dari `PR-01-01`/`PR-01-02` yang meleset | kalibrasi §7 |
| Acceptance `PR-01-04` — matriks permission, `role_version`, cache (`FR-02.2`, `SDD-AUTH-04`, `SDD-AUTH-10`) | perubahan matriks berlaku pada permintaan berikutnya tanpa restart; permission inti Administrator tidak dapat dicabut | **Terbukti** — 12 uji integrasi (PostgreSQL + Redis nyata). `m02-users-roles.test.ts` (6): `list()` menyertakan `jumlah_pengguna` + permission aktif per role; `updatePermissions()` mengganti matriks PENUH + `role_version` naik tepat satu + `ROLE_PERMISSION_UPDATED` tercatat dengan nilai sebelum/sesudah; kode permission tak dikenal → `VALIDATION_ERROR` tanpa mengubah matriks; role tak ada → `NotFoundError`; pencabutan permission inti dari Administrator → `CORE_PERMISSION_LOCKED` tanpa mengubah matriks maupun `role_version`; pencabutan permission inti dari role BUKAN Administrator (R-03, `activity_log.view`) DIIZINKAN — membuktikan penguncian khusus Administrator (`SDD-03 §4.7`), bukan permission mana pun. `permission-cache.test.ts` (4 + 1 penjaga lingkungan `REDIS_URL`): kunci `perm:{user_id}:{role_version}` benar-benar terisi di Redis; TTL ≤ 60 detik (`PM-05`); pengguna tak ada → `undefined` tanpa menaruh kunci; perubahan matriks lewat `RoleService` membuat panggilan `PermissionCache.load()` BERIKUTNYA membaca set baru — tanpa restart maupun penghapusan kunci lama secara manual (kunci lama basi dengan sendirinya karena `role_version` sudah naik). Plus 5 uji murni skema Zod (`role.schema.test.ts`). **Mutation-check langsung atas guard `role.kode === KODE_ROLE_ADMINISTRATOR` tidak dapat dijalankan pada sesi ini** — sandbox lokal menolak eksekusi uji sementara sebuah pemeriksaan keamanan dinonaktifkan (§7); cakupannya tetap terbukti secara konstruksi: uji "pencabutan permission inti dari Administrator" mengasersi `CORE_PERMISSION_LOCKED` PERSIS pada kondisi itu, dan akan lolos diam-diam (bukan menolak) bila baris itu dicabut. | `FR-02.2`, `SDD-AUTH-04`, `SDD-AUTH-10`, `PM-05` |
| Baris kode produksi vs uji, `PR-01-04` | kode `M` · uji `M` (§7) | **550** (`L`) / **523** (`M`, mendekati `L`) — kode meleset satu kelas ke atas: tiga deliverable rencana (endpoint, `role_version`, cache) adalah satu requirement saling bergantung (§3) | kalibrasi §3 |
| Acceptance `PR-01-05` — skema lokasi + CRUD (`FR-03.1`, `BR-013`, `BR-014`) | hierarki tiga tingkat; kode unik per tingkat; `PUT /rooms/{id}` tanpa `status` | **Terbukti** — 13 uji integrasi (PostgreSQL nyata) `m03-locations.test.ts`: katalog registri (`POST /buildings`·`/areas`·`/rooms`, `PUT /rooms/:id`, seluruhnya `location.manage`); `createBuilding()` tersimpan `AKTIF` + `LOCATION_CREATED` tercatat; kode gedung duplikat → `DUPLICATE_CODE`; `createArea()` tertaut `building_id`, `LOCATION_CREATED` tercatat; `building_id` tak ada → `VALIDATION_ERROR`; kode area duplikat → `DUPLICATE_CODE`; `createRoom()` berbawaan `dapat_direservasi`/`boleh_direservasi_siswa` = false (`BR-016`), `LOCATION_CREATED` tercatat; `area_id` tak ada → `VALIDATION_ERROR`; kode ruangan duplikat → `DUPLICATE_CODE`; `updateRoom()` mengganti field + `LOCATION_UPDATED` dengan nilai sebelum/sesudah; kode milik sendiri tidak dianggap duplikat; ruangan tak ada → `NotFoundError`; `area_id` baru tak ada → `VALIDATION_ERROR`. Plus 12 uji murni skema Zod (`location.schema.test.ts`): enum `room_type` (0002), kapasitas positif, `UpdateRoomBodySchema` menolak `status` dan menuntut kedua flag boolean eksplisit (PUT = pengganti penuh, tanpa bawaan). Migration `0014` naik/turun/naik dibuktikan langsung terhadap PostgreSQL nyata sebelum kode ditulis. | `FR-03.1`, `BR-013`, `BR-014`, `BR-016` |
| Baris kode produksi vs uji, `PR-01-05` | kode `M` · uji `M` (§7) | **960** (`L`) / **555** (`L`) — kode DAN uji sama-sama meleset satu kelas ke atas: modul pertama menanggung biaya scaffolding satu kali (§3), pola identik `PR-01-02` | kalibrasi §3 |
| Acceptance `PR-01-06` — pohon lokasi + kerangka penonaktifan berjenjang (`FR-03.1` langkah 1, `BR-015` dipersempit — keputusan 22) | pohon tersusun benar; gedung ditolak nonaktif selagi ada ruangan aktif; ruangan bebas diubah statusnya | **Terbukti** — 10 uji integrasi (PostgreSQL nyata) `m03-locations-tree.test.ts`: katalog registri (`GET /locations/tree` → `location.view`; kedua `PATCH .../status` → `location.manage`); `getTree()` kosong → `[]`; `getTree()` menyusun gedung→area→ruangan bersarang dengan benar; `updateBuildingStatus()` nonaktifkan gedung TANPA ruangan aktif → berhasil + `LOCATION_DEACTIVATED`; nonaktifkan gedung DENGAN ruangan AKTIF → `VALIDATION_ERROR` (`detail.rule: "BR-015"`), status gedung tidak berubah (dibuktikan baca ulang basis data); nonaktifkan gedung yang ruangannya SUDAH nonaktif → berhasil; aktifkan kembali → `LOCATION_UPDATED` (bukan `LOCATION_DEACTIVATED`); gedung tak ada → `NotFoundError`; `updateRoomStatus()` nonaktifkan lalu aktifkan kembali tanpa pemeriksaan apa pun (BR-015 tingkat ruangan sengaja belum ditegakkan) + aksi log benar pada kedua transisi; ruangan tak ada → `NotFoundError`. Plus 2 uji murni skema Zod (`UpdateLocationStatusBodySchema`, ditambahkan ke `location.schema.test.ts`). | `FR-03.1`, `BR-015` |
| Baris kode produksi vs uji, `PR-01-06` | kode `M` · uji `M` (§7) | **295** (`M`) / **292** (`M`) — **PERTAMA di Phase 01 yang tepat sesuai rencana**: modul `m03-locations` sudah hidup sejak `PR-01-05`, tidak ada biaya scaffolding satu kali (prediksi kalibrasi `PR-01-05` §3 terbukti) | kalibrasi §3 (`PR-01-05`) |
| Acceptance `PR-01-07` — daftar aset per lokasi, kerangka (`FR-03.2`) | endpoint mengembalikan struktur benar dengan daftar kosong | **Terbukti** — 4 uji integrasi (PostgreSQL nyata) `m04-assets.test.ts`: katalog registri (`GET /rooms/:id/assets` → `asset.view`, `m04-assets.md` §7, BUKAN `location.view`); `listByRoom()` mengembalikan `assets: []`, `ringkasan` dengan seluruh empat kondisi (`BAIK`/`RUSAK_RINGAN`/`RUSAK_BERAT`/`HILANG`) bernilai 0, `jumlah_dipinjam`/`jumlah_dalam_perbaikan` 0, dan metadata paginasi (`page`/`per_page`/`total_pages`) benar; filter kategori/kondisi/status DITERIMA dan tercermin di `page`/`per_page` tanpa mengubah `assets` (kontrak siap `PR-02-10`); ruangan tak ada → `NotFoundError`. Plus 6 uji murni skema Zod (`asset.schema.test.ts`): enum `asset_condition`/`asset_status` (0002), `per_page` maksimum 100. | `FR-03.2` |
| Baris kode produksi vs uji, `PR-01-07` | kode `S` · uji `S` (§7) | **287** (`M`) / **231** (`M`) — kode DAN uji meleset satu kelas ke atas: modul `m04-assets` lahir PERTAMA kali (enam berkas SDD-00 §4.1), meski isinya hanya satu endpoint baca — biaya scaffolding modul baru tidak sebanding ukurannya dengan jumlah endpoint (bandingkan `PR-01-02`/`PR-01-05` yang juga `L` dengan JAUH lebih banyak endpoint) | kalibrasi §3 |
| Acceptance `PR-01-08` — penelusuran activity log (`FR-18.2`) | filter kombinasi (tanggal, pengguna, role, modul, aksi, entitas); akses tercatat sebagai `ACTIVITY_LOG_VIEWED` | **Terbukti** — 7 uji integrasi (PostgreSQL nyata) `m18-activity-log.test.ts`: katalog registri (`activity_log.view`); filter modul+aksi mengembalikan HANYA baris cocok, terurut terbaru lebih dulu; filter `user_id` tunggal; filter `entitas`+`entitas_id` berpasangan; filter rentang tanggal (`dari`/`sampai`) mengecualikan baris di luar rentang (dibuktikan lewat baris yang sengaja ditaruh DI LUAR rentang lalu terbukti tidak ikut); paginasi (`page`/`per_page`) dengan `total`/`total_pages` benar lintas 3 halaman; setiap panggilan `list()` mencatat `ACTIVITY_LOG_VIEWED` bersama filter yang dipakai pada `nilai_sesudah` (dibaca ulang dari `activity_logs` langsung). Plus 6 uji murni skema Zod (`activity-log.schema.test.ts`): bawaan `page`/`per_page`, kombinasi filter, `dari > sampai` ditolak, `dari === sampai` diterima, `per_page` maksimum 100, `user_id` bukan angka positif ditolak. Kriteria "≤3 detik" TIDAK diklaim terbukti di sini (keputusan 24) — diperiksa gerbang keluar phase. | `FR-18.2` |
| Baris kode produksi vs uji, `PR-01-08` | kode `M` · uji `M` (§7) | **390** (`M`) / **309** (`M`) — PERTAMA di antara PR modul-baru Phase 01 yang mendarat TEPAT pada kelas rencana (bandingkan `PR-01-02`/`PR-01-05`/`PR-01-07` yang semuanya meleset ke atas): satu endpoint dengan filter kombinasi enam dimensi + tulis `ACTIVITY_LOG_VIEWED` ternyata sepadan dengan perkiraan `M`, berbeda dari kerangka satu-endpoint `PR-01-07` yang terlalu kecil untuk menyerap biaya scaffolding | kalibrasi §7 |

## 9. Gerbang keluar

Diisi saat phase dinyatakan selesai. Daftar lengkapnya ada di [`phase-01.md` §9 dan §12](../phases/phase-01.md).

- [ ] Seluruh 16 PR tergabung
- [ ] Acceptance checklist phase terpenuhi
- [ ] Definition of Done phase terpenuhi
- [ ] Bagian 5 log ini terisi seluruhnya
- [ ] [`IMPLEMENTATION-STATUS.md`](../IMPLEMENTATION-STATUS.md) diperbarui

## 10. Yang diserahkan ke phase berikutnya

Hal yang sengaja ditinggalkan terbuka, beserta di mana ia akan ditutup.

| Yang ditinggalkan | Ditutup di | Alasan penundaan |
|---|---|---|
| `users.foto_file_id` → `stored_files(id)` (foto profil `FR-01.4`) | `PR-03-04` (keputusan 4) | `stored_files` belum ada (`SDD-FS-02`) |
| Daftar password bocor + riwayat 3 password terakhir (`NFR-S-03a`) | `PR-02-31` (keputusan 9) | Menuntut sumber daftar dan tabel riwayat yang belum ditetapkan SDD mana pun |
| `failed_login_count`, `locked_until`, `totp_secret_enc`, `totp_enabled_at` pada `users` | `PR-02-03`, `PR-02-07` (`SDD-04 §4.1`) | Kolom autentikasi milik Phase 02 |
| `BR-015` sungguhan: `PATCH /rooms/{id}/status` menolak nonaktif bila ruangan masih memuat aset | `PR-02-10` (keputusan 22) | Tabel `assets` belum ada; `PR-01-06` hanya menegakkan kerangka hierarki (gedung vs ruangan aktif di bawahnya) |
| `FR-03.1 A2`: menonaktifkan ruangan berreservasi mendatang menampilkan daftar reservasi terdampak & meminta konfirmasi | Modul reservasi (`M-07`, Phase 02) | `booking_slots`/reservasi belum ada (keputusan 22) |
| **`NT-48`** (notifikasi in-app ke Administrator saat pengaktifan/pembuatan akun siswa ditolak, `DP-02`) dan **`NT-40`** (role/status akun berubah, belum terbit sejak `PR-01-02`) | **`PR-02-25`** (Phase 02, M-17) — keputusan 32; tugasnya tercatat di `phases/phase-02.md` §8 | Modul notifikasi belum ada; `NT-48` tidak dapat terbit dari transaksi yang ditolak (`SDD-EVT-04`), sehingga perlu transaksi terpisah dan event baru di katalog `SDD-07 §4.3` |
| Pencabutan persetujuan wali; akun siswa lama tanpa `consent_guardian_at` | Belum didefinisikan `DP-02` — perlu keputusan pemilik produk bila diperlukan | Tidak ada jalur mencabut penanda; siswa yang dibuat sebelum gerbang tetap aktif tanpa penanda dan hanya terblokir saat dinonaktifkan lalu diaktifkan kembali |
| **`NT-52`** — konsumen event `UserImportCompleted` (notifikasi in-app "impor pengguna selesai") | **`PR-02-25`** (Phase 02, M-17) — keputusan 33; tugasnya tercatat di `phases/phase-02.md` | Event sudah terbit sejak `PR-01-17`; hanya modul notifikasi yang belum ada |
| Masa simpan **laporan impor** (`user_import_jobs.laporan_gagal` memuat email baris gagal; barisnya tidak dihapus) | Pemilik produk — tidak didefinisikan PRD; kandidat digabung ke pseudonimisasi `PR-05-26` (`DP-04`) | Berkas mentah sudah dikosongkan saat berakhir (`DP-03`), tetapi laporannya tetap |
| Batas atas jumlah baris jalur asinkron | Kalibrasi Phase 07–08 (kelompok B) | Kini hanya dibatasi ukuran body 8 MB (`BATAS_BODY_JSON`); pekerjaan yang sangat besar memakan waktu proporsional dengan hashing Argon2id per baris |
| Celah pemulihan `JOB-06`: crash tepat di antara commit `UserService.create` dan `catatBaris` | Tidak dikerjakan — satu baris paling banyak, dan menutupnya menuntut `UserService.create` menerima transaksi luar | Baris itu dilaporkan "sudah digunakan" pada percobaan ulang, padahal berhasil dibuat |
| `docs/UX/USER-FLOWS.md` masih menulis `NT-42` untuk impor asinkron | Koreksi lapisan UX — bukan bagian PR ini | `NT-42` milik `m16-analytics`; keputusan 19(a) hanya memperbaiki PRD dan SDD-14 |
| **Memasang pekerjaan `student-graduation`** ke worker (`SL-03`, `DP-10`): `GraduationService.deactivateDueGraduates` sudah ada dan teruji; belum ada yang memanggilnya secara terjadwal, sehingga lulusan **belum dinonaktifkan otomatis** | **`PR-02-32`** (Phase 02, setelah `PR-02-02`, tinjauan arsitek) — keputusan 31 | Pelaku job `SYSTEM` (`AL-06`, `SDD-03`) tidak dapat dibentuk: `AuthContext.userId` wajib bilangan bulat dan dipakai `updated_by` pada seluruh repository. `PR-02-32` merancang `SystemAuthContext` bersama `AuthContext` hasil `PR-02-02`, dan menyesuaikan `GraduationService` (entri log pelaku `SYSTEM`, ringkasan `JOB-05`) |
| **Mendaftarkan pemeriksa kewajiban siswa** (peminjaman aktif, denda belum lunas) ke `studentObligations`, di proses API **dan** worker | `PR-05-09` (penutupan `SL-04`) — tugasnya kini memuat pendaftaran di proses API **dan** worker (keputusan 31) | Definisi kewajiban baru ada setelah peminjaman dan denda ada (Phase 05); sampai itu registri kosong dan `SL-04` belum memblokir apa pun |
| `SL-05` (pseudonimisasi akun siswa nonaktif setelah 2 tahun, `DP-10`) dan `DP-04` (hak penghapusan) | **`PR-05-26`** (Phase 05, setelah `PR-05-09`) — keputusan 31 | DP-10 mengecualikan akun yang masih berkewajiban, dan definisi kewajiban baru ada di `PR-05-09`; bukan bagian task breakdown `PR-01-13` (hanya `SL-01`…`SL-04`) |
| **Endpoint `work_units`** (daftar, buat, sunting, nonaktifkan — halaman `P-72`) | **`PR-01-18`** (keputusan 29); sementara: [runbook](../runbooks/master-data-awal.md) bagian 2 | Baris endpoint kini ada di PRD (`m20-settings.md` §7, keputusan 29); PR-nya belum dikerjakan. Sampai tergabung, master `work_units` hanya dapat diisi di luar aplikasi — dan `kode_unit_kerja` pada impor pengguna hanya berhasil untuk kode yang sudah ada di sana |
| **Menjalankan `map_users_unit_kerja()`** setelah master `work_units` terisi, dan menutup daftar tak terpetakannya | Operator, sebelum `PR-08-11` (contract) — [runbook](../runbooks/master-data-awal.md) bagian 3 (keputusan 29) | `work_units` lahir kosong; migration hanya menjalankannya sekali (no-op). Tidak ada penebakan — sisa tak terpetakan diputuskan manusia |
| `procurements.unit_kerja` → `work_unit_id` (`WU-01`) | Phase 05 (`M-14`, tabel `procurements` belum ada) | Tabel belum ada; pola `SDD-DB-19` berlaku |
| **Endpoint kalender akademik** (tahun ajaran, semester, hari libur, aktivasi — halaman `P-71`) beserta validasi "semester di dalam rentang tahun ajaran" dan pemicu `AC-YR-02` | **`PR-01-18`** (keputusan 29); sementara: [runbook](../runbooks/master-data-awal.md) bagian 1 | PRD belum mendaftarkan endpoint-nya (`m20-settings.md` §7); baris endpoint baru wajib masuk PRD lebih dulu. Tanpa ini DoD Phase 01 "Kalender akademik terisi" (`AC-YR-01`) tidak dapat dipenuhi lewat aplikasi |

---

*Log ini mencatat pelaksanaan. Requirement tetap milik [PRD](../../PRD/), keputusan desain tetap milik [SDD](../../SDD/).*
