# Log Phase 02 — Inti Sistem

| | |
|---|---|
| **Phase** | [`phases/phase-02.md`](../phases/phase-02.md) |
| **Milestone PRD** | `M1 (sebagian) · M2 (sebagian) · M5 (sebagian)` |
| **Status** | Lihat [`IMPLEMENTATION-STATUS.md`](../IMPLEMENTATION-STATUS.md) |
| **Mulai** | 19 September 2026 |
| **Selesai** | — |

Log ini mencatat **apa yang benar-benar terjadi** selama phase berjalan: keputusan yang diambil, hal yang berbeda dari rencana, dan angka hasil pengukuran. Ia bukan salinan rencana — rencananya ada di [`phases/phase-02.md`](../phases/phase-02.md).

Log tidak boleh memuat requirement, keputusan desain, maupun business rule baru. Bila selama phase muncul kebutuhan akan salah satunya, ia dinaikkan ke PRD atau SDD lebih dulu, dan log ini hanya mencatat bahwa hal itu terjadi.

---

## 1. Catatan harian

| Tanggal | Yang terjadi | PR terkait |
|---|---|---|
| 19 September 2026 | Phase 02 dimulai selagi Phase 01 `In Review` (gerbang lulus bersyarat backend). `PR-02-02` dikerjakan lebih dulu: migration `0021_refresh_tokens.sql`, `JwtKeys` (EdDSA lewat `node:crypto`), refresh token buram + rotasi, modul `m01-auth` (`POST /auth/login`, `POST /auth/refresh`), `authenticate` + gerbang ganti password. 34 uji integrasi + 66 uji unit baru; delapan mutasi pada `AuthService` semuanya terdeteksi uji. | `PR-02-02` |
| 19 September 2026 | `PR-02-02` tergabung ([#62](https://github.com/HanzzzBD/SIGM4/pull/62)). `PR-02-03` dibuka ([#63](https://github.com/HanzzzBD/SIGM4/pull/63), `feature/PR-02-03-penguncian-akun`): migration `0022` (`failed_login_count`, `failed_login_window_start`, `locked_until`), `lockout.ts` (aritmetika jendela tetap), penguncian + audit percobaan gagal di `AuthService.login`, `AuditLogger.writeAnonim`, event `AccountLocked`. Pemilik produk mengubah kontrak login menjadi **respons seragam** (keputusan 10): 401 untuk email tak dikenal, password salah, dan akun terkunci. 13 uji unit + 20 uji integrasi baru; 14 mutasi seluruhnya terdeteksi. | `PR-02-03` |
| 19 September 2026 | `PR-02-03` tergabung ([#63](https://github.com/HanzzzBD/SIGM4/pull/63)). `PR-02-04` dibuka ([#64](https://github.com/HanzzzBD/SIGM4/pull/64), `feature/PR-02-04-logout-sesi`): varian route `authenticated: true` (`SDD-AUTH-12`), `SessionStore` (sesi hidup diperiksa `authenticate` pada setiap permintaan), `SessionService`/`SessionRepository`, empat endpoint sesi (`logout`, `logout-all`, `GET /auth/sessions`, `DELETE /auth/sessions/{id}`), event `SessionRevoked`. 29 uji integrasi + 24 uji unit baru/diubah; 24 mutasi seluruhnya terdeteksi (dua yang lolos pada percobaan pertama menghasilkan dua uji lapis-pertahanan tambahan). | `PR-02-04` |
| 19 September 2026 | `PR-02-04` tergabung ([#64](https://github.com/HanzzzBD/SIGM4/pull/64)). `PR-02-05` dibuka ([#65](https://github.com/HanzzzBD/SIGM4/pull/65), `feature/PR-02-05-reset-password`): migration `0023` (`password_reset_requests` + dua enum), `PasswordResetService` (forgot publik, antrean, terbitkan, tolak), reset langsung `POST /users/{id}/reset-password` (M-02, logikanya M-01 lewat injeksi), kedaluwarsa 72 jam saat login, `generateTemporaryPassword` dipindah ke `shared/security`. Pembacaan PRD menemukan rencana phase keliru ("token sekali pakai", `NT-41`): `FR-01.3` adalah reset administratif (keputusan 21). 35 uji integrasi + uji unit registri/route; 27 mutasi seluruhnya terdeteksi. | `PR-02-05` |
| 20 September 2026 | `PR-02-05` menunggu tinjauan ([#65](https://github.com/HanzzzBD/SIGM4/pull/65)), belum tergabung ke `main`. `PR-02-06` dibuka di atas cabangnya (`feature/PR-02-06-ganti-password-profil`): `ProfileService`/`ProfileRepository` baru (FR-01.4) — `POST /auth/password/change` (verifikasi password lama, kebijakan `NFR-S-03a` panjang/komposisi/identitas TANPA daftar bocor/riwayat 3 password yang menunggu `PR-02-31`, mencabut sesi lain lewat `SessionRepository.cabutSemuaKecuali` baru, menerbitkan access token baru berklaim `pwd=false` bagi sesi ini sendiri, menyelesaikan penerbitan reset `DITERBITKAN` + event `PasswordChangedAfterReset`/`NT-38a`), `GET`/`PUT /me` (foto ditunda `PR-03-04`, email/role diabaikan — `BR-069`). Uji integrasi baru ditulis mengikuti pola `auth-session.test.ts`/`auth-password-reset.test.ts`, tetapi **tidak dapat dijalankan di lingkungan pengembangan ini**: tanpa Docker dan tanpa akun/skema `sigm4_app` pada PostgreSQL lokal yang tersedia (lihat §7). `tsc`, `eslint`, dan `audit_docs.py` lulus. | `PR-02-06` |
| 20 September 2026 | Docker diaktifkan; stack dev (`docker-compose.yml`) dinyalakan dan uji integrasi `PR-02-06` dijalankan sungguhan untuk pertama kali. Ditemukan gerbang final review menemukan REGRESI pada `npm run test:ci -w apps/api` (4 uji merah di berkas LAIN, gejala: R-01 kehilangan `setting.view` sesaat) yang tidak ada pada baseline sebelum PR ini. Investigasi bisection manual (>15 percobaan suite penuh, database di-reset bersih berulang lewat `docker compose down -v`) sempat menyimpulkan pemicunya `Promise.all` pada `ProfileService.lihat()` — **kesimpulan itu kemudian TERBUKTI KELIRU** (positif palsu sampel kecil); sekaligus menemukan bug nyata terpisah pada cleanup uji (`event_outbox` tanpa filter `event_name`, diperbaiki). Atas arahan pemilik produk investigasi dilanjutkan sebagai pekerjaan tersendiri, dan dengan instrumentasi sementara pada `PermissionCache` **akar masalah akhirnya terbukti**: kunci cache `perm:{userId}:{roleVersion}` tidak memuat role, sementara id pengguna dipakai ulang setelah `dbmate down` — tabrakan kunci di dalam TTL 60 detik. `PR-02-06` TIDAK di-push; perbaikan kunci cache dieskalasi ke PR tersendiri (lihat §7, §10). Rincian lengkap: §7. | `PR-02-06` |
| 20 September 2026 | Gerbang final `PR-02-06` menemukan regresi intermiten pada `npm run test:ci -w apps/api`: uji di berkas LAIN memerah (~70% percobaan) dengan gejala berpindah-pindah — kadang R-01 kehilangan `setting.view`, kadang Guru justru memegang `user.reset_password`. Investigasi terpisah dengan instrumentasi sementara pada `PermissionCache` membuktikan akar masalahnya: kunci cache `perm:{user_id}:{role_version}` tidak memuat identitas role. Cabang ini (`fix/PR-01-04-kunci-cache-permission`) memperbaikinya; rincian pada §4, §7, dan §10. | `fix/PR-01-04-kunci-cache-permission` |

## 2. Keputusan yang diambil

Keputusan teknis yang tidak berasal dari PRD maupun SDD, dan alasannya. Bila sebuah keputusan ternyata menyentuh requirement atau desain, ia **wajib** dinaikkan ke PRD/SDD, bukan diselesaikan di sini.

| # | Keputusan | Alasan | Menaikkan ke PRD/SDD? |
|:---:|---|---|:---:|
| 1 | Cakupan `PR-02-02`: login, refresh, `authenticate` (Bearer/cookie → `AuthContext` lewat `PermissionCache`), dan gerbang ganti password (klaim `pwd`). TANPA `LOGIN_FAILED`/penguncian (`PR-02-03`), logout (`PR-02-04`), ganti password (`PR-02-06`), 2FA (`PR-02-07`). | Rencana phase tidak menyebut `authenticate` maupun gerbang secara eksplisit, tetapi acceptance "refresh dipakai ulang → rantai dicabut" tak dapat diuji end-to-end tanpa token yang diverifikasi, dan `PR-01-15` sudah menyiapkan `authorize` yang menunggunya. | Ya — diputuskan pemilik produk (19 September 2026); `SDD-04 §4.7` ditambahkan. |
| 2 | Dua jalur token: WEB lewat cookie `httpOnly; Secure; SameSite=Strict` (body `tokens: null`), ANDROID/IOS lewat body + `Authorization: Bearer`. | `SDD-SESS-05` menetapkan cookie untuk web dan Keychain/Keystore untuk mobile, tetapi tidak bagaimana satu endpoint melayani keduanya. | Ya — `SDD-04 §4.7`, PRD `m01-auth.md` §7. |
| 3 | Body login memuat `platform` (`WEB`/`ANDROID`/`IOS`); disimpan pada `refresh_tokens.platform` dan diwarisi setiap rotasi, tidak pernah dari permintaan refresh. | Masa berlaku refresh (12 jam vs 30 hari) dan jalur token bergantung padanya; membacanya dari `User-Agent` dapat dipalsukan dan tidak andal. Enum `device_platform` sudah tertera di `SDD-04 §4.1`. | Ya — PRD Bab 11.3 (`Platform Perangkat`), `m01-auth.md` §7. |
| 4 | Kode galat `PASSWORD_CHANGE_REQUIRED` (403) ditambahkan. | Klien perlu membedakan "wajib ganti password" dari `INSUFFICIENT_PERMISSION` agar dapat mengarahkan pengguna ke layar ganti password (`FR-01.1 A4`). | Ya — PRD `api-conventions.md` Bab 17.3. |
| 5 | JWT ditulis di atas `node:crypto` (satu algoritma, satu kunci, klaim tetap), tanpa pustaka JWT. | Permukaan yang dibutuhkan < 100 baris; menghindari dependensi pada jalur autentikasi (SCA/rantai pasok). Kelas bug JWT (`alg=none`, kebingungan HS256↔kunci publik, `kid`/`crit`/`jku`, base64url longgar, kedaluwarsa pada tanda tangan palsu) diuji satu per satu di `tests/shared/security/jwt.test.ts`. | Tidak |
| 6 | `authenticate` **lenient**: token tak sah hanya ditandai, penolakan milik `authorize`. | Bila menolak di sini, `/auth/refresh` dan `/auth/login` tak dapat dipanggil dengan cookie access kedaluwarsa — padahal itulah saat refresh dibutuhkan. | Ya — `SDD-04 §4.7`. |
| 7 | Pencabutan karena pemakaian ulang di-commit meski respons `401`: hasil transaksi dikembalikan sebagai nilai, galat dilempar setelah commit. | Melempar dari dalam transaksi membatalkan pencabutan itu sendiri; acceptance justru menuntut rantai tercabut. | Ya — `SDD-04 §4.3`. |
| 8 | Pesan `401` tetap generik (`Permintaan tidak dapat diproses.`); `PESAN` di `api/chain.ts` tidak diubah. | Kontrak amplop galat lintas modul (keputusan 35 log phase-01) menguji pesan itu; klien memetakan `code` ke teks (`NFR-AC-09`). Sempat ditambahkan lalu dibatalkan setelah `error-envelope-contract.test.ts` merah. | Tidak |
| 9 | Jendela "5 gagal dalam 15 menit" adalah **jendela tetap** dari kegagalan pertama; kolom `failed_login_window_start` ditambahkan pada `users` (migration `0022`). | `SDD-04 §4.1` hanya punya `failed_login_count` dan `locked_until`; "dalam 15 menit" tak dapat dinyatakan tanpa titik awal jendela. Alternatif ditolak: tanpa jendela (bertentangan dengan "dalam 15 menit"), menghitung dari `activity_logs` (login bergantung pada tabel log berantai-hash). | Ya — dipilih pemilik produk (19 September 2026); `SDD-04 §4.1/§4.2`, PRD `m02-users.md` §8 dan `data-model.md` (atribut `users`). |
| 10 | Respons `/auth/login` **seragam** (`SDD-SESS-12`): email tak terdaftar, password salah, dan akun terkunci dijawab `401 UNAUTHENTICATED` yang identik; `423` dan sisa waktu kunci tidak pernah keluar dari endpoint ini. Penguncian tetap berjalan dan dicatat secara internal. | Acceptance rencana phase menuntut "pesan galat tidak membocorkan keberadaan akun", sedangkan `SDD-04 §4.2` dan PRD `FR-01.1 A2` menjawab `423` bagi akun terkunci — yang membuktikan sebuah email terdaftar. Pemilik produk memilih opsi di luar tiga yang saya tawarkan (penguncian bayangan Redis / ikuti SDD apa adanya / selalu 401 tanpa syarat) dan menetapkan cakupannya: tanpa `423`, tanpa sisa waktu, tanpa tabel penghitung per-email baru, tanpa mekanisme pemberitahuan pengguna baru. | Ya — kontrak berubah: PRD `m01-auth.md` (diagram, `A1`, `A2`, acceptance, baris endpoint, aksi log), `api-conventions.md` Bab 17.3 (catatan `423`), `SDD-04` (`SDD-SESS-12`, §4.2, §5, §6), UX `PAGE-SPECIFICATION §7.3` dan `USER-FLOWS F-01`. |
| 11 | Argon2id dijalankan tepat sekali pada ketiga keadaan (hash pengganti bila email tak ada; hash asli bila akun terkunci). Percobaan atas akun terkunci dicatat (`LOGIN_FAILED`, alasan `AKUN_TERKUNCI`) tetapi TIDAK dihitung dan tidak memperpanjang kunci. | Respons yang identik tidak berarti bila waktunya berbeda; menghitung percobaan selagi terkunci membuat siapa pun dapat memperpanjang kunci akun orang lain tanpa batas. | Ya — `SDD-SESS-12`, `SDD-04 §4.2`. |
| 12 | `LOGIN_FAILED` dicatat juga untuk email tak terdaftar lewat metode baru `AuditLogger.writeAnonim` (`user_id`/nama/role NULL; akun sasaran pada `entitas`/`entitas_id`; email yang dicoba tidak disimpan). | `AL-02`/`AL-07` mewajibkan kegagalan login tercatat, dan `AuditLogger` hanya mengenal pelaku ber-`AuthContext` atau `SYSTEM`. `AuditLogger` adalah tulang punggung — **tinjauan arsitek**. | Ya — dipilih pemilik produk (19 September 2026); PRD `m01-auth.md` §11. |
| 13 | `NT-39` terbit sebagai event outbox `AccountLocked` di transaksi penguncian (agregat `user`, payload `user_id`, `terkunci_sampai`); konsumennya dipasang `PR-02-25`. `actor_id` outbox = akun yang terkunci, karena permintaan ini tak berautentikasi dan `publish` mensyaratkan `AuthContext`. | Modul notifikasi belum ada; pola yang sama dengan `NT-48`/`NT-52` (keputusan 32–33 log phase-01), sehingga `PR-02-25` tidak perlu menyunting alur login. | Ya — dipilih pemilik produk; `SDD-07 §4.3`. |
| 14 | `ACCOUNT_UNLOCKED` tidak diterbitkan. | Kunci berakhir sendiri tanpa proses yang berjalan, dan tidak ada jalur pembuka kunci administratif di PRD mana pun. PRD `m01-auth.md` §11 mencatat bahwa kunci yang berakhir tidak menulis entri. | Ya — PRD `m01-auth.md` §11. |
| 15 | Kelas route baru `authenticated: true` untuk endpoint "Bearer" (`SDD-AUTH-12`): varian ketiga `RouteDefinition` yang saling meniadakan dengan `permission`/`public`; registri, OpenAPI (`x-authenticated`), dan `SEC-T-01` mengenalnya. | `PM-01` menuntut permission per endpoint, tetapi belasan endpoint PRD berstatus "Bearer" tanpa permission katalog. Ditolak: permission katalog untuk semua role (mengotori Lampiran C) dan `public: true` + cek di controller (meniadakan `SDD-AUTH-01`). Menyentuh lapisan permission — **tinjauan arsitek**. | Ya — dipilih pemilik produk (19 September 2026); PRD `roles-permissions.md` `PM-01`, `SDD-03` (`SDD-AUTH-12`, §3, §4.1). |
| 16 | Empat endpoint sesi ditambahkan ke PRD `m01-auth.md` §7: `POST /auth/logout-all`, `GET /auth/sessions`, `DELETE /auth/sessions/{id}` (di samping `POST /auth/logout` yang sudah ada). Id sesi = `family_id`; `403` seragam untuk sesi orang lain dan yang tidak ada; mencabut sesi yang sudah tercabut → `204` idempoten. | `FR-01.2 A1` dan UX P-79 (Perangkat Terhubung) meminta keluar dari semua perangkat, daftar sesi aktif, dan cabut satu perangkat, tetapi PRD §7 hanya mendaftarkan logout. | Ya — dipilih pemilik produk; PRD `m01-auth.md` (FR-01.2 A3, acceptance, §7, §11), `SDD-04 §4.6`. |
| 17 | Setiap pencabutan sesi menerbitkan event outbox `SessionRevoked` (payload `user_id`, `family_id`, `platform`, `alasan`; satu per sesi) di transaksinya; konsumen yang menonaktifkan device token FCM (`MOB-SEC-05`) dipasang `PR-02-25`. | Tabel `device_tokens` baru lahir di `PR-02-25`; pola yang sama dengan `AccountLocked`/`NT-52`, sehingga alur logout tidak perlu disunting lagi. | Ya — dipilih pemilik produk; `SDD-07 §4.3`. |
| 18 | `authenticate` memeriksa sesi hidup (`sid` = `family_id`, milik `sub`, masih punya baris belum dicabut) ke PostgreSQL pada SETIAP permintaan, bukan lewat cache. Akibatnya logout, cabut perangkat, dan pemakaian ulang refresh token mematikan access token seketika. | Acceptance "≤ 60 detik" dipenuhi tanpa jendela; `SDD-04 §5` menegaskan kebenaran pencabutan tak boleh bergantung pada Redis. Biayanya satu kueri berindeks per permintaan; cache berumur pendek dengan pembatalan eksplisit dicatat sebagai jalan keluar bila `NFR-P-09` menunjukkannya mahal. | Ya — `SDD-04 §4.7`, `§5`. |
| 19 | Yang ditampilkan `GET /auth/sessions` adalah token yang sedang berlaku (belum dirotasi, dicabut, atau kedaluwarsa) — satu per keluarga; `dibuat_pada` = penerbitan token pertama keluarga (login), `terakhir_diperbarui` = penerbitan token yang sedang berlaku. Mencabut satu perangkat memakai `LOGOUT` dengan `alasan: device_revoked`, bukan aksi log baru. | Aksi log PRD hanya `LOGOUT`/`LOGOUT_ALL_DEVICES`; menambah aksi baru adalah requirement baru. Sesi yang refresh-nya kedaluwarsa tidak dapat lagi diperbarui sehingga tidak layak disebut aktif. | Ya — PRD `m01-auth.md` §11. |
| 20 | `RouteBase.successStatus` ditambahkan (200/201/202/204) agar OpenAPI menggambarkan status sukses yang sebenarnya; `204` tanpa badan. Dipakai empat route sesi. | Generator sebelumnya memaksa `POST → 201`, yang keliru untuk logout (204). Route lama tidak disentuh (di luar cakupan PR ini): `POST /auth/login` dan `/auth/refresh` masih terdokumentasi `201` padahal menjawab `200`. | Tidak — mekanisme, `SDD-AUTH-11` terpenuhi; butir tindak lanjut di §10. |
| 21 | `PR-02-05` mengimplementasikan reset password **administratif** sesuai `FR-01.3` (Administrator memverifikasi identitas luring, mencatat metode, lalu menerbitkan password sementara sekali tampil) — bukan "token sekali pakai" seperti judul rencana. Rujukan `NT-41` (ringkasan harian M-15) diganti `NT-37`/`NT-38`. | PRD adalah sumber kebenaran: `FR-01.3` eksplisit menyatakan sistem tidak memakai kanal email (keputusan PRD 7), sehingga token yang dikirim ke pemohon tak memiliki jalur pengiriman. Rencana phase (lapisan turunan) yang diperbaiki. | Ya — `phase-02.md` baris `PR-02-05`; `SDD-SESS-13` menuliskan alasannya. |
| 22 | Endpoint yang dibangun (dipilih pemilik produk, 19 September 2026): `POST /auth/password/forgot` (publik) · `GET /auth/password/requests` · `POST /auth/password/requests/{id}/issue` · `POST /auth/password/requests/{id}/reject` (ketiganya `user.reset_password`) · dan reset langsung `POST /users/{id}/reset-password` (M-02, sudah tercantum di PRD tetapi belum ada implementasinya). | PRD `m01-auth.md` §7 hanya mendaftarkan `forgot`; antrean P-67 (daftar, catat metode verifikasi, terbitkan, tolak) tak punya endpoint, dan `/users/{id}/reset-password` tak dimiliki PR lain di rencana phase. | Ya — PRD `m01-auth.md` §7, `m02-users.md` §7, `SDD-04 §4.8`. |
| 23 | Penerbitan password sementara mencabut SELURUH sesi pemilik akun (`revoke_reason = password_reset`, event `SessionRevoked`) dan menghapus penguncian loginnya. | `FR-01.3` diam soal keduanya (butir yang saya tunda dari `PR-02-03` dan `PR-02-04`). Identitas pemilik sudah diverifikasi tatap muka, sedangkan sesi lama mungkin milik pihak yang tak berhak dan kunci yang tersisa tak boleh menghalangi password barunya. | Ya — PRD `m01-auth.md` (FR-01.3 langkah 4, acceptance), `SDD-SESS-13`. |
| 24 | Kedaluwarsa 72 jam (`FR-01.3 A3`) ditegakkan SAAT LOGIN dan saat antrean dibaca (status efektif dihitung dari `kedaluwarsa_pada`), tanpa pekerjaan terjadwal. Password sementara yang lewat batas ditolak 401 seragam, permintaannya ditutup `KEDALUWARSA`, dan tidak menambah penghitung kunci. Batas hanya berlaku bagi password hasil reset — akun baru buatan Administrator (`FR-02.1`) tidak dibatasi. | Pekerjaan terjadwal bergantung `SystemAuthContext` (`PR-02-32`); password sementara hanya berbahaya saat dipakai, yaitu saat login. | Ya — PRD `A3`, `SDD-SESS-13`, `SDD-04 §4.2/§4.8`. |
| 25 | Data PRD baru (disetujui pemilik produk): enum **Status Permintaan Reset Password** (Menunggu, Diterbitkan, Ditolak, Selesai, Kedaluwarsa) dan **Metode Verifikasi Identitas** (Kartu Identitas Tatap Muka, Konfirmasi Atasan atau Wali Kelas) pada Bab 11.3; atribut `alasan_penolakan`; akun NONAKTIF diperlakukan seperti email tak terdaftar; permintaan ke-4 dalam 24 jam dijawab netral tetapi tidak dibuat dan dicatat anomali (`PASSWORD_RESET_REQUESTED`, hasil `GAGAL`). | `FR-01.3` menyebut kedua kanal verifikasi dan siklus permintaan tanpa menuliskan enum-nya; `A4` menyebut "anomali keamanan" tanpa aksi log. | Ya — `data-model.md` Bab 11.3 (diuji `enums.test.ts`), `m01-auth.md` `A1`/`A4`/§11. |
| 26 | Reset langsung memakai injeksi: `POST /users/{id}/reset-password` tetap milik M-02, tetapi logikanya (`password_reset_requests`, penerbitan) milik M-01 dan dipanggil lewat `UsersModuleDeps.penerbitPassword`, dipenuhi `buatPenerbitPasswordSementara` di composition root. `generateTemporaryPassword` dipindah dari `m02-users/services` ke `shared/security`. | Modul tidak boleh mengimpor internal modul lain (`SDD-SYS-03`); keduanya kini memakai pembangkit password sementara sehingga tempatnya bukan lagi satu modul. Perilaku tak berubah. | Tidak — mekanisme, `SDD-AUTH-11` terpenuhi. |
| 27 | Penerbitan baru menggantikan yang lama: permintaan `DITERBITKAN` sebelumnya milik akun itu menjadi `KEDALUWARSA`. Reset langsung menyelesaikan permintaan `MENUNGGU` akun itu bila ada, bila tidak mencatat permintaan baru langsung `DITERBITKAN`. Kunci baris diambil dalam urutan tetap (pengguna, lalu permintaan) agar jalur per-permintaan dan jalur langsung tak saling menunggu (deadlock). | Hash password hanya satu; password sementara lama sudah tak berlaku, jadi permintaannya tak boleh tampak masih aktif. Metode verifikasi tetap wajib dan tercatat untuk setiap penerbitan (`FR-01.3 AC`). | Ya — `SDD-04 §4.8`. |
| 28 | Pemohon `forgot` belum diautentikasi: `PASSWORD_RESET_REQUESTED` dicatat lewat `AuditLogger.writeAnonim` (sasaran pada `entitas_id`, email tak disimpan) dan event `PasswordResetRequested` memakai `AuthContext` pemohon sebagai pelaku outbox — pola yang sama dengan `LOGIN_FAILED`/`AccountLocked`. | `publish` mensyaratkan konteks; pemohon tak punya sesi. | Tidak. |
| 29 | `POST /auth/password/change` menerbitkan access token BARU bagi sesi yang sama (`sid` tetap, klaim `pwd=false`) di dalam respons sukses, dikirim lewat cookie (WEB) atau body (mobile) — pola pemilihan jalur yang sama dengan login/refresh. Refresh token TIDAK dirotasi. | Klaim `pwd` dibekukan saat token diterbitkan (`SDD-04 §4.7`); tanpa token baru, gerbang ganti password (`SDD-AUTH-09`) tetap menutup `/me` dan route lain sampai access token lama kedaluwarsa (maks 60 menit) atau klien memanggil `/auth/refresh` sendiri — bertentangan dengan `USER-FLOWS.md` P-05 yang lanjut langsung ke Dashboard tanpa langkah tambahan. | Ya — PRD `m01-auth.md` §7 (deskripsi endpoint), `SDD-04 §4.6` (baris "Ganti password → cabut sesi lain" diberi catatan token baru). |
| 30 | Aksi log baru `PROFILE_UPDATED` (nama/telepon lewat `PUT /me`), dimiliki M-01, terpisah dari `USER_UPDATED` milik M-02 (perubahan oleh Administrator). | `AL-01` mewajibkan setiap tulis tercatat; PRD `m01-auth.md` §11 belum mencantumkan aksi bagi `PUT /me` sendiri (baris FR-01.4 sebelumnya hanya menyebut `PASSWORD_CHANGED`). Kode terpisah menjaga "satu baris satu pemilik": subjek dan objek penulisnya berbeda (diri sendiri vs Administrator) meski tabelnya sama. | Ya — PRD `m01-auth.md` §11. |
| 31 | Ganti password sendiri TIDAK menghapus `failed_login_count`/`locked_until` — berbeda dari penerbitan reset administratif (`PR-02-05`, keputusan 23), yang menghapusnya. | Menutup butir terbuka baris `phases/phase-02.md`§10 (row "Jalur pembuka kunci administratif… PR-02-06") untuk bagian PR ini: endpoint ini menuntut sesi aktif (Bearer), dan sesi aktif hanya terbentuk lewat login yang sudah melewati pemeriksaan kunci — jadi pemanggilnya tidak sedang terkunci. Tidak ada requirement yang memintanya, dan menghapusnya diam-diam mengubah perilaku penguncian tanpa dasar PRD. | Tidak — pelaksanaan; TBD-nya milik `PR-02-05` (identitas terverifikasi luring), yang sudah menjawabnya untuk jalurnya sendiri. |
| 32 | `NT-38a` (`PasswordChangedAfterReset`) hanya terbit bila akun punya permintaan `password_reset_requests` berstatus `DITERBITKAN` saat password diganti (ditandai `SELESAI` dalam transaksi yang sama); akun `must_change_password` TANPA permintaan seperti itu (mis. akun baru buatan Administrator, `FR-02.1`) tidak menerbitkan apa pun. | `NT-38a` PRD berbunyi "Password berhasil diganti **setelah reset**" — bukan setelah pembuatan akun; siklus status `password_reset_requests` (`MENUNGGU → DITERBITKAN → SELESAI`) sudah menyatakan transisi mana yang relevan (`0023_password_reset_requests.sql`). | Tidak — PRD `m01-auth.md` NT-38a sudah eksplisit; ini hanya penerapan siklus status yang sudah ada. |

## 3. Penyimpangan dari rencana

| Yang direncanakan | Yang dikerjakan | Sebab | Dampak pada phase berikutnya |
|---|---|---|---|
| `PR-02-02`: "deteksi pemakaian ulang → cabut seluruh rantai + alarm"; `SDD-04 §4.3` menambah "notifikasi Administrator" | Rantai dicabut, entri `REFRESH_TOKEN_REUSE_DETECTED` ditulis, alarm berupa log `error` (`OBS-05`). Notifikasi ke Administrator belum terbit. | Modul notifikasi baru lahir di `PR-02-25`; tidak ada katalog `NT-xx` untuk kejadian ini. | `PR-02-25` memasang konsumennya; jika kejadian ini belum punya `NT-xx`, naikkan ke PRD lebih dulu. |
| Skema `refresh_tokens` `SDD-04 §4.1` (tanpa `parent_id`, `ip`, `user_agent`) | Ketiganya ditambahkan; `SDD-04 §4.1` diselaraskan. | Tugas `PR-02-02` mewajibkan `parent_id`; `ip`/`user_agent` bahan daftar perangkat `PR-02-04` sehingga tak perlu migration kedua. | Tidak ada. |
| `SDD-04 §4.2`: `423 ACCOUNT_LOCKED` + sisa waktu bagi akun terkunci; `PRD FR-01.1 A2`: "sistem menampilkan sisa waktu penguncian" | `401` seragam; tidak ada `423` dan tidak ada sisa waktu pada `/auth/login`. | Keputusan 10 — mencegah enumerasi akun. | `PR-02-07` (`/auth/2fa/verify`) masih memakai `423` (password sudah terbukti benar); ia harus memutuskan bentuk penguncian TOTP dengan mengingat `SDD-SESS-12`. UI login (Phase 04+) tidak menampilkan keadaan terkunci. |
| Skema `users` `SDD-04 §4.1` (`failed_login_count`, `locked_until`) | Ditambah `failed_login_window_start`. | Keputusan 9. | Tidak ada. |
| `SDD-04 §4.6`: logout "cabut refresh token yang dipakai"; hapus device token FCM pada logout | Logout mencabut seluruh keluarga refresh token sesi (dari `sid` pada access token), dan token FCM ditangani lewat event `SessionRevoked` (konsumen `PR-02-25`), bukan langsung. | Logout diautentikasi lewat access token ("Bearer"), sehingga yang diketahui adalah sesinya, bukan satu refresh token; token FCM belum punya tabel (keputusan 17). | `PR-02-25` wajib memasang konsumen `SessionRevoked`. |
| `SDD-04 §4.6`: "Nonaktifkan akun → cabut seluruh sesi seketika" | Tidak ada perubahan pada `m02-users`. Efeknya sudah seketika: `authenticate` menolak akun nonaktif pada permintaan berikutnya (`PM-05`) dan refresh mencabut keluarganya (`account_deactivated`, `PR-02-02`). Baris `refresh_tokens` milik akun nonaktif baru ditandai tercabut saat refresh berikutnya. | Mengait `UserService.updateStatus` ke modul auth adalah pekerjaan lintas-modul di luar `PR-02-04`; hasil yang dapat diamati sudah terpenuhi. | Bila daftar sesi atau laporan menuntut `revoked_at` yang akurat pada akun nonaktif, kaitkan di PR tersendiri. |
| `PR-02-04` berskala `M` (≤ 400 baris kode produksi) | ±630 baris kode produksi (`L`): 17 berkas, di antaranya kelas route `authenticated` + `successStatus` (±60), `SessionStore` + `authenticate` (±60), modul sesi `m01-auth` (±330), dokumen tak dihitung. | Rencana phase tidak menyebut kelas route "Bearer" (keputusan 15) dan empat endpoint sesi (keputusan 16); keduanya dibutuhkan agar acceptance dapat dibuktikan end-to-end. Tidak dipecah: kelas route tanpa endpoint pemakai tak dapat diuji nyata, dan endpoint tanpa kelas route tak dapat dideklarasikan. | Fondasi `authenticated` mengurangi ukuran `/me`, `PR-02-06`, dan PR Bearer berikutnya. |
| `PR-02-05` berskala `M` (≤ 400 baris kode produksi); judul "token sekali pakai"; `NT-41` | ±1.100 baris kode produksi (`L`): migration, `PasswordResetService`, dua repository (pra-auth dan berkonteks), lima route, controller, skema, route M-02 + injeksi. Diimplementasikan sebagai reset administratif dengan `NT-37`/`NT-38` (keputusan 21). | Rencana phase menulis alur email yang bertentangan dengan `FR-01.3`; dua alur (antrean dan reset langsung) plus enum PRD ikut karena `FR-01.3` dan `m02-users.md` §7 mensyaratkannya. Tidak dipecah: penerbitan, antrean, penolakan, dan kedaluwarsa memakai satu tabel dan satu siklus status yang tak dapat dipisah tanpa keadaan setengah jadi. | `PR-02-06` (ganti password) hanya perlu menandai permintaan `SELESAI` dan mengirim `NT-38a`. |
| Butir checklist `phase-02.md` merujuk `SDD-SESS-05` untuk deteksi pemakaian ulang dan `SDD-SESS-03` untuk `kid` | Yang berlaku adalah `SDD-SESS-04` (rantai keluarga) dan `SDD-SESS-02` (EdDSA). | Salah rujuk ID pada checklist; kode dan komentar memakai ID yang benar. | Perbaiki checklist bila `phase-02.md` disunting berikutnya. |
| `PR-02-06` berskala `S` (≤ 200 baris kode produksi) | ±520 baris kode produksi: `ProfileService`/`ProfileRepository` baru (±290), controller + skema `/me`/`password/change` (±150), `SessionRepository.cabutSemuaKecuali` (±20), pengkabelan route/index/`api/index.ts` (±60). | `FR-01.4` menggabungkan tiga permukaan yang masing-masing butuh lapisan penuh (repository→service→controller→route): ganti password (verifikasi + kebijakan + pencabutan sesi + penerbitan token baru + penyelesaian `NT-38a`) dan kelola profil (`GET`/`PUT /me`). Tidak dipecah: keduanya satu FR, satu `ProfileService`, dan memisahkannya berarti dua PR yang masing-masing bergantung pada berkas yang sama pada hari yang sama. | Fondasi `authenticated` dari `PR-02-04` (keputusan 15) sudah mengurangi ukurannya; sisanya murni permukaan baru. |

## 4. PR di luar rencana

PR yang tidak ada pada daftar [`phase-02.md` §7](../phases/phase-02.md).

| ID | Judul | Mengapa tidak terencana |
|---|---|---|
| `fix/PR-01-04-kunci-cache-permission` | Kunci cache permission memuat `role_id` | Tindak lanjut `PR-01-04`, bukan PR bernomor — pola yang sama dengan `fix/PR-00-18-graceful-shutdown` (keputusan 57 log phase-00). **Total PR rencana tetap 169.** Bug-nya pre-existing sejak `PR-01-04` dan baru terlihat saat `PR-02-06` menambah berkas uji yang mengalokasikan lebih banyak id pengguna. Tidak dimasukkan ke `PR-02-06` (keputusan pemilik produk, 20 September 2026): penyebabnya bukan milik PR itu, dan menggabungkannya akan menyembunyikan perbaikan lapisan permission di dalam PR fitur — persis yang dilarang `CLAUDE.md` ("sekalian rapikan"). |

Kolom ketiga adalah yang paling berharga di seluruh log ini. Pola yang berulang di sana menunjukkan di mana perencanaan phase berikutnya perlu diperbaiki.

## 5. Butir yang wajib tercatat pada phase ini

- [ ] Bentuk akhir skema `booking_slots` dan alasan setiap kolomnya — ia mengikat Phase 03, 04, dan 05
- [ ] Hasil uji konkurensi `PR-02-17`: berapa permintaan serentak, berapa yang berhasil
- [ ] Hasil uji *first-responder-wins* `PR-02-21`
- [ ] Penerapan `SDD-APR-13/14/15`, `SDD-NTF-10`, dan `UXD-05`/`UXD-12` — seluruh TBD terkait sudah tertutup sebelum phase dimulai
- [ ] `assets.procurement_id` dibuat nullable tanpa FK aktif — pemutus siklus M-04 ↔ M-14

## 6. TBD yang tertutup

| TBD | Keputusan | Diputuskan oleh | Tanggal |
|---|---|---|---|
| — | — | — | — |

Setiap TBD yang tertutup wajib juga diperbarui di [`../../SDD/TBD-REGISTER.md`](../../SDD/TBD-REGISTER.md). Menutupnya hanya di sini membuat register menjadi salah.

## 7. Masalah yang ditemukan

| Masalah | Dampak | Penyelesaian | Terbuka? |
|---|---|---|:---:|
| Staging menolak menyala setelah PR ini tergabung: `JWT_PRIVATE_KEY`/`JWT_PUBLIC_KEY` kini wajib pada skema API (`SDD-INF-08`). | `deploy-staging` gagal (readiness tak pernah hijau) sampai env operator diisi. | Operator menambahkan pasangan kunci ke `/etc/sigm4/staging.env` **sebelum** merge (perintah pembangkitan di `SDD-16 §4.7`). Diperingatkan pada deskripsi PR. | Ya |
| `tests/integration/users.test.ts` dan `activity-log.test.ts` (AL-03b) gagal secara lokal: `password authentication failed for user "sigm4_app"`. | Tidak terkait PR ini — akun `sigm4_app` pada `.env` lokal tidak cocok dengan basis data lokal. Lulus di CI. | Tidak diubah di PR ini. | Ya (lokal) |
| CI pertama merah: CodeQL `js/clear-text-storage-of-sensitive-data` (High) pada penulisan access token ke cookie web (`auth.controller.ts`). | Positif palsu: token adalah JWT bertanda tangan yang memang dikirim lewat cookie `httpOnly; Secure`; heuristik CodeQL menandainya karena klaim diturunkan dari `must_change_password`/`AMR_PASSWORD`. Gerbang `CD-02` menolak PR. | Identifier pada jalur token dinamai ulang (`wajib_ganti` lewat alias kolom, `AMR_KREDENSIAL`); query TIDAK dikecualikan dan penekanan per baris tidak dipakai (lihat `.github/codeql/codeql-config.yml`). CI hijau setelahnya. | Tidak |
| Saya memakai `SDD-SESS-10` untuk keputusan respons seragam, padahal ID itu sudah milik *challenge token* 2FA. | Rujukan salah pada komentar kode dan uji. | Ditemukan sebelum PR dibuka saat membaca tabel keputusan `SDD-04`; ID baru `SDD-SESS-12` (`SDD-SESS-11` sudah dipakai break-glass). | Tidak |
| Uji `auth-login-refresh.test.ts` (PR-02-02) membuktikan "IP lain tidak terdampak" dengan akun yang sama, yang kini terkunci oleh lima kegagalan tadi. | Merah setelah `PR-02-03`. | Memakai akun lain untuk pemeriksaan IP itu. | Tidak |
| Mutasi pertama pada `PR-02-04` meloloskan dua perubahan: `SessionStore.aktif` tanpa filter `user_id`, dan `SessionRepository.cabutKeluarga` tanpa filter pemilik. | Keduanya lapisan pertahanan berlapis yang tak terjangkau lewat HTTP biasa (token diterbitkan server; jalur cabut memeriksa kepemilikan lebih dulu), sehingga tak ada uji yang gagal bila salah satunya longgar. | Ditambah tiga uji langsung (token bertanda sah dengan `sub` pengguna B dan `sid` sesi A; `sid` bukan uuid; repository dipanggil dengan `AuthContext` pengguna lain). Semua 24 mutasi kini terdeteksi. | Tidak |
| Mutasi "password sementara masuk log" pertama saya lolos. | Mutasinya keliru: ia menaruh nilai lain (NIP/email) ke log, bukan password — uji tak punya alasan gagal. | Diganti dua mutasi yang benar-benar membocorkan password (ke `activity_logs` dan ke payload event); keduanya terdeteksi uji "tidak tersimpan di mana pun". Semua 27 mutasi kini terdeteksi. | Tidak |
| Tiga uji penolakan (`metode`/`alasan` kosong) lolos 200 pada percobaan pertama. | Bug helper uji: parameter bawaan JavaScript mengganti `undefined` yang disengaja dengan nilai sah, sehingga "tanpa metode" tak pernah dikirim. | Argumen opsional dibedakan lewat `...opsi` (kosong vs `undefined`), diberi komentar. | Tidak |
| Tujuh berkas uji yang membangun `usersRouter` langsung gagal kompilasi. | `UsersModuleDeps.penerbitPassword` wajib (jalur produksi tak boleh menyala tanpa penerbit). | Helper `penerbitPalsu` di `tests/helpers/auth.ts` (menolak bila dipanggil). | Tidak |
| `phase-01-gate.test.ts` menjaga daftar route tulis dan publik; route sesi berstatus `authenticated`. | Tidak merah (daftar publik tidak berubah; route `m01-auth` sudah dikecualikan dari sapuan `AL-01`), tetapi route `authenticated` tak tercakup sapuan `SEC-T-01` ber-permission. | Sapuan baru pada aplikasi terakit di `tests/shared/sec-t-01.test.ts`: setiap route `authenticated` dijawab `401` sebelum controller pada tanpa token, token rusak, skema salah, dan cookie rusak. | Tidak |
| Mutasi "cek ulang penguncian di transaksi login berhasil dibuang" (M13) terdeteksi hanya oleh uji tak langsung; balapan antara pembacaan awal dan transaksi login tidak dapat dipaksa lewat HTTP. | Perilaku terbukti lewat mutasi tetapi bukan lewat uji deterministik balapan. | Diterima; kode dijaga komentar dan `SELECT … FOR UPDATE`. | Ya |
| `phase-01-gate.test.ts` menuntut seluruh route tulis tersentuh dan mengecualikan probe kesehatan saja. | `POST /auth/login`/`refresh` (publik, tanpa `AuthContext`) membuatnya merah. | Route `m01-auth` dikecualikan dengan komentar; bukti `AL-01`-nya ada di `auth-login-refresh.test.ts`. Daftar route publik pada uji yang sama diperbarui. | Tidak |
| `tests/shared/health-http.test.ts` menjaga tiga daftar route persis (`registry.all()`, `authenticatedRoutes()`, `buildOpenApiDocument().paths`) — ketiganya merah begitu tiga route baru terdaftar. | Terdeteksi oleh `npx vitest run` biasa (bukan integrasi), tanpa perlu PostgreSQL/Redis. | Ketiga daftar diperbarui menyisipkan `GET /me`, `PUT /me`, `POST /auth/password/change` pada posisi registrasinya (setelah rute reset password, sebelum rute M-02); judul uji juga disunting menyebut `PR-02-06`. `npx vitest run` (konfigurasi bawaan — SELURUH `tests/**/*.test.ts` DI LUAR `tests/integration/`, 46 berkas/679 uji) hijau. Ini **bukan** `tests/integration/auth-password-change.test.ts` (baru) — lihat baris berikutnya. | Tidak |
| `PR-02-06` semula ditulis pada lingkungan tanpa Docker; setelahnya Docker diaktifkan (stack dev `docker-compose.yml`: `postgres:15-alpine` + `redis:7-alpine` port `55432`/`6379`, `.env` akar) sehingga `tests/integration/auth-password-change.test.ts` (16 uji) benar-benar DAPAT dijalankan terhadap PostgreSQL/Redis nyata, bukan hanya `tsc`/`eslint`. | — (baris ini digantikan hasil eksekusi nyata di bawah). | Dijalankan berulang kali; lihat dua baris berikutnya untuk hasilnya. | Tidak |
| Berkas uji baru sendiri (`auth-password-change.test.ts`) **lulus 16/16 pada SETIAP eksekusi** — terisolasi, digabung parsial, maupun bagian dari suite penuh — termasuk setelah `docker compose down -v` (basis data benar-benar kosong, migration dijalankan ulang dari `0001`). | Tidak ada. | Tidak ada tindakan; dicatat sebagai bukti positif. | Tidak |
| `npm run test:ci -w apps/api` (skrip CI sesungguhnya, PostgreSQL+Redis nyata) pada cabang ini GAGAL pada percobaan pertama: 4 uji merah di `auth-login-refresh.test.ts`/`auth-session.test.ts` (BUKAN berkas PR ini) — gejala selalu sama: pengguna R-01 yang baru dibuat dan login kehilangan permission `setting.view` sesaat (`/settings` → `403`, atau `data.permissions["setting.view"]` hilang dari respons login). Baseline sebelum PR ini (`d972bf7`, lingkungan/perintah identik) LULUS bersih pada 4/4 percobaan; cabang ini gagal pada 5/7 percobaan awal (~70%), termasuk pada basis data yang benar-benar bersih (menyingkirkan dugaan data lokal menumpuk dari puluhan run manual). | Menandakan REGRESI, bukan flakiness acak pre-existing: sinyal 0/4 vs 5/7 terlalu konsisten untuk kebetulan, meski ukuran sampel kecil. | **AKAR MASALAH TERBUKTI** (instrumentasi sementara pada `PermissionCache.load()` + `RoleRepository.replacePermissions/bumpVersion`, menulis NDJSON tiap baca/tulis permission; instrumentasi sudah DIHAPUS kembali). Kunci cache permission adalah `sigm4:perm:{userId}:{roleVersion}` (`SDD-AUTH-04`, `SDD-03 §4.5`) — **kunci itu tidak memuat identitas ROLE**, dan `role_version` dimiliki per-role, bukan global: SELURUH tujuh role seed berangkat dari `role_version = 1`. Akibatnya `perm:13:1` berarti "pengguna 13, versi 1" tanpa menyebut role 13 itu apa. Jejak nyata dari satu jalan suite: `t=…641021` pengguna id **13 ber-role R-01** → cache MISS → 74 permission Administrator ditulis ke `sigm4:perm:13:1` (TTL 60 dtk); `t=…691310` (**50,3 detik kemudian**, TTL belum habis) pengguna id **13 ber-role R-05 (Guru)** → kunci yang dihitung **identik** → cache **HIT** → Guru menerima 74 permission Administrator (`punyaSettingView:true`, `jumlahScope:74`). Id pengguna dapat dipakai ulang karena empat berkas uji menjalankan `dbmate("down")` (`users.test.ts:226`, `migrations.test.ts:111`, `migration-runner.test.ts:55`, `db-timezone.test.ts:57`) yang men-DROP tabel `users` lalu `up` membuatnya kembali — `users_id_seq` restart dari 1. Terukur: **12 dari 86 userId** dalam satu jalan muncul dengan dua role berbeda. Gejala "R-01 kehilangan `setting.view`" dan "Guru mendapat `user.reset_password`" adalah **dua arah dari tabrakan kunci yang sama**, bukan dua masalah berbeda. | **Ya — dieskalasi, sengaja TIDAK ditutup di `PR-02-06`.** Atas arahan pemilik produk: PR ini TIDAK di-push/dibuka dahulu. **Koreksi atas catatan sebelumnya:** perubahan `Promise.all` → dua `await` berurutan pada `ProfileService.lihat()` **BUKAN perbaikan bug ini**; ia hanya menggeser timing sehingga peluang sebuah `load()` jatuh di dalam jendela TTL 60 detik berubah. Kesimpulan "pemicu terisolasi di baris itu" yang sempat dicatat adalah **positif palsu dari sampel kecil** dan dengan ini dicabut — perubahannya pun sudah **di-revert** (keputusan pemilik produk, 20 September 2026) agar `PR-02-06` kembali murni implementasi `FR-01.4`. `PR-02-06` sendiri **bukan penyebab**: ia hanya menambah satu berkas uji yang mengalokasikan lebih banyak id pengguna sehingga peluang tabrakan naik — bug-nya pre-existing pada `PermissionCache` (`PR-01-04`) ditambah berkas uji yang me-rollback migration. |
| Cleanup `afterAll` pada `auth-password-change.test.ts` semula `DELETE FROM event_outbox WHERE aggregate_id IN (daftar_id_pengguna_uji_ini)` TANPA filter `event_name`. `event_outbox.aggregate_id` adalah `bigint` telanjang dipakai bersama SELURUH jenis agregat (`SDD-07 §4.3`, migration `0006`) — baris kebetulan bernomor sama milik agregat lain (role, building, dst.) berisiko ikut terhapus bila berjalan bersamaan dengan berkas uji lain dalam satu basis data (`fileParallelism: false`). | Ditemukan saat investigasi baris di atas; secara terpisah mengurangi jumlah uji yang gagal per insiden (4 → 1-2 pada beberapa sampel), meski tidak menghilangkan akar masalah utamanya. | Diperbaiki menjadi `WHERE event_name IN ('SessionRevoked', 'PasswordChangedAfterReset') AND aggregate_id IN (...)`, menyamai pola `auth-session.test.ts`/`auth-password-reset.test.ts` yang sudah menyaring dengan `event_name`. Dipertahankan sebagai perbaikan yang benar terlepas dari hasil investigasi akar masalah utama. | Tidak — sudah diperbaiki dan benar. |
| **Kunci cache permission tidak memuat identitas role.** `SDD-03 §4.5` sudah menuliskan `batal: … atau saat role pengguna berubah`, tetapi implementasi `PR-01-04` tidak pernah memenuhi bagian role itu: kunci `perm:{user_id}:{role_version}` hanya memuat versi, sedangkan `role_version` dimiliki PER-ROLE dan ketujuh role seed sama-sama bernilai `1`. | **Produksi:** `PUT /users/{id}` (`FR-02.1`) memindahkan role pengguna tanpa menaikkan `role_version` mana pun dan tanpa membatalkan cache — pengguna yang DITURUNKAN haknya tetap memegang permission role lamanya sampai TTL 60 detik habis (nol rujukan `PermissionCache` di `m02-users/`). **Uji:** empat berkas menjalankan `dbmate down` sehingga `users_id_seq` restart dan id pengguna dipakai ulang; id yang dipakai ulang menabrak entri TTL milik pemilik sebelumnya. Terukur 12 dari 86 userId dalam satu jalan muncul dengan dua role berbeda. Jejak nyata: pengguna id 13 sebagai R-01 menulis 74 permission Administrator ke `sigm4:perm:13:1`; 50,3 detik kemudian pengguna id 13 sebagai R-05 menghitung kunci yang identik, cache HIT, dan Guru menerima 74 permission Administrator. | Kunci menjadi `perm:{user_id}:{role_id}:{role_version}`; `SDD-AUTH-04` dan `SDD-03 §4.5` diselaraskan; uji regresi baru pada `permission-cache.test.ts`. Mutation-check: dengan `role_id` dicabut, uji itu memerah (`expected true to be false` pada `user.reset_password` yang bocor). | Tidak — diperbaiki di cabang ini. |

## 8. Hasil pengukuran

Angka nyata, bukan perkiraan. Kosongkan bila belum diukur — jangan diisi tebakan.

| Yang diukur | Target | Hasil | Rujukan |
|---|---|---|---|
| — | — | — | — |

## 9. Gerbang keluar

Diisi saat phase dinyatakan selesai. Daftar lengkapnya ada di [`phase-02.md` §9 dan §12](../phases/phase-02.md).

- [ ] Seluruh 29 PR tergabung (`PR-02-01` dan `PR-02-09` dipensiunkan)
- [ ] Acceptance checklist phase terpenuhi
- [ ] Definition of Done phase terpenuhi
- [ ] Bagian 5 log ini terisi seluruhnya
- [ ] [`IMPLEMENTATION-STATUS.md`](../IMPLEMENTATION-STATUS.md) diperbarui

## 10. Yang diserahkan ke phase berikutnya

Hal yang sengaja ditinggalkan terbuka, beserta di mana ia akan ditutup.

| Yang ditinggalkan | Ditutup di | Alasan penundaan |
|---|---|---|
| `LOGIN_FAILED`, penghitung/penguncian akun, `NT-39` | `PR-02-03` | Direncanakan terpisah; kelas rate limit `login` (sumbu IP) sudah aktif. |
| Logout, logout semua perangkat, pencabutan saat nonaktif akun/ganti password | `PR-02-04` | Direncanakan terpisah. `cabutKeluarga` dan kolom `revoke_reason` sudah tersedia. |
| Jalur pembuka kunci administratif dan apakah reset/ganti password (`PR-02-05`/`PR-02-06`) menghapus `locked_until` | `PR-02-05`/`PR-02-06` | Tidak ada requirement; tanpanya kunci hanya berakhir sendiri setelah 15 menit. Perlu keputusan pemilik produk. |
| Penguncian kegagalan TOTP (`FR-01.5 A1`) dan bentuk responsnya | `PR-02-07` | Memakai kolom `locked_until` yang sama; harus mempertimbangkan `SDD-SESS-12`. |
| Konsumen `SessionRevoked` (nonaktifkan device token FCM, `MOB-SEC-05`) | `PR-02-25` | `device_tokens` belum ada (keputusan 17). |
| Konsumen `PasswordResetRequested` (`NT-37`) dan `PasswordResetIssued` (`NT-38`) | `PR-02-25` | Modul notifikasi belum ada (keputusan 22, pola `AccountLocked`). |
| Menandai permintaan `SELESAI` dan `NT-38a` saat pengguna mengganti password sementara | `PR-02-06` | Ganti password milik `PR-02-06`; siklus status sudah menyediakan `SELESAI`. |
| Pemberitahuan pemohon yang permintaannya ditolak (`FR-01.3 A2`) | Luring (PRD) | Tak ada `NT-xx`; PRD menyerahkannya ke pemberitahuan di luar sistem. |
| Sapuan terjadwal penutupan permintaan `DITERBITKAN` kedaluwarsa | `PR-02-32` (opsional) | Kedaluwarsa sudah tegak saat login dan baca (keputusan 24); sapuan hanya merapikan kolom `status`. |
| Larangan memakai ulang 3 password terakhir dan daftar password bocor berlaku pada password baru pengguna, bukan pada password sementara | `PR-02-31` | Password sementara dibangkitkan acak dan lolos kebijakan panjang/komposisi; riwayat baru ada di `PR-02-31`. |
| Kelas route `authenticated: true` dipakai endpoint "Bearer" berikutnya: `/me`, `/auth/password/change` (`PR-02-06`), `/files/*`, `/notifications/*`, `/device-tokens` | PR pemilik masing-masing | Mekanismenya sudah ada (keputusan 15); tiap PR hanya mendeklarasikan `authenticated: true` dan memasang `authenticated()`. |
| OpenAPI mendokumentasikan `POST /auth/login` dan `POST /auth/refresh` sebagai `201` padahal menjawab `200` | PR yang menyunting route itu berikutnya (`successStatus: 200`) | Di luar cakupan `PR-02-04` (keputusan 20). |
| Auto-logout web 30 menit idle (`FR-01.2 A2`) | `apps/web` (Phase 04+) | Dilakukan klien; server tidak menyimpan aktivitas. |
| Pemberitahuan pemilik akun bahwa akunnya terkunci | `PR-02-25` (`NT-39`) | Sengaja tidak ada di endpoint login (keputusan 10); `NT-39` in-app baru terbaca setelah kunci berakhir. |
| Pembersihan baris `refresh_tokens` kedaluwarsa | `TBD-SESS-B` | Retensi belum ditetapkan; indeks `expires_at` sudah ada. |
| Notifikasi Administrator atas pemakaian ulang refresh token | `PR-02-25` | Modul notifikasi belum ada (lihat Penyimpangan). |
| **[SUDAH ADA PERBAIKANNYA — menunggu tinjauan arsitek & merge]** Perbaikan kunci cache permission (akar masalah TERBUKTI, §7). Cabang `fix/PR-01-04-kunci-cache-permission` (dari `d972bf7`, belum di-push): kunci menjadi `perm:{user_id}:{role_id}:{role_version}`; `SDD-AUTH-04` dan `SDD-03 §4.5` diselaraskan; uji regresi baru di `permission-cache.test.ts` (pengguna R-01 dimuat → role dipindah ke R-05 lewat SQL seperti `PUT /users/{id}` → `load()` berikutnya wajib memakai matriks R-05). **Mutation-check:** dengan `role_id` dicabut dari kunci, uji itu memerah (`expected true to be false` pada `user.reset_password` yang bocor dari entri Administrator). **Verifikasi:** cabang fix sendiri 37 berkas/455 uji hijau; di-cherry-pick ke `PR-02-06` lalu suite penuh hijau **2/2** pada database bersih (sebelumnya gagal ~70%), dan `npm run test:ci -w apps/api` penuh hijau: 84 berkas/1.150 uji, cakupan 98,1% statements / 98,92% lines, `assert_no_skipped.mjs` lolos. Cherry-pick itu sudah DILEPAS kembali dari `PR-02-06` agar cabangnya tetap murni `FR-01.4`. | `sigm4:perm:{userId}:{roleVersion}` tidak memuat identitas role, sedangkan `role_version` per-role dan semua role seed bernilai `1`. Dua konsekuensi: (a) di lingkungan uji, id pengguna yang dipakai ulang setelah `dbmate down` menabrak entri TTL 60 detik milik pemilik id sebelumnya; (b) **di PRODUKSI**, Administrator yang mengubah **role seorang pengguna** (`PUT /users/{id}`, `FR-02.1`/`NT-40`) tidak mengubah `role_version` mana pun dan tidak membatalkan cache — pengguna yang **diturunkan** haknya tetap memegang permission role LAMA sampai 60 detik. Diverifikasi: tidak ada satu pun rujukan `PermissionCache` di `m02-users/`. | PR/investigasi tersendiri (nomor belum ditetapkan) — **wajib tinjauan arsitek** karena menyentuh `PermissionCache`/lapisan permission (`BRANCHING-STRATEGY §3.1`); kemungkinan juga menyentuh `SDD-AUTH-04`/`SDD-03 §4.5` yang menuliskan bentuk kunci itu | Sengaja dieskalasi atas arahan pemilik produk (20 September 2026). `PR-02-06` **bukan penyebab** — ia hanya menaikkan peluang tabrakan; satu-satunya perbaikan dari investigasi yang benar-benar layak dipertahankan di `PR-02-06` adalah scoping `event_name` pada cleanup uji `event_outbox`. |
| **Id pengguna dipakai ulang di lingkungan uji.** Empat berkas menjalankan `dbmate down` (`users.test.ts`, `migrations.test.ts`, `migration-runner.test.ts`, `db-timezone.test.ts`) yang men-DROP tabel `users` sehingga `users_id_seq` restart dari 1; berkas berikutnya memakai id yang sama untuk orang yang berbeda. | PR/utang teknis tersendiri (nomor belum ditetapkan) | **Bukan blocker** setelah kunci cache memuat `role_id` — id yang dipakai ulang tidak lagi mewarisi permission pemilik sebelumnya. Sengaja TIDAK diubah sekarang (keputusan pemilik produk, 20 September 2026): mengubah pola isolasi empat berkas uji adalah pekerjaan tersendiri di luar cakupan perbaikan kunci cache. Dicatat karena pola ini dapat menyembunyikan bug sejenis di kemudian hari: keadaan bersama apa pun yang berkunci id pengguna akan tertukar diam-diam. |

---

*Log ini mencatat pelaksanaan. Requirement tetap milik [PRD](../../PRD/), keputusan desain tetap milik [SDD](../../SDD/).*
