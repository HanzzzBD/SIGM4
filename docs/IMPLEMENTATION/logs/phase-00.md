# Log Phase 00 — Foundation

| | |
|---|---|
| **Phase** | [`phases/phase-00.md`](../phases/phase-00.md) |
| **Milestone PRD** | `M0` |
| **Status** | Lihat [`IMPLEMENTATION-STATUS.md`](../IMPLEMENTATION-STATUS.md) |
| **Mulai** | 6 September 2026 |
| **Selesai** | — |

Log ini mencatat **apa yang benar-benar terjadi** selama phase berjalan: keputusan yang diambil, hal yang berbeda dari rencana, dan angka hasil pengukuran. Ia bukan salinan rencana — rencananya ada di [`phases/phase-00.md`](../phases/phase-00.md).

Log tidak boleh memuat requirement, keputusan desain, maupun business rule baru. Bila selama phase muncul kebutuhan akan salah satunya, ia dinaikkan ke PRD atau SDD lebih dulu, dan log ini hanya mencatat bahwa hal itu terjadi.

---

## 1. Catatan harian

| Tanggal | Yang terjadi | PR terkait |
|---|---|---|
| 15 September 2026 | `PR-00-16` dibuka di `feature/PR-00-16-seed-rbac`. Sebelum satu baris seed ditulis ditemukan bahwa rencananya tidak dapat dijalankan: tabel `roles`/`permissions`/`role_permissions` baru dibuat `PR-01-01` dan `system_settings` baru dibuat `PR-01-10`, padahal `PR-00-16` hanya bergantung pada `PR-00-05`. Menurunkan matriks bawaan menyingkap enam titik tafsir lagi — pemilik non-role di Lampiran C, 🟡 tanpa catatan cakupan, dan pertentangan Bab 18 ↔ `SDD-03 §4.3` pada `material.view`. Seluruhnya dinaikkan ke pemilik produk dalam dua putaran (keputusan 39–43). Seed 79 permission · 7 role · 234 grant · `work_days` terbukti identik dengan dokumen terhadap PostgreSQL nyata. | — |
| 15 September 2026 | Pemilik produk menetapkan `@PM-Codexpert` (PM, akses `write`) sebagai peninjau; `#35` menambahkannya ke baris `*` `CODEOWNERS`. Sebelum tinjauan wajib dinyalakan ditemukan dua hal: baris spesifik `CODEOWNERS` mengalahkan `*`, sehingga sembilan area masih berpemilik tunggal dan akan mengunci PR `@HanzzzBD`; dan GitHub membaca `CODEOWNERS` dari cabang tujuan, sehingga `main` masih memuat versi satu-pemilik. Karena itu `@PM-Codexpert` dicantumkan pada seluruh baris di `chore/codeowners-pm-codexpert`, dan *Code Owners review* + minimal 1 approval dinyalakan di `develop` saja setelah cabang itu tergabung; `main` ditunda (`GITHUB-CI-STATE §4`). | [#35](https://github.com/HanzzzBD/SIGM4/pull/35) |
| 15 September 2026 | Audit dua butir terbuka `#34`. Zona waktu sesi basis data tidak dipaksa maupun diverifikasi — PostgreSQL pengembangan hanya kebetulan UTC — padahal batas partisi `activity_logs` di `0008` ditulis sebagai tanggal pada kolom `timestamptz` dan ditafsirkan memakai zona **sesi** migration, sementara job `ensurePartitions` memakai batas UTC eksplisit. `APP_BASE_URL` ternyata hanya dipakai payload QR (`FR-05.1`). Temuan baru: `cors` di rantai `SDD-06 §4.2` tanpa PR pemilik maupun konfigurasi origin. Tiga keputusan pemilik produk (36–38), dikerjakan di cabang yang sama. | [#34](https://github.com/HanzzzBD/SIGM4/pull/34) |
| 15 September 2026 | `PR-00-15` tergabung ke `develop`. Audit tiga butir terbuka yang tersisa: validasi env tersebar tanpa pemilik — `LOG_LEVEL` tak dikenal diam-diam jadi `info`, dan **`TZ=UTC` tidak pernah diverifikasi** padahal komentar `Dockerfile` mengklaimnya; gaya URL object storage tanpa keputusan maupun TBD; dan rujukan `BR-070`…`BR-070c` yang bergeser di tiga PR Phase 02 (judul `PR-02-03` pun menyebut `NT-40` alih-alih `NT-39`). Temuan sampingan: `409 FILE_NOT_SCANNED` di `SDD-09 §4.4` berada di luar katalog Bab 17.3. Empat keputusan pemilik produk (32–35), dikerjakan di `fix/PR-00-15-validasi-env-url-storage`. | [#33](https://github.com/HanzzzBD/SIGM4/pull/33) |
| 15 September 2026 | Audit tiga butir terbuka `PR-00-15` sebelum perbaikan. Temuan terbesar: `createApp` menjawab 404 dan 500 dengan HTML bawaan Express — **500 membocorkan pesan galat asli dan stack trace ke klien** bila `NODE_ENV` bukan `production` — tanpa `X-Request-Id`, karena `ErrorMapper`/`RequestContext` `PR-00-06` tidak pernah terpasang ke server dan tidak ada PR yang memiliki ujung rantai itu. Juga: sumbu IP login menghitung semua percobaan (risiko NAT sekolah), rujukan `SDD-SESS-06/07` salah tempel, dan klaim log bahwa CSP web tanpa pemilik keliru (`PR-08-04`). Empat keputusan pemilik produk (28–31), seluruhnya dikerjakan di cabang `PR-00-15`. | [#33](https://github.com/HanzzzBD/SIGM4/pull/33) |
| 14 September 2026 | `PR-00-15` dibuka: nama kelas rate limit diselaraskan ke `SDD-13 §4.3`, limiter *sliding window* Redis (skrip Lua atomik, waktu dari `Clock`), dan header keamanan `SDD-13 §4.2` lewat helmet dengan nonce per permintaan. Kedua acceptance terbukti: CSP tanpa `unsafe-inline`, dan kelas limit terpisah terhadap Redis nyata. Dua keputusan pemilik produk diambil sebelum kode ditulis — keputusan 25 dan 26. | — |
| 14 September 2026 | Tindak lanjut keputusan 24 (`SERVICE_NOT_READY`, kelengkapan `/health` bertahap, worker *blocking*) tergabung ke `develop`. | [#32](https://github.com/HanzzzBD/SIGM4/pull/32) |
| 14 September 2026 | `PR-00-14` tergabung ke `develop`. Pemilik produk memutuskan tiga butir terbukanya (keputusan 24): kelengkapan `/health` bertahap sampai `PR-03-20`; kode `SERVICE_NOT_READY` untuk `503` readiness; worker sebagai proses tetap **blocking** dengan target `PR-00-18`. Dikerjakan di `fix/PR-00-14-kode-galat-readiness`. | [#31](https://github.com/HanzzzBD/SIGM4/pull/31) |
| 14 September 2026 | `chore/format-kode-prettier` tergabung ke `develop` (*squash*). `PR-00-14` di-rebase ke atasnya; pohon hasil rebase identik dengan sebelum rebase. Rebase pertama sempat menabrak konflik di 5 berkas karena `develop` lokal belum di-*fetch* setelah merge; rebase dibatalkan tanpa resolusi apa pun dan diulang ke `origin/develop`. Instruksi rebase di deskripsi PR tidak menyebut `git fetch` — itulah celahnya. | [#30](https://github.com/HanzzzBD/SIGM4/pull/30) |
| 14 September 2026 | `PR-00-14` dibuka di atas `chore/format-kode-prettier`: `HealthRegistry` di `shared/observability` (penentu kesiapan dikunci per nama dependensi), probe `database`/`redis`, server Express minimal di `api/` untuk `/api/v1/health/live` dan `/ready`, serta server `node:http` worker. Acceptance dibuktikan: `llm`/`fcm` yang mati, melempar, atau menggantung tidak membuat `ready` gagal — dan tidak dipanggil sama sekali olehnya. Dua keputusan pemilik produk diambil sebelum kode ditulis — keputusan 21 dan 22. | — |
| 14 September 2026 | Seluruh berkas `.ts` diformat ulang ke gaya Prettier 3 (`tabWidth` 4) oleh formatter editor saat `PR-00-14` dikerjakan; diputuskan pemilik produk sebagai gaya yang **disengaja**. Dipisah menjadi `chore/format-kode-prettier` agar `PR-00-14` tidak memuat ±3.000 baris kosmetik. Format ulang itu memerahkan gerbang kompilasi `PR-00-04` — lihat §7. | — |
| 6 September 2026 | `PR-00-01` tergabung ke `develop`: monorepo `apps/*` dan `packages/schemas`, TypeScript, lint, serta uji batas antar-pohon dan lapisan tersedia. | [#6](https://github.com/HanzzzBD/SIGM4/pull/6) |
| 6 September 2026 | `PR-00-02` tergabung ke `develop`: batas impor antar-modul ditambahkan; uji negatif `check_import_boundaries.mjs` lulus 10 dari 10 kasus. | [#9](https://github.com/HanzzzBD/SIGM4/pull/9) |
| 6 September 2026 | `PR-00-03` tergabung ke `develop`: Dockerfile multi-stage + compose pengembangan. | [#11](https://github.com/HanzzzBD/SIGM4/pull/11) |
| 6 September 2026 | Audit keputusan stack sebelum `PR-00-04`. Empat celah ditemukan — perkakas yang dirujuk rencana PR tetapi tidak pernah dinyatakan di berkas mana pun — lalu ditutup pemilik produk pada hari yang sama: `SDD-DB-15`, `SDD-REPO-11`, `SDD-INF-12`, `SDD-FE-13`. Tidak ada requirement, business rule, maupun kriteria penerimaan yang berubah. | — |
| 6 September 2026 | Sapuan audit **kedua** atas sisa `docs/`, dijalankan setelah sapuan pertama selesai. Sembilan celah lagi dengan pola sama ditemukan dan ditutup hari itu juga: `SDD-INF-13`, `SDD-SEC-11`, `SDD-API-13`, `SDD-FE-14/15/16`, `SDD-MOB-11/12`, `SDD-FS-12`. Tiga di antaranya memblokir `PR-00-09`, `PR-00-17`, dan `PR-00-18`. | — |
| 6 September 2026 | Sapuan audit **ketiga**, atas lima butir menggantung di luar keputusan stack. Tiga ditutup: `SDD-SYS-10` (letak `SlotService`), proteksi cabang `main`/`develop` dinyalakan, dan `TS5083` diperbaiki di cabang `chore/`. Dua tetap terbuka karena bergantung pihak luar — nomor surat `SDD-AI-16` dan pemilik `CODEOWNERS`. | — |
| 8 September 2026 | `PR-00-13` tergabung ke `develop` bersama tindak lanjutnya: pola *false green* menjadi aturan, `APP_DB_*` menjadi dependency wajib CI. | [#28](https://github.com/HanzzzBD/SIGM4/pull/28) · [#29](https://github.com/HanzzzBD/SIGM4/pull/29) |
| 8 September 2026 | `PR-00-13` dibuka: `0007_app_role.sql` (akun `sigm4_app`), `0008_activity_logs.sql` (terpartisi + rantai hash), `AuditLogger`, verifikasi rantai, dan dua job pemeliharaan. Ketiga butir acceptance terbukti terhadap PostgreSQL nyata. **Dua omission katalog Bab 11.3 ditutup** — `activity_result` dan `holiday_type`; yang kedua tersingkap hanya karena uji enum diperluas membaca seluruh migration. | — |
| 8 September 2026 | `PR-00-12` tergabung ke `develop`. | [#27](https://github.com/HanzzzBD/SIGM4/pull/27) |
| 8 September 2026 | `PR-00-12` dibuka: `0006_event_outbox.sql` + `EventBus` + `OutboxDispatcher`. **Uji menangkap bug konkurensi nyata pada rancangan saya sendiri**: kunci per baris membuat dua dispatcher saling menunggu dan PostgreSQL melaporkan *deadlock*. Kuncinya dipindah ke baris KEPALA agregat (`id = min(id)` di antara baris layak). Lihat §7. | — |
| 8 September 2026 | `PR-00-11` tergabung ke `develop`. | [#26](https://github.com/HanzzzBD/SIGM4/pull/26) |
| 8 September 2026 | `PR-00-11` dibuka: `shared/cache/` (koneksi `ioredis`) + `worker/scheduler.ts` (antrean tunggal, `JobRegistry`, `wibCronToUtc`). Acceptance dibuktikan terhadap **Redis nyata dengan dua instance worker**: satu job dieksekusi tepat sekali. Kunci terdistribusi diambil dari `upsertJobScheduler` BullMQ, bukan `SET NX` buatan sendiri (`SDD-AVL-10`) — lihat keputusan 16. | — |
| 8 September 2026 | `PR-00-10` tergabung ke `develop`. | [#25](https://github.com/HanzzzBD/SIGM4/pull/25) |
| 7 September 2026 | `PR-00-10` dibuka: `0005_idempotency_keys.sql` + `runIdempotent`. Acceptance dibuktikan terhadap PostgreSQL nyata untuk ketiga jalur `ID-03`/`ID-04`/`ID-05`. Uji sendiri menangkap satu bug nyata: kunci **kedaluwarsa** tidak terbaca `SELECT` tetapi barisnya masih ada, sehingga `INSERT` polos menabrak primary key — diperbaiki dengan `ON CONFLICT DO UPDATE`. | — |
| 7 September 2026 | `PR-00-09` tergabung ke `develop`. | [#24](https://github.com/HanzzzBD/SIGM4/pull/24) |
| 7 September 2026 | `PR-00-09` dibuka: `shared/http/` (`defineRoute`, `RouteRegistry`) + generator OpenAPI di `api/`. Acceptance dijaga **dua lapis** — tipe menolaknya saat kompilasi, `validateOrThrow` menolaknya saat runtime. Mencabut lapis runtime membuat 4 uji merah; mencabut lapis tipe menggagalkan typecheck sebelum uji berjalan. | — |
| 7 September 2026 | `PR-00-08` tergabung ke `develop`. | [#23](https://github.com/HanzzzBD/SIGM4/pull/23) |
| 7 September 2026 | `PR-00-08` dibuka: `0004_work_calendar.sql` (`work_days`, `holidays`, enum `holiday_type`) + `BusinessCalendarService`. Acceptance dibuktikan terhadap PostgreSQL nyata; mencabut penanganan `holidays` membuat 6 uji merah, mencabut offset WIB membuat **14** uji merah. | — |
| 7 September 2026 | `PR-00-07` tergabung ke `develop`; `#22` (pemisahan estimasi §7) juga tergabung. | [#21](https://github.com/HanzzzBD/SIGM4/pull/21) · [#22](https://github.com/HanzzzBD/SIGM4/pull/22) |
| 7 September 2026 | `PR-00-07` dibuka: `document_counters` (0003) + `DocumentNumberService`. Acceptance dibuktikan terhadap PostgreSQL nyata — **1.000 permintaan paralel menghasilkan 1.000 nomor unik, berurutan 1–1000 tanpa lompatan, dalam 1,27 detik**. Menukar `ON CONFLICT DO UPDATE` dengan baca-lalu-tulis (pola yang `SEQ-02` larang) membuat dua uji paralel merah. | — |
| 7 September 2026 | `PR-00-06` tergabung ke `develop`. | [#20](https://github.com/HanzzzBD/SIGM4/pull/20) |
| 7 September 2026 | `PR-00-06` dibuka: `Clock` (`SDD-SYS-07`), katalog kode galat + `ErrorMapper` (`SDD-06 §4.4`), `RequestContext`, dan logger terstruktur ber-*redaction*. Aturan lint `no-restricted-syntax` menolak `new Date()` **dan** `Date.now()` di luar `shared/clock`. 88 uji berkas lulus; kedua gerbang acceptance dibuktikan dengan mencabut logikanya. | — |
| 7 September 2026 | `PR-00-04` tergabung ke `develop`. | [#14](https://github.com/HanzzzBD/SIGM4/pull/14) |
| 7 September 2026 | `PR-00-05` dibuka: dbmate ber-pembungkus advisory lock, `0001_extensions.sql` (btree_gist, pgcrypto), `0002_enums.sql` (**27** tipe enum). Diverifikasi terhadap PostgreSQL 15 nyata: 27 tipe terbentuk dengan nilai identik Bab 11.3, exclusion constraint gaya `SDD-01 §4.1` benar-benar dapat dibuat dan menolak irisan dengan `23P01`, serta siklus `down`→`up` bersih. 44 uji berkas + 9 uji integrasi lulus. | — |
| 7 September 2026 | `PR-00-05` dan kedua sapuan audit tergabung ke `develop` lewat `#18`. `#16` dan `#17` sempat berstatus *merged* tetapi tergabung ke cabang antara, bukan ke `develop`; cabang itu lalu dihapus sehingga isinya sempat yatim di remote. Dipulihkan dari salinan lokal dengan dua kali *rebase*, diverifikasi pohon berkasnya identik byte-per-byte sebelum dan sesudah. | [#18](https://github.com/HanzzzBD/SIGM4/pull/18) |
| 7 September 2026 | **Sapuan kedua audit**, atas area yang sapuan pertama sentuh tipis: kolom tabel SDD vs atribut entitas PRD, traceability, nama role, dan ID menggantung. Lima mismatch lagi ditemukan, lima keputusan pemilik produk. Terbesar: kolom waktu-dibuat punya **tiga** ejaan di 13 tabel SDD (`created_at`, `dibuat_pada`, `waktu`) sementara `SDD-05 §4.2` mewajibkan satu — dan tidak satu pun tabel memenuhi aturannya sendiri. ID menggantung: **0** dari 786 ID terdefinisi. | — |
| 7 September 2026 | **Audit konsistensi lintas dokumen** atas seluruh `docs/` (±24.000 baris, 7 kategori). Tujuh pertentangan menuntut keputusan pemilik produk dan seluruhnya ditutup hari itu; tiga lagi diperbaiki langsung karena aturan otoritatifnya sudah tegas. Temuan terbesar: katalog Lampiran C berisi **79** kode permission sementara enam tempat menulis 78 — riwayat git menunjukkan selisih itu **selalu satu sejak commit pertama**, jadi hitungannya memang tidak pernah benar. | — |
| 7 September 2026 | Verifikasi `SDD-DB-12` di `PR-00-05` menemukan dbmate 2.35.1 **tidak mengambil advisory lock**: `pg_locks` kosong pada 5 sampel berturut sementara migration `pg_sleep(6)` terbukti aktif, dan CLI-nya tidak punya opsi lock. Opt-out transaksi terbukti ada (`25001` tanpa `transaction:false`, berhasil dengannya). Dinaikkan ke pemilik produk; diputuskan mempertahankan dbmate dan memindahkan lock ke pembungkus. `SDD-DB-12` disunting. | — |
| 7 September 2026 | Persiapan `PR-00-05` menemukan dua pertentangan di dalam docs, dinaikkan ke pemilik produk sebelum satu baris migration ditulis: (1) `booking_status` ditulis `'Tentative'` di `SDD-01 §4.1` dan `PRD 26.2` padahal `SDD-DB-02` mewajibkan kode huruf besar; (2) `Bab 11.3` mendaftarkan "Kanal Notifikasi" sebagai enum sedangkan `SDD-08 §4.1` mendeklarasikan kolomnya `text`. Keduanya diputuskan ke huruf besar; docs diselaraskan lebih dulu di cabang `chore/`. | — |
| 7 September 2026 | `PR-00-04` dibuka: `shared/auth` (`AuthContext`) dan `shared/db` (koneksi Kysely+`pg`, `withTransaction`, `BaseRepository`). Vitest dipasang di `apps/api` karena inilah PR pertama yang menulis uji — `npm run test` di akar kini bermakna (`SDD-17 §4.4`). 28 uji lulus; gerbang kompilasi `ScopedRepository` dibuktikan dengan mencabut batasannya (4 `@ts-expect-error` berubah merah). | [#14](https://github.com/HanzzzBD/SIGM4/pull/14) |

## 2. Keputusan yang diambil

Keputusan teknis yang tidak berasal dari PRD maupun SDD, dan alasannya. Bila sebuah keputusan ternyata menyentuh requirement atau desain, ia **wajib** dinaikkan ke PRD/SDD, bukan diselesaikan di sini.

| # | Keputusan | Alasan | Menaikkan ke PRD/SDD? |
|:---:|---|---|:---:|
| 43 | `material.view` ber-scope `all` bagi seluruh pemegangnya; scope baris permintaan bahan dibaca dari `material.request` (`own` bagi Teknisi, Guru, Staf). | Bab 18 memberi ketiganya 🔍, bukan 🟡, pada saldo & kartu stok, sementara `SDD-03 §4.3` menyaring permintaan lewat `scopeOf('material.view')` = `own`. PRD menang; yang diperbaiki lapisan bawahnya. Pimpinan (tanpa `material.request`) tetap melihat seluruh permintaan secara baca, seperti maksud rancangan semula. | Ya — diputuskan pemilik produk (15 September 2026); `SDD-03 §4.3` disunting. |
| 42 | Pemilik bawaan yang bukan daftar role diselesaikan lewat **Bab 18**: `approval.decide`, `procurement.approve`, `approval.delegate` → Admin, Petugas, Pimpinan; `reservation.cancel_own` → pemegang `reservation.create`; "Semua (scope berbeda)" pada `loan.view`/`fine.view`/`approval.view` **tanpa** Teknisi (Bab 18 ❌). 🟡 tanpa catatan cakupan: `damage.view` Teknisi `own`, `dashboard.view` Teknisi/Guru/Staf/Siswa `own`. | Lampiran C mengikat (`PM-06`), tetapi "Sesuai approval rules", "Approver aktif", "Semua pemohon", dan "Semua" bukan role sehingga tidak dapat di-seed apa adanya. Bab 18 satu-satunya sumber PRD yang menyebut role untuk baris itu. | Ya — diputuskan pemilik produk (15 September 2026); tafsir lengkap ditulis `SDD-03 §4.8` dan dibaca langsung oleh uji pembanding. |
| 41 | Scope permission disimpan per baris `role_permissions` sebagai enum `permission_scope`, tanpa nilai bawaan. | `AuthContext.scopeOf` (`SDD-03 §4.2`) dan cache `SDD-AUTH-04` membutuhkan scope per role × permission, sedangkan entitas PRD `role_permissions` hanya `role_id, permission_id`. Lolos uji tiga syarat `SDD-AUTH-11`. | Ya — diputuskan pemilik produk (15 September 2026); `SDD-DB-16` dan `SDD-05 §4.7`; kelompok "Cakupan Data Permission" masuk Bab 11.3. |
| 40 | Seed parameter sistem pindah dari `PR-00-16` ke `PR-01-10`. | Tabel `system_settings` dan validasi rentangnya milik `PR-01-10`, dan katalog kunci beserta sebagian besar nilai bawaan `FR-20.1` (tarif denda, jam operasional, …) belum tertulis di mana pun — men-seed-nya sekarang berarti menebak. | Ya — diputuskan pemilik produk (15 September 2026); `phase-00.md` §2/§7 dan `phase-01.md` §7 disunting. |
| 39 | Skema `roles`, `permissions`, `role_permissions` ditarik dari `PR-01-01` ke `PR-00-16` (`0009_rbac.sql`, `expand`). `users`, kolom baku `roles`, dan trigger `updated_at` tetap `PR-01-01`. | Seed katalog permission Phase 00 (`SDD-DB-10`) tidak dapat berjalan tanpa tabelnya; deliverable §6 phase ini sudah menyebut "tabel inti RBAC". Kolom baku `roles` menunggu `users` karena `created_by` merujuknya. | Ya — diputuskan pemilik produk (15 September 2026); `SDD-05 §4.7`, `phase-00.md` §7/§8, `phase-01.md` §7. |
| 38 | Topologi origin web/API — dan dengan itu perlu tidaknya `cors` (`SDD-06 §4.2`) — diputuskan di `PR-00-18` bersama konfigurasi Nginx. | Kebutuhan CORS bergantung pada apakah web disajikan satu origin dengan API (reverse proxy) atau berbeda (CDN), dan `SDD-16 §4.2` belum memilih; memasangnya sekarang berarti menebak origin. | Ya — dinaikkan dan diputuskan pemilik produk (15 September 2026); acceptance `PR-00-18` dilengkapi. |
| 37 | `APP_BASE_URL` dimiliki `PR-03-01`: masuk skema `shared/config` sebagai URL https berhost bersama pembuatan payload QR, dan dipakai ulang `PR-05-23`. | Satu-satunya pemakai nyatanya payload QR `FR-05.1`: tidak ada kanal email (`NO-08`), reset password diterbitkan Administrator, dan deep link notifikasi berupa path relatif (`SDD-NTF-09`). | Ya — dinaikkan dan diputuskan pemilik produk (15 September 2026); `SDD-16 §4.7` mencatat pemakainya. |
| 36 | Zona sesi basis data **dipaksa** `TimeZone=UTC` pada setiap koneksi pool aplikasi (`createDb`) dan jalur migration (URL dbmate), lalu **diverifikasi** saat startup api dan worker; `start()` API menjadi async. | Batas partisi `0008` dan SQL tanggal lain ditafsirkan memakai zona sesi; bila penyedia terkelola bersesi WIB, partisi buatan migration bergeser 7 jam dari partisi buatan job dan job berikutnya dapat gagal *partition would overlap*. Paksaan menutup risikonya apa pun setelan penyedia; verifikasi membuktikan paksaannya bekerja. | Ya — dinaikkan dan diputuskan pemilik produk (15 September 2026); `SDD-INF-09` disunting. |
| 35 | `409 FILE_NOT_SCANNED` (`SDD-09 §4.4`) tidak disunting sekarang; keputusannya terhadap katalog tertutup Bab 17.3 menjadi acceptance `PR-03-04`. | Endpoint unduh baru lahir di Phase 03; memutuskan kontrak klien sekarang tidak diuji apa pun. | Ya — dinaikkan dan diputuskan pemilik produk (15 September 2026). |
| 34 | Rujukan `BR-070`…`BR-070c` diluruskan di Phase 02: `PR-02-03` melepas `BR-070`/`BR-070a` dan judulnya menyebut `NT-39`; `PR-02-07` memperoleh `BR-070` dan `BR-070c`; `PR-02-08` memakai `BR-070b`, bukan `BR-070c`. `BR-070a` tetap milik `PR-01-02` dan `GL-10`. | Pergeseran satu nomor membuat PR 2FA dapat selesai tanpa pernah menguji kewajiban 2FA (`BR-070`) maupun hash kode cadangan (`BR-070c`), sementara PR break-glass merujuk aturan kode cadangan. | Ya — dinaikkan dan diputuskan pemilik produk (15 September 2026). |
| 33 | URL object storage **path-style**; `S3_PUBLIC_ENDPOINT` (baru) untuk presigned URL dan `img-src`, `S3_ENDPOINT` untuk operasi server. `img-src` API kini diturunkan dari `S3_PUBLIC_ENDPOINT`. Pemilihan SDK tetap milik `PR-03-04`. | Path-style memberi satu origin stabil untuk CSP dan didukung AWS S3 maupun MinIO tanpa DNS wildcard; endpoint publik terpisah menyelesaikan host internal container (`minio:9000`) yang tak terjangkau peramban. | Ya — dinaikkan dan diputuskan pemilik produk (15 September 2026); `SDD-FS-13` ditambahkan, `SDD-16 §4.7` dan `SDD-13 §4.2` disunting. |
| 32 | Validasi konfigurasi terpusat di `shared/config` (Zod), anggota *shared kernel* baru `SDD-SYS-14`. Skema **bertahap**: hari ini `DATABASE_URL`, `DB_POOL_SIZE`, `REDIS_URL`, `LOG_LEVEL`, `TZ`, dan — khusus API — `S3_PUBLIC_ENDPOINT`; variabel lain masuk bersama PR pemakainya. Pembaca koneksi dan logger mendelegasikan ke parser skema; api dan worker menolak menyala sebelum membuka koneksi; zona waktu yang **benar-benar berlaku** di proses ikut diperiksa, bukan hanya variabel `TZ`. | Aturan yang tersebar di tiga berkas berperilaku berbeda — satu melempar, satu diam-diam jatuh ke bawaan — pola "aturan tersalin di beberapa tempat" yang sama dengan temuan `PR-00-12` di §7. Memvalidasi seluruh §4.7 sekarang memaksa dev dan staging mengisi rahasia fitur yang belum ada. | Ya — dinaikkan dan diputuskan pemilik produk (15 September 2026); `SDD-SYS-06`, `SDD-00 §4.1`, dan `SDD-16 §4.7` disunting. |
| 31 | Ujung rantai `SDD-06 §4.2` dipasang di `PR-00-15`: `requestId` (API membangkitkan `X-Request-Id` sendiri, tidak mempercayai header masuk), 404 JSON `NOT_FOUND` berkelas limit `default`, dan error handler berbasis `mapError` — galat 4xx lapisan HTTP (mis. URL rusak) menjadi `INVALID_REQUEST`, galat 5xx masuk log terstruktur, pesan asli tidak pernah ke klien. Pesan pengguna generik per kode; `details` belum dikirim. | 500 membocorkan pesan+stack di luar production (`NFR-R-10`), 404 HTML menyimpang dari Bab 17.2, dan `X-Request-Id` wajib (`SDD-OBS-03`) — tanpa satu PR pun pemiliknya. Header masuk tidak dipercaya karena klien dapat memalsukannya dan Nginx belum ditetapkan membangkitkannya. `detail` dari `mapError` berbentuk objek sementara `details` Bab 17.2 berbentuk larik; memetakannya berarti memutuskan kontrak. | Ya — dinaikkan dan diputuskan pemilik produk (15 September 2026); scope `PR-00-15` di `phase-00.md` §7 diperluas. |
| 30 | Kelas `login` hanya menghitung **percobaan gagal**: limiter memeriksa tanpa mencatat sebelum handler, lalu mencatat hanya bila respons berstatus ≥ 400 selain `429`. | Menghitung semua percobaan membuat satu jaringan sekolah ber-NAT terkunci setelah ±5 login, sementara sumbu akun `SDD-04 §4.2` sudah menghitung kegagalan saja. Pemeriksaan-lalu-pencatatan tidak atomik: kegagalan yang benar-benar serentak dapat melampaui batas beberapa buah — diterima, karena sumbu akun di PostgreSQL tetap mengunci. | Ya — dinaikkan dan diputuskan pemilik produk (15 September 2026); `NFR-S-07`, `SDD-SESS-07`, dan `SDD-13 §4.3` disunting; `PR-02-02` memasang pemakaiannya. |
| 29 | Sumbu akun kelas `login` adalah penguncian akun `PR-02-03` (`failed_login_count`/`locked_until`, `SDD-04 §4.2`). Rujukan rencana dirapikan: `PR-02-03` memperoleh `SDD-SESS-06/07`, `NFR-S-07`, `SEC-T-06`; `SDD-SESS-07` dicabut dari `PR-02-07` (2FA). | Rujukan yang salah tempel membuat `PR-02-03` dapat selesai tanpa pernah menguji dua sumbu `SDD-SESS-07`. | Ya — dinaikkan dan diputuskan pemilik produk (15 September 2026). |
| 28 | CSP halaman web memakai `'self'` + **hash sha256 build-time**; nonce per permintaan hanya untuk respons API. Acceptance ditempel ke `PR-08-04`. | Web statis (`SDD-FE-14`, `SDD-16 §4.2`) tidak dapat membawa nonce per permintaan; hash menjaga larangan `unsafe-inline` dan tetap ramah CDN. | Ya — dinaikkan dan diputuskan pemilik produk (15 September 2026); `SDD-SEC-04` dan `SDD-13` §3/§5 disunting. |
| 27 | Rincian teknis `PR-00-15` di dalam batas `SDD-13`: (a) nama `RateLimitClass` dari `PR-00-09` diganti tujuh kelas `SDD-13 §4.3`; (b) *sliding window* berupa **log** di sorted set Redis lewat satu skrip Lua atomik, tanpa pustaka tambahan, waktu dari `Clock`; permintaan yang ditolak tidak dicatat; (c) header `X-RateLimit-Limit`/`-Remaining`/`-Reset` (detik) plus `Retry-After` pada `429`; (d) `login` yang *fail closed* menjawab `429 RATE_LIMIT_EXCEEDED` tanpa `Retry-After`; (e) `trust proxy` satu hop; (f) `img-src` diturunkan dari `S3_ENDPOINT`, `connect-src 'self'`; (g) header bawaan helmet di luar §4.2 dibiarkan aktif, bawaan CSP-nya tidak (`useDefaults: false`). | (a) SDD mengikat; kodenya yang menyimpang — lihat §7. (b) Batas terbesar 100 per jendela, sehingga log per kunci kecil dan hitungannya eksak; satu skrip menghindari balapan baca-lalu-tulis antar-instance API. (c) `NFR-S-07` hanya menyebut `X-RateLimit-*`, sedangkan `PAGE-SPECIFICATION` menampilkan waktu tunggu dari header itu. (d) Saat Redis mati, waktu tunggu tidak dapat dihitung, jadi tidak dijanjikan. (e) `SDD-16 §4.2` menempatkan Nginx tepat satu hop di depan API. (f) `S3_ENDPOINT` sudah wajib di `SDD-16 §4.7`. (g) Bawaan helmet menambah pengerasan tanpa melonggarkan §4.2. | tidak |
| 26 | CSP `SDD-13 §4.2` disetel **literal** oleh middleware API dengan nonce acak per permintaan (`res.locals.cspNonce`). Cara aset web statis memperoleh nonce **tidak** diputuskan di sini — dicatat terbuka di §7. | Web dibangun Vite dan disajikan statis oleh Nginx/CDN (`SDD-16 §4.2`), sehingga nonce per permintaan tidak dapat disuntikkan ke berkasnya — padahal `SDD-13 §5` mengandaikan *build* frontend menyuntikkannya. | Ya — dinaikkan dan diputuskan pemilik produk sebelum kode ditulis (14 September 2026); pertentangan `SDD-13 §5` × `SDD-16 §4.2` ditutup keputusan 28. |
| 25 | Kelas berkunci pengguna (`default`, `export`, `qr-print`, `chat`, `upload`) **jatuh ke IP klien** bila permintaan belum terautentikasi. Kedua probe `/health` tetap berkelas `default`. | Autentikasi baru lahir di Phase 02, sementara kedua probe publik tidak punya pengguna. Membebaskan probe dari rate limit menghapus header `X-RateLimit-*` dari responsnya, padahal `NFR-S-07` menuntutnya pada seluruh respons. | Ya — dinaikkan dan diputuskan pemilik produk sebelum kode ditulis (14 September 2026); `SDD-13 §4.3` disunting. |
| 24 | Tiga keputusan pemilik produk atas butir terbuka `PR-00-14` (14 September 2026): (1) `/health` **tidak wajib** melaporkan keenam dependensi di Phase 00 — registrinya dibuat dulu, pemeriksaannya diisi bertahap sampai `PR-03-20`; (2) kode `SERVICE_NOT_READY` (503) ditambahkan ke Bab 17.3 dan dipakai body `503` readiness; (3) worker sebagai proses menjadi butir **blocking** dengan target `PR-00-18`, dan server kesehatan worker tidak dinyatakan tervalidasi runtime sebelum itu. | Butir §9 phase ini mustahil ditutup tanpa adapter yang baru lahir di Phase 02–03 (keputusan 22); body `503` menyimpang dari Bab 17.2 (keputusan 23); server kesehatan worker hanya pernah berjalan sebagai server uji pada port acak. | Ya — PRD 29.2 (`M0`), Bab 17.3, dan `SDD-15 §4.5` disunting; acceptance `PR-00-18`, `PR-02-27`, `PR-03-04`, `PR-03-05`, `PR-03-20` dilengkapi. |
| 23 | Body probe publik memakai amplop Bab 17.2 **tanpa** kode galat: `200 {success:true, data:{status:"ready"}}` dan `503 {success:false, data:{status:"not_ready"}}`. Path worker tanpa awalan `/api/v1`. Satu probe dibatasi 2 detik; probe yang melempar atau menggantung dijawab `down` dengan catatan generik, tanpa pesan galat aslinya. | Katalog Bab 17.3 tertutup dan tidak punya kode "belum siap" — `LLM_UNAVAILABLE`/`STORAGE_UNAVAILABLE` menyesatkan saat yang mati basis data. Konsumen probe (proxy pasif `SDD-INF-13`, skrip deploy, pemantau uptime) hanya membaca kode status. Pesan galat koneksi dapat memuat host dan nama akun (`SDD-16 §4.7`). Bab 17.1 mengatur API, bukan proses worker. | Ya — body `503` tanpa kode digantikan amplop galat `SERVICE_NOT_READY` (keputusan 24). |
| 22 | Pemeriksaan dependensi didaftarkan **bertahap** oleh adapter pemiliknya: `PR-00-14` hanya mendaftarkan `database` dan `redis`; `object_storage`/`av_scanner` menyusul adapter Phase 03, `fcm` `PR-02-27`, `llm` `PR-03-20`. Dependensi penentu kesiapan dikunci **per nama** di `shared/observability/health.ts` (`database`, `redis`, `object_storage`), bukan dipilih pendaftar. | Adapter keempatnya belum ada, SDK S3 belum dipilih, dan alamat ClamAV tidak ada di `SDD-16 §4.7` — memeriksanya sekarang berarti mengambil keputusan stack di dalam PR implementasi. Mengunci penentu per nama membuat acceptance tetap berlaku bagi adapter yang belum ditulis. Konsekuensinya: sampai Phase 03 `ready` belum menilai storage, dan butir §9 "`/health` melaporkan keenamnya" tidak dapat ditutup di Phase 00 — lihat §10. | Ya — dinaikkan dan diputuskan pemilik produk sebelum kode ditulis (14 September 2026). |
| 21 | Express dipasang di `api/` sebagai server **minimal**: hanya kedua probe publik yang dipasang, di bawah `/api/v1`. `/health` ringkasan (`setting.view`) dideklarasikan beserta handler-nya, tetapi **tidak** dipasang maupun didaftarkan ke registri proses sampai middleware permission `PR-02-09` ada. Worker memakai `node:http` untuk kedua probe (`SDD-SYS-08`), dipasang di `bootstrap()`. Entrypoint API kini menyala saat dijalankan sebagai proses (CMD Dockerfile). | Belum ada server HTTP maupun PR yang merakitnya, sementara `PR-00-18` membutuhkan probe yang dapat dipanggil. Memasang `/health` tanpa middleware permission membeberkan status seluruh dependensi tanpa penjaga (`PM-02`, `SDD-AUTH-01 §4.1`); mendaftarkannya tanpa memasangnya membuat OpenAPI mengiklankan endpoint yang tidak dilayani (`NFR-M-05`). | Ya — dinaikkan dan diputuskan pemilik produk sebelum kode ditulis (14 September 2026). |
| 20 | Backoff dan sandi akun aplikasi: sandi `sigm4_app` disetel **jalur migration** (`scripts/migrate.mjs`) dari `APP_DB_PASSWORD`, bukan dari berkas migration. | `ALTER ROLE … PASSWORD` menuntut hak DDL, dan jalur migration adalah satu-satunya langkah yang memilikinya (`SDD-INF-03`). Menuliskannya di berkas `.sql` berarti sandi produksi ikut ter-commit; `SEC-CFG-01` melarangnya. Bila variabelnya kosong, langkah itu dilewati dan sandi tidak disentuh. | tidak |
| 19 | Rantai hash berlingkup **global**, melintasi partisi, dan penulisannya serial di bawah advisory lock. Kanonikalisasi baris ditetapkan sampai ke pemisah. Dinaikkan ke pemilik produk lebih dulu; `SDD-05 §4.4` disunting pada commit pertama PR ini. Nama akun aplikasi `sigm4_app` dan variabel `MIGRATION_DATABASE_URL` ikut ditetapkan di `SDD-16 §4.7`. | `SDD-05 §4.4` menyebut "job harian memverifikasi rantai per partisi" tanpa menyatakan apakah rantainya satu atau satu per bulan — dan rantai per partisi buta terhadap penghapusan satu partisi utuh, yang justru paling perlu terlihat (`AL-09`). `AL-03b` juga tidak dapat ditulis sebagai `REVOKE` tanpa nama role, sementara `SEC-CFG-03` hanya menuntut pemisahannya. | Ya — keduanya sudah dinaikkan dan diputuskan; log ini hanya mencatat bahwa hal itu terjadi. |
| 18 | Backoff outbox disimpan **di memori dispatcher**, bukan di kolom. `SDD-07 §4.1` tidak punya kolom waktu-percobaan-berikutnya, dan pemilik produk baru saja memutuskan §4.1 tidak berubah. | Akibatnya terbatas dan tidak menyentuh kebenaran: proses yang restart, atau dispatcher kedua, mencoba lebih awal daripada jedanya. `SDD-EVT-07` sudah menuntut handler idempoten, jadi yang hilang hanya jeda — bukan jaminan. Menambah kolom demi jeda akan memperbesar skema untuk sesuatu yang tidak harus bertahan lintas proses. | tidak |
| 17 | Dead letter direpresentasikan sebagai **predikat** `attempts >= 5 AND processed_at IS NULL` pada `event_outbox`, bukan tabel kedua maupun kolom penanda. Dinaikkan ke pemilik produk lebih dulu; `SDD-07 §4.2` disunting pada commit pertama PR ini. | `§4.2` berbunyi "pindah ke dead-letter" sementara `§4.1` memberi skema lengkap tanpa satu pun penanda — titik yang belum ditetapkan, dan Phase 05 akan membaca bentuk apa pun yang dipilih di sini. Skema §4.1 ternyata sudah memuat seluruh yang dibutuhkan kartu 19.2: `event_name`, `occurred_at`, `last_error`. | Ya — sudah dinaikkan dan diputuskan; log ini hanya mencatat bahwa hal itu terjadi. |
| 16 | `JOB-02` menyebut *distributed lock* `SET NX PX`; diterapkan sebagai `queue.upsertJobScheduler()` BullMQ, bukan `SET NX` yang ditulis sendiri. Kuncinya bekerja dua tingkat: identitas penjadwal berkunci pada nama pekerjaan (instance kedua **memperbarui** jadwal yang sama, bukan menambah jadwal kedua), dan BullMQ menyerahkan tiap job yang lahir dari jadwal itu ke tepat satu consumer. | `SDD-AVL-10` sudah memilih BullMQ dan menyebut alasan menolak lock buatan sendiri: kasus tepi lock kedaluwarsa saat job masih berjalan. `SET NX PX` adalah **mekanisme** yang `JOB-02` sebut, bukan hasil yang ia tuntut — hasilnya "dieksekusi tepat sekali", dan itulah yang diuji terhadap Redis nyata. Lock manual di atas BullMQ justru menambah lapisan kedua yang dapat gagal sendiri. | tidak |
| 15 | Koneksi Redis ditempatkan di `shared/cache/`, ditulis sebagai `SDD-SYS-13` pada commit pertama PR ini. Daftar tertutup `SDD-SYS-06` kini 12 anggota. | Redis dipakai **tiga** hal di dua entrypoint: antrean pekerjaan (`JOB-01`, di `worker/`), rate limit (`NFR-S-07`, `PR-00-15`, di `api/`), dan cache permission ber-TTL 60 detik (`SDD-AUTH-04`, di `api/`). Menaruh koneksinya di `worker/` membuat `api/` tidak dapat memakainya — `SDD-00 §4.2` melarang impor lintas entrypoint. | Ya — dinaikkan dan diputuskan sebelum kode ditulis. |
| 14 | `ID-05` (`409 REQUEST_IN_PROGRESS`) tidak dapat tercapai oleh alur `SDD-01 §4.4`: kunci pemblokir + satu transaksi (`SDD-AVL-08`) membuat baris ber-`status_code NULL` tidak pernah terlihat sesi lain, sehingga cabang itu kode mati. Dinaikkan ke pemilik produk lebih dulu; diputuskan `pg_try_advisory_xact_lock`, dan `SDD-01 §4.4` dikoreksi pada commit pertama PR ini. | Selain membuat `ID-05` mustahil, kunci pemblokir menahan satu koneksi pool selama bisnis berjalan. Mutation-check membuktikannya bukan teori: mengembalikan kunci ke versi pemblokir membuat **lima uji timeout 60 detik**. | Ya — sudah dinaikkan dan diputuskan; log ini hanya mencatat bahwa hal itu terjadi. |
| 13 | Registri route tidak punya rumah: `SDD-06 §4.6` menaruh deklarasi route di `modules/*/routes.ts`, `SDD-00 §4.2` hanya mengizinkan `modules/*` mengimpor `shared/*`, dan daftar tertutup `SDD-SYS-06` (10 anggota sejak `SDD-SYS-11`) tidak memuatnya. Dinaikkan ke pemilik produk lebih dulu; diputuskan `shared/http/`, ditulis sebagai `SDD-SYS-12` pada commit pertama PR ini. Generator OpenAPI **tidak** ikut — ia tinggal di `api/`. | Menaruh `defineRoute` di `api/` membuat seluruh modul tidak dapat mendeklarasikan route-nya. Satu folder dipilih agar rantai middleware `SDD-06 §4.2` yang `PR-00-10`/`PR-00-15` perluas tidak menuntut suntingan daftar tertutup lagi. | Ya — sudah dinaikkan dan diputuskan; log ini hanya mencatat bahwa hal itu terjadi. |
| 12 | Tabel `holidays` diklaim **dua PR di dua phase** — `PR-00-08` (judul §7) dan `PR-01-11` (judul §7 serta scope §2) — sementara `academic_year_id` miliknya menunjuk `academic_years` yang baru lahir di `PR-01-11`. Dinaikkan ke pemilik produk lebih dulu; diputuskan `PR-00-08` membuat `work_days` + `holidays` **tanpa** FK itu, dan `PR-01-11` menambahkannya sebagai migration `expand`. | `PR-00-08` tidak dapat membuat `holidays` utuh karena tabel rujukan FK-nya belum ada, tetapi menundanya seluruhnya membuat acceptance-nya tidak dapat dibuktikan — separuhnya persis tentang hari libur. Dua PR yang membuat tabel sama juga akan bertabrakan sebagai dua `CREATE TABLE`. | Ya — sudah dinaikkan dan diputuskan; log ini hanya mencatat bahwa hal itu terjadi. |
| 11 | Kolom `Kompleksitas` pada §7 kesembilan phase dipecah menjadi **`Kode`** dan **`Uji`**. Nilai `Kode` dipertahankan apa adanya; `Uji` diturunkan aturan yang dikalibrasi terhadap empat PR Phase 00 yang sudah terukur dan mereproduksi keempatnya. Definisi kanoniknya di [`PHASE-TEMPLATE.md`](../templates/PHASE-TEMPLATE.md); `DELIVERY-PLAN.md` dan `CLAUDE.md` menunjuk ke sana. | Empat PR berturut "meleset" dari perkiraannya, padahal setiap kali kode produksinya berada di dalam batas — `PR-00-07` membuktikannya paling tajam: diperkirakan `S`, terkirim 417 baris, kode produksinya **134**. Perkiraannya benar; yang keliru adalah membaca satu angka sebagai dua hal. Dibiarkan, angka §7 akan berhenti dipercaya justru saat Phase 01 mulai memakainya. | Tidak — bentuk tabel perencanaan, bukan requirement maupun keputusan desain. Skalanya sendiri tidak berubah. |
| 10 | Katalog prefiks `SEQ-04` tidak cocok dengan entitas bernomor **di dua arah**: `material_requests` (M-22) punya kolom `nomor` tanpa prefiks, sementara `OPN` punya prefiks tanpa entitas penyimpan. Dinaikkan ke pemilik produk lebih dulu; diputuskan melengkapi keduanya — `PMB` ditambahkan dan `audit_sessions` memperoleh kolom `nomor` — lalu `PRD 26` memperoleh **tabel pemilik prefiks** agar celah semacam ini terlihat sebagai baris kosong, bukan tersembunyi di dalam regex. | Tanpa prefiks bahan, `NT-50` tidak punya nomor untuk dirender dan M-22 terhalang di Phase 05. Tanpa kolom penyimpan, nomor `OPN` tidak dapat bertahan — `SEQ-02` mewajibkannya lahir dari penghitung basis data, bukan dirakit ulang setiap PDF dibuat. | Ya — sudah dinaikkan dan diputuskan; log ini hanya mencatat bahwa hal itu terjadi. |
| 9 | `Logger` dan `RequestContext` tidak punya rumah: `SDD-SYS-06` mendaftar isi *shared kernel* sebagai daftar **tertutup** berisi delapan anggota, dan pohon `SDD-00 §4.1` tidak punya foldernya — padahal `phase-00.md §7` menugaskan keduanya ke `PR-00-06`. Dinaikkan ke pemilik produk lebih dulu; diputuskan `shared/observability/`, dan `SDD-SYS-11` ditulis sebagai commit **pertama** PR ini, sebelum satu baris kode. | Menaruhnya di `api/` mustahil — `SDD-SYS-02` melarang `shared/*` dan `modules/*` mengimpor entrypoint, sehingga seluruh kernel dan seluruh modul menjadi tidak dapat mencatat log; `SDD-OBS-03` juga menuntut `request_id` sampai ke worker. Satu folder dipilih agar metrik (`SDD-OBS-05`) dan tracing (`SDD-OBS-08`) Phase 08 tidak menuntut suntingan daftar tertutup dua kali lagi. | Ya — sudah dinaikkan dan diputuskan; log ini hanya mencatat bahwa hal itu terjadi. |
| 8 | Sapuan kedua audit tidak menghasilkan keputusan di log ini. Lima pertentangan dinaikkan lebih dulu dan diterapkan setelah dijawab: `stored_files.scan_status` (PRD kehilangan `FAILED`), `path` vs `object_key`, `notifications.kanal`/`dikirim_pada` yang sebenarnya milik `notification_deliveries`, penyeragaman `created_at`/`created_by` beserta perjelasan cakupan `§4.2`, dan tiga kolom domain berbahasa Inggris (`verified_by`/`verified_at`/`last_login_at`). | Seluruhnya kontrak data yang akan menjadi migration Phase 01–05. Aturan `§4.2` yang dilanggar oleh ke-13 contohnya sendiri tidak akan menahan apa pun; memperjelas cakupannya lebih jujur daripada membiarkannya. | Ya — seluruhnya sudah dinaikkan dan diputuskan. |
| 7 | Audit konsistensi lintas dokumen 7 September 2026 tidak menghasilkan keputusan di log ini. Tujuh pertentangan dinaikkan ke pemilik produk lebih dulu lewat pertanyaan berstruktur (konflik · sumber A · sumber B · dampak · rekomendasi · konsekuensi tiap opsi), lalu keputusannya diterapkan ke berkas pemiliknya: `SDD-DB-02` (himpunan nilai tetap = native enum huruf besar), `stocktake_result` (enum 5, kolom dibatasi 4), pemetaan kelompok notifikasi (52 dari 52), katalog kode galat (tertutup, 22 → 25), dua endpoint yang hanya hidup di diagram, jumlah permission 78 → 79, dan otorisasi `/health*`. | Ketujuhnya menyentuh kontrak data, kontrak klien, atau katalog ber-satu-pemilik — bukan bentuk kode. Memutuskannya di dalam PR implementasi akan membuat Phase 01–05 berdiri di atas pilihan yang tidak beralamat ID. | Ya — seluruhnya sudah dinaikkan dan diputuskan; log ini hanya mencatat bahwa hal itu terjadi. |
| 6 | Penyelarasan kode enum 7 September 2026 tidak diputuskan di log ini. Kedua pertentangan dinaikkan ke pemilik produk lebih dulu, lalu `PRD 26.2`, `SDD-01 §4.1`, dan `SDD-08 §4.1` disunting di cabang `chore/` **sebelum** `PR-00-05` menulis `0002_enums.sql`. Suntingan dibatasi pada titik definisi dan literal SQL; ±30 sebutan prosa di 12 berkas sengaja tidak disentuh, dan sebuah kalimat normatif ditambahkan pada `PRD 26.2` agar setiap sebutan itu terbaca sebagai nama keadaan, bukan nilai kolom. | Nilai enum adalah kontrak data: bila `0002_enums.sql` dan `SDD-01 §4.1` berselisih, `PR-02-16` akan ditulis dari salah satunya dan selisihnya baru terlihat saat runtime. Menyunting seluruh sebutan prosa akan menyentuh teks `BR-017`/`BR-023b` yang dimiliki berkas modul, sehingga melanggar aturan satu-pemilik demi keseragaman kosmetik. | Ya — sudah dinaikkan dan diputuskan pemilik produk; log ini hanya mencatat bahwa hal itu terjadi. |
| 5 | Berkas uji `apps/api` diletakkan di `apps/api/tests/`, di luar `rootDir` build (`./src`), dan di-*typecheck* proyek terpisah `tsconfig.test.json` yang dijalankan `npm run test -w apps/api` sebelum Vitest. | Uji di dalam `src/` akan ikut ter-*emit* ke `dist/` dan masuk image produksi — dilarang `CLAUDE.md` ("tidak ada data uji pada jalur produksi"). Kompilasi terpisah itu juga yang **menjadikan** acceptance `PR-00-04` sebuah gerbang: uji negatifnya berupa `@ts-expect-error`, yang hanya menolak sesuatu bila ada yang benar-benar mengkompilasinya. | Tidak — `SDD-17 §4.1` menyatakan hanya tingkat yang dimilikinya, dan letak berkas uji tidak termasuk. |
| 4 | Vitest dipasang di `apps/api` pada `PR-00-04`, bukan menunggu `PR-00-17`. | `SDD-REPO-11` sudah memilih perkakasnya; yang menjadi milik `PR-00-17` adalah *tahap pipeline*-nya, bukan pemasangan runner-nya. Acceptance `PR-00-04` dan DoD 29.5 sama-sama menuntut uji, dan PR pertama yang menulis uji adalah PR yang harus menyediakan cara menjalankannya. | Tidak — perkakasnya sudah ditetapkan `SDD-REPO-11`. |
| 3 | `AuthContext.scopeOf()` **melempar** untuk permission yang tidak dipegang, alih-alih mengembalikan scope bawaan. | Nilai bawaan di titik ini mengubah kelalaian pemanggil menjadi kebocoran diam-diam — persis kelas kesalahan yang `SDD-AUTH-02` hilangkan dengan mewajibkan `ctx`. Diuji dengan tiga syarat `SDD-AUTH-11` dan lolos ketiganya: tidak ada perilaku pengguna yang berubah, tidak ada baris baru pada empat katalog, dan ia semata cara memenuhi `PM-03`. | Tidak — mekanisme teknis, lolos uji tiga syarat `SDD-AUTH-11`. |
| 2 | Audit stack 6 September 2026 tidak menghasilkan keputusan di log ini. Keempat celah dinaikkan lebih dulu ke SDD pemiliknya (`SDD-DB-15`, `SDD-REPO-11`, `SDD-INF-12`, `SDD-FE-13`) dan dicatat di [`TBD-REGISTER.md`](../../SDD/TBD-REGISTER.md) sebagai `TBD-DB-B`, `TBD-QA-A`, `TBD-INF-C`, `TBD-FE-C` — dibuka dan ditutup pada hari yang sama. | Ketiganya menyentuh bentuk kode dan perkakas, bukan requirement, sehingga masuk kelompok C. Menuliskannya hanya di log akan membuat `PR-00-04` dan `PR-00-05` berdiri di atas keputusan yang tidak beralamat ID. | Ya — sudah dinaikkan; log ini hanya mencatat bahwa hal itu terjadi. |
| 1 | Cuplikan Dockerfile `SDD-16 §4.1` diadaptasi ke tata letak monorepo saat `PR-00-03`: seluruh manifest workspace disalin sebelum `npm ci`, tahap runtime ikut menyalin manifest dan `dist` tiap workspace yang dipakainya, dan hanya `apps/api` yang dibangun. | Cuplikan itu ditulis untuk satu paket, sebelum `SDD-17` menetapkan monorepo. `npm ci` menolak berjalan tanpa seluruh manifest yang disebut `package-lock.json`; `node_modules` datar menaruh `@sigm4/schemas` sebagai symlink, sehingga tanpa direktori tujuannya impor gagal saat proses dinyalakan. Ketiga perintah inti (`npm ci`, `npm run build`, `npm prune --omit=dev`) tetap dipakai apa adanya sesuai `SDD-REPO-03`. | Tidak — bentuknya sudah ditetapkan `SDD-REPO-03`/`SDD-REPO-09`. Yang tertinggal adalah cuplikan `SDD-16 §4.1`; penyelarasannya diusulkan sebagai suntingan SDD tersendiri, bukan bagian PR implementasi. |

## 3. Penyimpangan dari rencana

| Yang direncanakan | Yang dikerjakan | Sebab | Dampak pada phase berikutnya |
|---|---|---|---|
| `PR-00-16` — "Seed: 79 permission, 7 role, matriks, `work_days`, parameter", kode `M` · uji `L`, bergantung `PR-00-05` — [`phase-00.md` §7](../phases/phase-00.md). | Skema RBAC (`0009`) + seed RBAC (`0010`) + seed `work_days` (`0011`); **tanpa** parameter sistem. Terkirim kode **474** baris (`L`) · uji **495** baris (`L`). §7 dinyatakan ulang ke kode `L`. | Rencana men-seed tabel yang belum ada (keputusan 39–40). Kode naik satu kelas karena migration skema ikut masuk dan matriks bawaan berjumlah **234** baris data — 320 dari 366 baris `0010` adalah data. Memecahnya tidak mengurangi yang perlu ditinjau: acceptance membandingkan seed **utuh** dengan Lampiran C, sehingga separuh seed tidak dapat dibuktikan sendiri. | `PR-01-01` menyusut menjadi `users` + kolom baku `roles`; `PR-01-10` bertambah seed parameter beserta katalog kuncinya di SDD. |
| `PR-00-15` direncanakan kode `M` · uji `M` — [`phase-00.md` §7](../phases/phase-00.md). | Terkirim kode **559** baris (`L`) · uji **1.014** baris (`L`), di luar `package-lock.json`. | Rencana `M` untuk header dan rate limit terpenuhi di 371 baris. Tambahan 188 baris datang dari audit atas PR ini sendiri: ujung rantai HTTP (keputusan 31) dan mode hitung-gagal kelas `login` (keputusan 30). Saat keputusan diminta, tambahannya diperkirakan ±80 baris dengan PR tetap `M` — perkiraan itu keliru. | Tidak ada pada scope Phase 01. Pelajarannya: ujung rantai HTTP semestinya punya PR sendiri sejak `PR-00-14` merakit server; celah itu baru terlihat saat PR yang memakainya diaudit. |
| `PR-00-14` direncanakan kode `S` · uji `S` — [`phase-00.md` §7](../phases/phase-00.md). | Terkirim kode **436** baris (`L`) · uji **434** baris (`L`), di luar `package-lock.json`. | Dua sebab. (1) Scope yang tak terlihat saat estimasi: belum ada server HTTP sama sekali, sehingga PR ini ikut merakit Express di `api/` dan server `node:http` worker (keputusan 21). (2) Gaya Prettier `tabWidth` 4 sejak `chore/format-kode-prettier` membungkus baris pada 80 kolom — `health.ts` saja 192 baris. Scope fungsionalnya tidak melebar: probe storage/AV/FCM/LLM justru ditunda (keputusan 22). | Estimasi §7 Phase 01 ditulis sebelum gaya baru; isi yang sama kini menghasilkan lebih banyak baris. Belum dikalibrasi ulang — satu PR belum cukup untuk mengukur faktornya. |
| `PR-00-07` direncanakan berkompleksitas **S** (≤ 200 baris berubah) — [`phase-00.md` §7](../phases/phase-00.md). | Terkirim **417 baris** di luar `package-lock.json` — lebih dari dua kali batasnya. | **Pemisahannya kali ini tegas dan menyelesaikan perdebatan:** kode produksi **134 baris** (migration 22 · service 83 · barrel 3 · tipe tabel 26) — **muat di dalam `S`**. Uji **222 baris**, dokumentasi dan konfigurasi ±61. Estimasi `S` ternyata benar terhadap kode produksinya, dan hanya keliru karena tidak menghitung uji. | Tidak ada pada scope. **Ini PR keempat berturut yang meleset dengan sebab yang sama, dan yang pertama membuktikan estimasinya sebenarnya akurat.** Tindak lanjut yang sudah dicatat pada baris `PR-00-06` — memisahkan estimasi §7 antara kode produksi dan uji — karena itu bukan lagi usulan melainkan koreksi terhadap cara §7 dibaca: angka `S`/`M`/`L` yang ada **sudah** menggambarkan kode produksi dengan benar. |
| `PR-00-06` direncanakan berkompleksitas **M** (≤ 400 baris berubah) — [`phase-00.md` §7](../phases/phase-00.md). | Terkirim **L**: ±810 baris di luar `package-lock.json`, dengan 287 baris di antaranya uji. | **PR ketiga berturut** dengan sebab yang persis sama. Pola ini sekarang cukup jelas untuk ditindaklanjuti, bukan sekadar dicatat lagi. | Tidak ada pada scope. **Tindak lanjut: estimasi §7 Phase 01 dinyatakan terpisah antara kode produksi dan uji.** Tanpa itu, setiap PR Phase 01 akan meleset dengan cara yang sama dan angkanya berhenti bermakna. |
| `PR-00-05` direncanakan berkompleksitas **M** (≤ 400 baris berubah) — [`phase-00.md` §7](../phases/phase-00.md). | Terkirim **L**: 702 baris berubah di luar `package-lock.json`. Scope tidak melebar — runner, `0001`, `0002`, tidak lebih — tetapi 458 baris di antaranya adalah uji dan helper-nya, dan 68 baris lagi adalah pembungkus advisory lock yang tidak ada dalam rencana karena kebutuhannya baru diketahui **hari itu** dari hasil verifikasi. | Pola yang sama dengan `PR-00-04`, ditambah satu sebab baru: acceptance PR ini menuntut pembuktian terhadap PostgreSQL nyata, sehingga lapis uji integrasi beserta perkakasnya lahir di sini. | Tidak ada pada scope. Untuk perencanaan phase berikutnya: dua PR berturut meleset dari **M** ke **L** dengan sebab yang sama — estimasi §7 menghitung kode produksi dan tidak menghitung uji. Estimasi Phase 01 sebaiknya dinyatakan terpisah antara keduanya. |
| `PR-00-04` direncanakan berkompleksitas **M** (≤ 400 baris berubah) — [`phase-00.md` §7](../phases/phase-00.md). | Terkirim **L**: 546 baris berubah di luar `package-lock.json`. Scope-nya tidak melebar — keempat butir §8 persis — tetapi 245 baris di antaranya adalah uji dan ±90 baris komentar penelusuran ID yang diwajibkan `CLAUDE.md`. | Estimasi M dibuat sebelum `SDD-DB-15` dan `SDD-REPO-11` ditetapkan (6 September 2026), sehingga tidak memperhitungkan pemasangan Vitest maupun empat berkas uji. | Tidak ada; scope `PR-00-05` tidak berubah. Pelajarannya untuk perencanaan phase berikutnya: PR pertama yang memperkenalkan sebuah perkakas menanggung biaya pemasangannya, dan estimasi §7 belum mencerminkan itu. |
| `develop` direncanakan berdiri bersama `staging` pada `PR-00-18`. | `develop` dibuat manual dari `main` pada 6 September 2026 saat pemulihan cabang; jalur kerja sejak itu `feature/*` → PR → `develop`. `staging` tetap scope `PR-00-18`. | Cabang `main` lokal sempat menyimpang dari `origin/main`; pemulihan memerlukan cabang integrasi lebih dahulu. | Tidak mengubah scope Phase 00; `BRANCHING-STRATEGY.md` dan `GITHUB-CI-STATE.md` disegarkan agar keadaan aktual tidak disamarkan. |

## 4. PR di luar rencana

PR yang tidak ada pada daftar [`phase-00.md` §7](../phases/phase-00.md).

| ID | Judul | Mengapa tidak terencana |
|---|---|---|
| `chore/format-kode-prettier` | Format ulang seluruh berkas `.ts` ke Prettier 3 (`tabWidth` 4) + `.prettierrc.json` | Bukan PR bernomor. Format ulang lahir di pohon kerja selagi `PR-00-14` dikerjakan, lalu diputuskan pemilik produk sebagai gaya yang disengaja (14 September 2026). Memasukkannya ke `PR-00-14` akan menyembunyikan perubahan perilaku di antara ±3.000 baris kosmetik (`CLAUDE.md` — "sekalian rapikan"); karena itu ia berdiri sendiri dan `PR-00-14` ditumpuk di atasnya. `.prettierrc.json` ikut agar gayanya dapat direproduksi tanpa pengaturan editor pribadi. |
| `chore/tsconfig-akar-typecheck` | `tsconfig.json` akar agar `npm run typecheck` hijau | Bukan PR bernomor. `TS5083` sudah ada sejak `PR-00-01` dan tercatat di §7, tetapi berada di luar scope `PR-00-02` maupun `PR-00-03`; memasukkannya ke salah satunya akan mencampur perbaikan perkakas dengan pekerjaan yang sedang ditinjau (`CLAUDE.md` — "sekalian rapikan"). `PR-00-17` menuntut perintah ini hijau, jadi ia tidak dapat menunggu lebih lama. |
| `chore/codeowners-pm-codexpert` | `@PM-Codexpert` pada seluruh baris `CODEOWNERS` + keadaan tinjauan wajib di `GITHUB-CI-STATE §4` | Bukan PR bernomor. Tata kelola repositori, bukan pekerjaan fitur: pemilik produk menetapkan peninjau kedua (15 September 2026), dan tanpa pemilik kedua pada setiap baris, tinjauan wajib `BRANCHING §3.1` tidak dapat dinyalakan tanpa mengunci repositori. |

Kolom ketiga adalah yang paling berharga di seluruh log ini. Pola yang berulang di sana menunjukkan di mana perencanaan phase berikutnya perlu diperbaiki.

## 5. Butir yang wajib tercatat pada phase ini

- [x] Runner migration **dbmate** (`SDD-DB-12`) dibuktikan di `PR-00-05`, dan pembuktiannya **gagal separuh**: opt-out transaksi per-migration terpenuhi, *advisory lock* **tidak ada sama sekali** pada dbmate 2.35.1. Temuan dinaikkan ke pemilik produk; `SDD-DB-12` disunting sehingga advisory lock menjadi syarat **jalur** migration dan dipegang pembungkus `scripts/migrate.mjs`. Butir inilah yang menangkapnya — nama yang dikunci memang tidak menggantikan pembuktian
- [x] Tipe tabel Kysely (`SDD-DB-15`) disegarkan pada PR yang sama dengan migration yang mengubah bentuk tabel — **berlaku pertama kali di `PR-00-07`**: `0003` membuat `document_counters`, dan `Database` pada `shared/db/schema.ts` ikut terisi pada PR yang sama. Butir ini tetap berlaku bagi setiap migration berikutnya
- [ ] Perpanjangan sertifikat certbot (`SDD-INF-13`, `INF-06`) terpantau alarm — kegagalannya tidak boleh baru diketahui saat sertifikat kedaluwarsa
- [ ] *Readiness gate* `SDD-INF-04` dibuktikan di staging: Nginx OSS hanya memeriksa upstream secara **pasif**, sehingga gerbangnya sepenuhnya bersandar pada skrip deploy `SDD-INF-10`
- [ ] Ukuran image setelah Chromium masuk (`SDD-FS-12`) diukur dan dicatat di §8 — bila menjadi persoalan, jalannya memisahkan image (`SDD-INF-01`), bukan mengganti pembangkit PDF
- [ ] `CODEOWNERS` diganti tim arsitek, lalu *required approvals* dan *Code Owners review* dinyalakan bersamaan (`GITHUB-CI-STATE §4`) — keduanya sengaja mati selama pemiliknya satu orang
- [ ] Nomor surat persetujuan lintas yurisdiksi (`SDD-AI-16`) dilengkapi sebelum `GL-07` diperiksa — pelacakannya sudah ada di `phase-08.md` §8/§9 dan `RELEASE-PLAN.md` §2
- [ ] Pembangkitan OpenAPI dari skema Zod berjalan (`SDD-API-11`, `SDD-API-02`) dan `/api/docs` tidak aktif di produksi (`SDD-API-12`)
- [ ] Perkakas observability terpasang sesuai `SDD-OBS-09`, dan penyedianya **ber-region Indonesia** (`SDD-OBS-10`, `SDD-SEC-10`) — region diverifikasi sebelum kontrak, bukan sesudahnya
- [ ] Koreografi deploy `SDD-INF-10` teruji di staging — penggantian instance API satu per satu dan drain worker
- [ ] Penyediaan mengikuti `SDD-INF-11` — VPS ber-region Indonesia + PostgreSQL terkelola; ketersediaan `btree_gist` diverifikasi sebelum langganan dibuka (§10 `phases/phase-00.md`)
- [ ] `PR-00-17` menyediakan `APP_DATABASE_URL` **dan** `APP_DB_PASSWORD` di CI — keduanya dependency **wajib** acceptance `AL-03b` (keputusan pemilik produk, 8 September 2026). Tanpa keduanya suite wajib **FAILED**, bukan *skipped* dan bukan hijau; penjaganya sudah ditulis sebagai uji tersendiri di `PR-00-13`, jadi butir ini memeriksa lingkungannya, bukan kodenya
- [ ] **Blocking** — worker berjalan sebagai proses dan `/health/live`/`/health/ready`-nya tervalidasi di runtime (`SDD-SYS-08`), target `PR-00-18` (keputusan 24). Sampai butir ini tertutup, server kesehatan worker **tidak** dinyatakan tervalidasi: yang ada hanya uji `createHealthServer` pada port acak dari `PR-00-14`

## 6. TBD yang tertutup

| TBD | Keputusan | Diputuskan oleh | Tanggal |
|---|---|---|---|
| `TBD-DB-B` | `SDD-DB-15` — akses data memakai **Kysely + `pg`**; skema tetap milik berkas `.sql` (`SDD-DB-08`) | Pemilik produk | 6 September 2026 |
| `TBD-QA-A` | `SDD-REPO-11` — **Vitest** (unit & integration) · **Playwright** (E2E Web) · **Maestro** (E2E Mobile) | Pemilik produk | 6 September 2026 |
| `TBD-INF-C` | `SDD-INF-12` — penyedia CI/CD adalah **GitHub Actions** | Pemilik produk | 6 September 2026 |
| `TBD-FE-C` | `SDD-FE-13` — **Radix UI** bawaan; **React Aria** hanya untuk tanggal/kalender dan number field | Pemilik produk | 6 September 2026 |
| `TBD-INF-D` | `SDD-INF-13` — *reverse proxy* **Nginx**; certbot sebagai komponen tersendiri | Pemilik produk | 6 September 2026 |
| `TBD-SEC-C` | `SDD-SEC-11` — **CodeQL · Dependabot · Trivy · OWASP ZAP** | Pemilik produk | 6 September 2026 |
| `TBD-API-C` | `SDD-API-13` — **`zod-openapi`** lewat registri route `PR-00-09` | Pemilik produk | 6 September 2026 |
| `TBD-FE-D` | `SDD-FE-14` — **Vite**, satu konfigurasi dengan Vitest | Pemilik produk | 6 September 2026 |
| `TBD-FE-E` | `SDD-FE-15` — **TanStack Router** | Pemilik produk | 6 September 2026 |
| `TBD-FE-F` | `SDD-FE-16` — **axios**, identik di web dan mobile | Pemilik produk | 6 September 2026 |
| `TBD-MOB-C` | `SDD-MOB-11` — **expo-sqlite** untuk antrean unggah | Pemilik produk | 6 September 2026 |
| `TBD-MOB-D` | `SDD-MOB-12` — **expo-router** | Pemilik produk | 6 September 2026 |
| `TBD-FS-C` | `SDD-FS-12` — **Playwright (Chromium)** HTML → PDF | Pemilik produk | 6 September 2026 |
| `TBD-SYS-A` | `SDD-SYS-10` — `SlotService` di *shared kernel* `shared/booking/`; `SDD-SYS-06` disunting | Pemilik produk | 6 September 2026 |

Ketiga belasnya dibuka dan ditutup pada hari yang sama, dalam dua sapuan. Yang dicatat bukan penundaannya melainkan **temuannya**: `SDD-DB-12` menyebut "kelas dbmate/Postgrator" tanpa nama, `PRD 30` menetapkan ambang cakupan tanpa alat pengukur, `SDD-17 §4.1` menetapkan letak workflow tanpa penyedianya, dan `SDD-FE-12` menulis "Radix UI / React Aria" dengan garis miring. Empat kali pola yang sama — kelas keputusan ditulis, anggotanya tidak — dan keempatnya baru terlihat saat PR yang memakainya hendak dikerjakan. Sapuan kedua atas sisa `docs/` menemukan sembilan lagi dengan pola identik; satu di antaranya (`INF-06` "Nginx/Caddy") bahkan sudah berselisih dengan dua berkas yang menulis "Nginx" dalam prosa. **Temuan yang sebenarnya bukan ketiga belas perkakasnya, melainkan bahwa pola ini tidak terdeteksi sampai ada yang menyisirnya** — tidak ada validator yang menangkap kelas keputusan tanpa anggota.

Setiap TBD yang tertutup wajib juga diperbarui di [`../../SDD/TBD-REGISTER.md`](../../SDD/TBD-REGISTER.md). Menutupnya hanya di sini membuat register menjadi salah.

## 7. Masalah yang ditemukan

| Masalah | Dampak | Penyelesaian | Terbuka? |
|---|---|---|:---:|
| `npm run typecheck` di akar gagal: `TS5083`, `tsconfig.json` akar tidak ada. | Validasi typecheck akar belum dapat dijalankan; lint dan build per-workspace tetap hijau. | **Selesai 6 September 2026** lewat cabang `chore/tsconfig-akar-typecheck`: berkas solution akar (`files: []` + `references` ke empat proyek) ditambahkan. Tidak ada compiler option baru — `composite: true` sudah ada di `tsconfig.base.json`. `typecheck`, `lint`, `build`, dan `check:boundaries` hijau. | tidak |
| `SDD-01 §4.6` mendaftar **4** pekerjaan terjadwal dan menjatuhkan `approval-sla-check`, padahal `PRD 26.7` mewajibkannya (tiap 30 menit) dan `SDD-02 §4.5` justru menunjuk `SDD-01` sebagai tempat jadwalnya. | Phase 02 akan mendaftarkan 4 job ke `JobRegistry`, bukan 5; SLA persetujuan (`FR-10.2 A2`) tidak pernah berjalan, dan tidak ada yang merah karena job yang tak terdaftar tidak punya uji. | **Selesai 8 September 2026** pada `PR-00-11`: baris `approval-sla-check` ditambahkan ke tabel `SDD-01 §4.6`. Tidak dinaikkan sebagai pertanyaan — aturan otoritatifnya sudah tegas di PRD, dan yang diperbaiki adalah lapisan turunannya. | tidak |
| Dua dispatcher outbox berjalan bersamaan mengalami **deadlock**, bukan sekadar urutan terbalik. Kueri pemungut mengunci baris pending tertua yang belum terkunci; untuk agregat beranak banyak, dispatcher kedua mengunci baris **lain dari agregat yang sama**, lalu keduanya meminta seluruh rantai agregat itu dan saling menunggu. | `SDD-EVT-09` gagal justru pada keadaan yang `SKIP LOCKED` disiapkan untuknya. Di produksi ia muncul sebagai galat `deadlock detected` yang hilang-timbul, hanya saat satu agregat menerbitkan lebih dari satu event. | **Selesai 8 September 2026** pada `PR-00-12`, ditemukan oleh uji acceptance-nya sendiri sebelum PR dibuka. Kunci dipindah ke baris **kepala** agregat — `id = min(id)` di antara baris yang layak — sehingga satu agregat hanya punya satu baris yang dapat dikunci dan dispatcher kedua berpindah ke agregat lain. `SDD-07 §4.2` ikut mencatat granularitasnya. | tidak |
| Saringan `attempts < MAX_ATTEMPTS` tersalin ke **tiga** tempat pada kueri yang sama, dan dua mutation-check pertama saya lolos karena salinan ketiga masih menegakkannya. | Bukan cacat perilaku, melainkan cacat **pembuktian**: aturan yang punya tiga penegak dapat kehilangan dua tanpa satu pun uji memerah. | **Selesai 8 September 2026**: salinan yang benar-benar redundan dicabut, dan mutation-check diulang atas dua sisanya — kali ini memerah. Alasannya ditulis sebagai komentar di tempatnya. | tidak |
| `SDD-05 §4.4` memakai enum `activity_result` yang tidak pernah masuk katalog Bab 11.3, sehingga `0002_enums.sql` tidak membuatnya. Sapuan yang diperluas menyingkap omission **kedua** dengan bentuk sama: `holiday_type` dari `0004` (`PR-00-08`). | Tabel `activity_logs` tidak dapat dibuat sama sekali tanpa tipe itu. Yang kedua lolos berbulan-bulan karena uji pembanding enum hanya membaca `0002_enums.sql`, bukan seluruh migration. | **Selesai 8 September 2026** pada `PR-00-13`: keduanya ditambahkan ke Bab 11.3 ("Hasil Aktivitas", "Jenis Hari Libur"), `activity_result` dibuat di `0008`, dan uji pembanding diperluas membaca **seluruh** berkas migration. Nilainya tidak diputuskan sendiri — `AL-07` sudah menetapkan sukses/gagal dan `SDD-DB-02` bentuk hurufnya. | tidak |
| **Temuan proses pengujian — *false green*, sejak `PR-00-12`.** Tiga kali berturut sebuah uji tampak menjaga aturannya padahal tidak pernah mengeksekusi kondisi yang diklaimnya: (1) `PR-00-12` — saringan `attempts < MAX_ATTEMPTS` tersalin ke tiga tempat, sehingga mencabut satu lalu dua tetap hijau; (2) `PR-00-13` `AL-08` — kueri lanjutan dijalankan lewat **koneksi lain**, tidak pernah menyentuh transaksi yang batal; (3) `PR-00-13` `AL-03b` — suite **ter-skip diam-diam**, dan `beforeAll` yang melempar pun ditandai Vitest *skipped*, bukan *failed*. | Ini bukan tiga kebetulan melainkan satu pola: uji hijau yang tidak menyentuh jalur yang diklaimnya. Yang paling berbahaya adalah bentuk ketiga — di CI ia lolos tanpa membuktikan apa pun, dan tidak ada yang merah untuk menandainya. Dua yang pertama hanya ketahuan karena mutation-check dijalankan; tanpa disiplin itu ketiganya akan tergabung sebagai jaminan palsu. | **Ditutup 8 September 2026 sebagai aturan, bukan sebagai tiga perbaikan.** Ketiga ujinya diperbaiki dan diverifikasi memerah saat logikanya dicabut. Sebagai aturan tetap (keputusan pemilik produk): **uji acceptance dan uji keamanan wajib benar-benar mengeksekusi kondisi yang diklaimnya uji** — bila prasyarat lingkungannya tidak tersedia, suite **FAILED**, bukan *skipped* dan bukan hijau. Ditulis ke checklist [`templates/PULL-REQUEST.md`](../templates/PULL-REQUEST.md). | tidak |
| Format ulang Prettier **memerahkan gerbang kompilasi `PR-00-04`**: dua `@ts-expect-error` di `tests/shared/db/repository.test.ts` terpisah dari baris yang galat karena pemanggilannya dibungkus ulang, sehingga `tsc -p tsconfig.test.json` gagal `TS2578`. | Perubahan yang tampak kosmetik mengubah apa yang diuji. Ia tertangkap hanya karena `npm run test -w apps/api` menjalankan `tsc` sebelum Vitest (keputusan 5); tanpa langkah itu keempat uji negatif `SDD-AUTH-02` hilang tanpa satu pun uji merah. | **Selesai 14 September 2026** pada `chore/format-kode-prettier`: kedua direktif dipindah ke baris yang galat, dengan komentar alasannya di tempat. | tidak |
| Probe `database` menjawab `down` terhadap PostgreSQL compose yang sehat. Penyebabnya lingkungan: layanan PostgreSQL bawaan Windows ikut mendengarkan `0.0.0.0:5432`, sehingga `localhost:5432` mendarat di server itu dan autentikasi gagal `28P01`. | Uji integrasi yang benar tampak seperti bug probe; catatan `pemeriksaan gagal` — sengaja tanpa pesan asli (keputusan 23) — tidak menunjuk penyebabnya. | Diselesaikan untuk pengukuran PR ini dengan `POSTGRES_PORT=55432`, variabel yang sudah disediakan `docker-compose.yml`. dbmate lewat port itu juga menuntut `?sslmode=disable`. | tidak |
| Bab 17.3 tidak punya kode galat bagi `503` readiness, padahal Bab 17.2 mewajibkan amplop galat ber-`code`. | Body `503` probe memakai amplop tanpa `code` (keputusan 23) — satu-satunya respons sistem yang menyimpang dari 17.2. | **Selesai 14 September 2026** (keputusan 24): `SERVICE_NOT_READY` ditambahkan ke Bab 17.3 dan `KODE_GALAT`; `503` readiness kini amplop galat 17.2 lengkap dengan `request_id`, dan tipe kodenya terikat ke katalog. | tidak |
| Tipe `RateLimitClass` dari `PR-00-09` memakai lima nama (`auth`, `write`, `read`, `export`, `chat`) yang sebagian tidak ada di tabel `SDD-13 §4.3`, dan tidak memuat `login`, `public-asset`, `qr-print`, `upload`. | Setiap route sejak `PR-00-09` — termasuk probe `PR-00-14` — membawa kelas tanpa batas. Tidak ada yang merah karena belum ada limiter yang membaca kelas itu. | **Selesai 14 September 2026** pada `PR-00-15`: tipe diganti tujuh kelas §4.3, dan uji baru membandingkan `KELAS_LIMIT` dengan tabel SDD-nya sehingga penyimpangan serupa langsung merah. | tidak |
| `SDD-13 §5` mengandaikan *build* frontend menyuntikkan nonce CSP, sedangkan `SDD-16 §4.2` menyajikan web sebagai aset statis dari CDN/Nginx — berkas statis tidak dapat membawa nonce per permintaan. | Tanpa jalan keluar, CSP ber-nonce hanya melindungi respons API (JSON); halaman web akan butuh `unsafe-inline` atau tanpa CSP — persis yang `SDD-SEC-04` larang. | **Selesai 15 September 2026** (keputusan 28): web memakai hash sha256 build-time, API tetap nonce; acceptance di `PR-08-04`. Klaim sebelumnya bahwa butir ini tanpa pemilik keliru — `PR-08-04` sudah merujuk `SDD-SEC-04`. | tidak |
| `createApp` menjawab 404 dan 500 dengan HTML bawaan Express. **500 membocorkan pesan galat asli beserta stack trace ke klien** bila `NODE_ENV` bukan `production` (terbukti dengan string koneksi palsu); dengan `production` klien aman, tetapi stack tercetak ke stderr sebagai teks bebas. `X-Request-Id` tidak pernah dikirim. | Melanggar `NFR-R-10`, `SDD-06 §4.4`, `SDD-OBS-02/03`, dan Bab 17.2. Proses yang dijalankan tanpa image (mis. `node dist/…`) membocorkan detail infrastruktur. Akarnya perencanaan: `PR-00-06` membangun `ErrorMapper` sebelum ada server, `PR-00-14` merakit server tanpa rantai, dan tidak ada PR yang memiliki ujungnya. | **Selesai 15 September 2026** pada `PR-00-15` (keputusan 31), dengan uji kebocoran yang berjalan di mode non-production. | tidak |
| Sumbu IP kelas `login` di `PR-00-15` menghitung **semua** percobaan, 5 per 15 menit per IP. | Satu jaringan sekolah di balik satu IP publik dapat terkunci setelah ±5 login — termasuk yang berhasil. | **Selesai 15 September 2026** (keputusan 30): hanya percobaan gagal yang dihitung; `NFR-S-07` diperjelas. | tidak |
| `PR-02-03` (penguncian akun) tidak merujuk `SDD-SESS-06/07` maupun `NFR-S-07`, sementara `SDD-SESS-07` menempel di `PR-02-07` (2FA). | Dua sumbu rate limit login dapat terlewat tanpa satu pun acceptance yang memerah. | **Selesai 15 September 2026** (keputusan 29). `BR-070`/`BR-070a` yang tersisa ternyata bagian dari pergeseran di tiga PR, diluruskan keputusan 34. | tidak |
| Validasi skema env terpusat (`SDD-INF-08`) tidak dimiliki PR mana pun; tiap berkas membaca variabelnya sendiri (`DATABASE_URL`, `REDIS_URL`, `S3_ENDPOINT`), sementara `JWT_*`, `GEMINI_API_KEY`, dan `FCM_CREDENTIALS` belum divalidasi di mana pun. | Konfigurasi tidak lengkap baru ketahuan saat fitur pertama memakainya, bukan saat startup. | **Selesai 15 September 2026** (keputusan 32): skema terpusat bertahap `shared/config`; variabel lain menjadi acceptance PR pemakainya. | tidak |
| `img-src` API diturunkan dari origin `S3_ENDPOINT`, padahal gaya URL object storage (path-style vs virtual-hosted) belum ditetapkan dan SDK S3 belum dipilih. | Dengan virtual-hosted, URL gambar ber-host `{bucket}.{endpoint}` tidak cocok dengan `img-src` dan gambar diblokir peramban. Di pengembangan, `S3_ENDPOINT` (`http://minio:9000`) bukan host yang dapat dijangkau peramban. | **Selesai 15 September 2026** (keputusan 33): path-style + `S3_PUBLIC_ENDPOINT`; `img-src` API kini diturunkan darinya. | tidak |
| Komentar `Dockerfile` menyatakan `TZ=UTC` "diverifikasi lagi saat startup" (`SDD-INF-09`), padahal tidak ada kode yang memverifikasinya. | Klaim di berkas deploy membuat peninjau menganggap kontrol yang tidak ada sudah berjalan. | **Selesai 15 September 2026** (keputusan 32): api dan worker kini memeriksa `TZ` dan zona proses saat startup. | tidak |
| Sisi basis data `SDD-INF-09` — zona waktu PostgreSQL terkelola — belum diverifikasi saat startup. | Kolom `timestamptz` menjaga kebenaran penyimpanan, tetapi fungsi tanggal di SQL (mis. `date_trunc`) mengikuti zona sesi. | **Selesai 15 September 2026** (keputusan 36): sesi dipaksa `TimeZone=UTC` pada pool aplikasi dan jalur migration, lalu diverifikasi saat startup; dibuktikan terhadap basis data yang sengaja diberi zona bawaan Asia/Jakarta. | tidak |
| `SDD-09 §4.4` memakai `409 FILE_NOT_SCANNED`, kode di luar katalog tertutup Bab 17.3. | `PR-03-04` akan menulis kode yang ditolak penjaga katalog, atau diam-diam menambah kontrak klien. | Dicatat (keputusan 35); diputuskan di `PR-03-04`. | **ya** |
| `APP_BASE_URL` wajib di `SDD-16 §4.7` tetapi tidak dirujuk PR mana pun. | Skema bertahap tidak punya PR yang memasukkannya. | **Selesai 15 September 2026** (keputusan 37): pemakainya payload QR `FR-05.1`; masuk skema bersama `PR-03-01`, dipakai ulang `PR-05-23`. | tidak |
| `cors` ada di rantai `SDD-06 §4.2` tanpa PR pemilik maupun konfigurasi origin, sementara `SDD-16 §4.2` belum memilih antara web satu origin dengan API (reverse proxy) atau berbeda (CDN). | Bila web disajikan dari CDN, peramban memblokir setiap panggilan API dan kegagalannya baru terlihat saat web pertama kali dirakit. | Dicatat (keputusan 38); diputuskan di `PR-00-18`. | **ya** |
| `PR-00-16` men-seed `roles`/`permissions`/`role_permissions` dan `system_settings`, padahal keempat tabel baru dibuat Phase 01 (`PR-01-01`, `PR-01-10`); dependensinya hanya `PR-00-05`. | Migration seed gagal `42P01` sejak baris pertama; acceptance Phase 00 "Seed permission identik dengan Lampiran C" mustahil dipenuhi di phase ini. Tidak ada validator yang menangkap PR yang menulis ke tabel milik PR sesudahnya. | **Selesai 15 September 2026** (keputusan 39–40). | tidak |
| `SDD-03 §4.3` menyaring permintaan bahan lewat `scopeOf('material.view')` = `own` bagi Teknisi/Guru/Staf, sedangkan Bab 18 memberi mereka 🔍 (`all`) pada `material.view`. | Seed yang mengikuti PRD membuat contoh SDD membocorkan seluruh permintaan bahan kepada pemohon (`BR-074`); seed yang mengikuti SDD menyimpang dari PRD. | **Selesai 15 September 2026** (keputusan 43). | tidak |

## 8. Hasil pengukuran

Angka nyata, bukan perkiraan. Kosongkan bila belum diukur — jangan diisi tebakan.

| Yang diukur | Target | Hasil | Rujukan |
|---|---|---|---|
| Tipe enum terbentuk di PostgreSQL 15 | Seluruh kelompok Bab 11.3 | **27 dari 27**, nilai & urutan identik | acceptance `PR-00-05` |
| `btree_gist` dapat dipakai exclusion constraint | Irisan ditolak | Ditolak `23P01`; resource berbeda tetap lolos | `CI-01`, `SDD-AVL-01` |
| Advisory lock dbmate polos | Ada | **Tidak ada** — `pg_locks` kosong pada 5 sampel selama `pg_sleep(6)` aktif | `SDD-DB-12` |
| Advisory lock lewat `scripts/migrate.mjs` | Ada | Ada; terlepas setelah migration selesai | `SDD-DB-12`, `SDD-INF-03` |
| Opt-out transaksi per-migration dbmate | Ada | Ada — `25001` tanpa `transaction:false`, berhasil dengannya | `SDD-DB-12` |
| Kode permission Lampiran C | — | **79** (bukan 78; selisih satu sejak commit pertama) | `PR-00-16` |
| Kode `NT-xx` terpetakan ke kelompok preferensi | 52 dari 52 | 48 → **52** setelah audit | `SDD-NTF-06`, `FR-17.3` |
| Endpoint dirujuk alur tetapi tak berkatalog | 0 | 2 → **0** setelah audit | `PM-01` |
| Kolom berhimpunan tetap bertipe `text` | 0 | 6 → **0** setelah audit | `SDD-DB-02` |
| Nomor dokumen unik pada 1.000 permintaan paralel | 1.000 | **1.000 dari 1.000**, urut 1–1000 tanpa lompatan, 1,27 detik | acceptance `PR-00-07`, `SEQ-02` |
| Cakupan gabungan uji berkas + integrasi | ≥ 70% (`CD-02`) | `PR-00-07`: **93,43%** pernyataan · `PR-00-08`: **95,37%** pernyataan · 92,54% cabang · 96,72% fungsi | · `PR-00-10`: **96,41%** · `PR-00-11`: **95,57%** · `PR-00-12`: **95,70%** · `PR-00-13`: **96,16%** pernyataan (451/469), 89,69% cabang, 96,00% fungsi · `PR-00-14`: **98,47%** pernyataan, 91,77% cabang, 97,98% fungsi (28 berkas, 332 uji) · `PR-00-15`: **98,33%** pernyataan, 90,98% cabang, 98,25% fungsi (34 berkas, 377 uji) | `NFR-M-03` |
| Jam kerja melewati akhir pekan & hari libur | benar | Terbukti: akhir pekan, satu hari libur, dan libur beruntun (Minggu + 2 hari cuti bersama) | acceptance `PR-00-08`, `CAL-01` |
| Route tanpa permission menggagalkan bootstrap | gagal | Terbukti dua lapis: tipe menolak saat kompilasi, `validateOrThrow` menolak saat runtime | acceptance `PR-00-09`, `PM-01` |
| Kunci sama+body sama → tersimpan; body beda → 409 | sesuai `ID-03`/`ID-04` | Terbukti; ditambah `ID-05` yang dijawab **seketika** (< 1 detik) alih-alih menunggu | acceptance `PR-00-10` |
| Baris **kode produksi** vs **uji**, `PR-00-04` | — | 301 / **245** (kode `M`, uji `M`) | kalibrasi §7 |
| idem, `PR-00-05` | — | 244 / **458** (kode `M`, uji `L`) | idem |
| idem, `PR-00-06` | — | ±400 / **391** (kode `M`, uji `M`) | idem |
| idem, `PR-00-07` | — | **134** / **222** (kode `S`, uji `M`) | idem |
| idem, `PR-00-08` | kode `M` · uji `M` (§7) | **299** / **291** — **kedua estimasi tepat** | verifikasi pertama setelah §7 dipisah |
| idem, `PR-00-09` | kode `M` · uji `M` (§7) | **315** / **333** — **kedua estimasi tepat** | verifikasi kedua |
| idem, `PR-00-10` | kode `M` · uji `M` (§7) | **186** (`S`) / **252** (`M`) — **kode meleset satu kelas ke bawah**, uji tepat | verifikasi ketiga |
| Dua instance worker → job dieksekusi tepat sekali | tepat 1× | **Terbukti terhadap Redis nyata**: dua consumer pada antrean sama, job dieksekusi 1× (dijeda 700 ms setelahnya untuk menangkap eksekusi kedua bila ada). Penjadwalan dari dua instance juga menghasilkan tepat 2 jadwal untuk 2 job, bukan 4. | acceptance `PR-00-11`, `JOB-02` |
| idem, `PR-00-11` | kode `M` · uji `L` (§7) | **234** (`M`) / **249** (`M`) — **kode tepat**, uji meleset satu kelas ke bawah | verifikasi keempat |
| Event terbit hanya setelah commit; urut per agregat | sesuai `SDD-EVT-04`/`09` | **Terbukti terhadap PostgreSQL 15 nyata**: transaksi gagal tidak meninggalkan satu pun baris; dispatcher pada koneksi lain tidak melihat event selagi transaksinya terbuka; dan **dua dispatcher bersamaan** tidak membalik urutan satu agregat | acceptance `PR-00-12` |
| idem, `PR-00-12` | kode `M` · uji `M` (§7) | **568** (`L`) / **430** (`L`) — **kedua estimasi meleset satu kelas ke ATAS**, yang pertama kali terjadi | verifikasi kelima |
| Partisi bulan berjalan ada; rantai terverifikasi; akun app tanpa UPDATE/DELETE | ketiganya | **Terbukti terhadap PostgreSQL 15 nyata**: 4 partisi terbentuk sejak migration; penyuntingan langsung terdeteksi `ISI_BERUBAH` dan penghapusan terdeteksi `RANTAI_PUTUS`; `UPDATE`/`DELETE`/`TRUNCATE`/DDL ditolak `42501` — termasuk `UPDATE` langsung ke **partisi**, bukan hanya lewat induknya | acceptance `PR-00-13` |
| Rantai hash di bawah penulisan paralel | tidak bercabang | **12 transaksi paralel**, 12 `prev_hash` berbeda, verifikasi bersih | `AL-03a`, `NFR-S-03d` |
| idem, `PR-00-13` | kode `L` · uji `L` (§7) | **683** / **519** — **kedua estimasi tepat** | verifikasi keenam |
| `llm`/`fcm` mati tidak membuat `ready` gagal | `ready` 200 | **Terbukti** untuk tiga bentuk kegagalan — `down`, melempar, menggantung melewati batas — lewat registri dan lewat HTTP (API; worker hanya sebagai server uji pada port acak — **belum** sebagai proses, lihat §5); `llm`/`fcm` dipanggil **0 kali** oleh `ready`. Terhadap PostgreSQL + Redis nyata: `ready` siap dengan `llm` mati, tidak siap dengan Redis tak terjangkau. Mutation-check: mencabut saringan penentu → 3 uji merah · mencabut syarat kritis pada `down` → 2 · mencabut `catch` → 3 | acceptance `PR-00-14`, `SDD-OBS-06`, `AI-CTL-10` |
| idem, `PR-00-14` | kode `S` · uji `S` (§7) | **436** (`L`) / **434** (`L`) — **kedua estimasi meleset dua kelas ke atas**; lihat §3 | verifikasi ketujuh |
| CSP tanpa `unsafe-inline`; kelas limit terpisah aktif | keduanya | **Terbukti.** CSP: tanpa `unsafe-inline`/`unsafe-eval` pada respons route dan 404, nonce `script-src`/`style-src` sama di satu respons dan berganti tiap permintaan. Limit, terhadap **Redis nyata**: `chat` habis pada permintaan ke-11 sementara `default` pemohon yang sama tetap lolos; jendela bergeser (5 slot terlepas di t = 61 s, 5 lainnya bertahan); lewat HTTP permintaan ke-101 dari satu IP → `429`, IP lain tetap lolos. Mutation-check: `unsafe-inline` ditambahkan → uji CSP merah · nama kelas dicabut dari kunci → 4 uji unit + acceptance integrasi merah · batas `chat` 11 → penjaga tabel SDD merah · `ZREMRANGEBYSCORE` dicabut → 2 uji integrasi merah · *fail closed* dicabut → uji `login` merah. **Tambahan setelah audit (keputusan 30–31):** 500 di mode non-production dijawab JSON `INTERNAL_ERROR` tanpa pesan asli maupun stack trace, pesan aslinya masuk log terstruktur ber-`request_id`; 404 JSON `NOT_FOUND` ber-`X-RateLimit-*`; parameter URL rusak → `400`; setiap respons ber-`X-Request-Id` yang tidak dapat dipilih klien. Terhadap Redis nyata: 20 login berhasil dari satu IP tetap lolos, 5 gagal menutupnya. Mutation-check tambahan: error handler dicabut → 4 uji merah · `requestId` dicabut → 4 · `login` menghitung semua → 5 · pengenalan galat 4xx dicabut → 1 · pencatatan kegagalan dicabut → acceptance integrasi NAT merah | acceptance `PR-00-15`, `NFR-S-07`, `NFR-S-11`, `NFR-R-10`, `SDD-SEC-04/05`, `SDD-OBS-03` |
| idem, `PR-00-15` | kode `M` · uji `M` (§7) | **559** (`L`) / **1.014** (`L`) — sebelum audit **371** (`M`) / **579** (`L`); kenaikan kode berasal dari keputusan 30–31, lihat §3 | verifikasi kedelapan |
| Seed = Lampiran C + Bab 5 + tafsir `SDD-03 §4.8` | tanpa selisih | **Terbukti terhadap PostgreSQL 15 nyata**: **79** permission (11 🔒, identik dengan `CORE_PERMISSIONS` `SDD-03 §4.7`) · **7** role · **234** grant ber-scope · `work_days` Senin–Sabtu aktif — dibandingkan `toEqual` atas seluruh himpunan. Grant tanpa scope ditolak `23502`. Menjalankan ulang seed memulihkan deskripsi, 🔒, nama role, scope, dan grant yang dicabut tanpa menggandakan baris. Mutation-check: grant Teknisi `workorder.execute` dicabut → 2 uji merah · scope Siswa `asset.view` → `ALL` → 2 · 🔒 `user.create` dicabut → 2 · matriks `DO NOTHING` → 1 · Minggu aktif → 1 · parser membaca `own` sebagai `ALL` → 1 uji berkas merah. Seluruh suite: uji berkas **295/295**, integrasi **13 berkas / 119 uji** | acceptance `PR-00-16`, `SDD-DB-10`, `SDD-DB-16` |
| idem, `PR-00-16` | kode `M` · uji `L` (§7) | **474** (`L`) / **495** (`L`) — **kode meleset satu kelas ke atas**, uji tepat; lihat §3. Tidak ada kode `src/` berlogika (hanya tipe tabel), sehingga cakupan tidak diukur ulang | verifikasi kesembilan |
| Atribut entitas PRD tanpa padanan kolom SDD | 0 | 6 → **0** setelah sapuan kedua | `SDD-05 §4.1` |
| Ejaan kolom waktu-dibuat di tabel SDD | 1 | 3 → **1** (`created_at`; `activity_logs.waktu` dikecualikan tertulis) | `SDD-05 §4.2` |
| ID dirujuk tetapi tak pernah didefinisikan | 0 | **0** dari 786 ID | `audit_docs.py` |
| Endpoint pada indeks tergenerate | — | 119 → **124** | `gen_indexes.py` |

## 9. Gerbang keluar

Diisi saat phase dinyatakan selesai. Daftar lengkapnya ada di [`phase-00.md` §9 dan §12](../phases/phase-00.md).

- [ ] Seluruh 18 PR tergabung
- [ ] Acceptance checklist phase terpenuhi
- [ ] Definition of Done phase terpenuhi
- [ ] Bagian 5 log ini terisi seluruhnya
- [ ] [`IMPLEMENTATION-STATUS.md`](../IMPLEMENTATION-STATUS.md) diperbarui

## 10. Yang diserahkan ke phase berikutnya

Hal yang sengaja ditinggalkan terbuka, beserta di mana ia akan ditutup.

| Yang ditinggalkan | Ditutup di | Alasan penundaan |
|---|---|---|
| Kolom baku `SDD-05 §4.2` + trigger `updated_at` pada `roles`; `users.role_id` → `roles(id)` | `PR-01-01` (keputusan 39) | `created_by`/`updated_by` merujuk `users(id)` yang belum ada |
| `role_version` (`SDD-AUTH-04`); `CORE_PERMISSIONS` di `SDD-03 §4.7` kini berpadanan dengan kolom `permissions.inti` — sumber mana yang dibaca validator ditetapkan bersama validatornya | `PR-01-04` | Keduanya milik matriks permission; uji berkas `PR-00-16` sudah menjaga keduanya tetap identik |
| Seed parameter sistem + katalog kunci & nilai bawaan `FR-20.1` di SDD | `PR-01-10` (keputusan 40) | Tabel dan validasi rentangnya milik PR itu; nilai bawaannya belum tertulis |
| Pemeriksaan `object_storage` dan `av_scanner` — tanpanya `ready` belum menilai storage sebagaimana `SDD-15 §4.5` | Adapter storage & AV Phase 03 (`SDD-FS-01` … `SDD-FS-09`) | Keputusan 22: klien dan SDK-nya belum ada |
| Pemeriksaan `fcm` | `PR-02-27` | Keputusan 22 |
| Pemeriksaan `llm` | `PR-03-20` | Keputusan 22; paruh `AI-CTL-10` yang lain memang milik PR itu |
| Memasang `/health` ringkasan (`setting.view`) dan mendaftarkannya ke registri | `PR-02-09` | Keputusan 21: tanpa middleware permission ia endpoint tanpa penjaga |
| Butir §9 dan kriteria keluar `M0` "`/health` melaporkan status DB, Redis, object storage, AV, FCM, LLM" | **Tidak wajib di Phase 00** (keputusan 24) — kelengkapannya diperiksa di [`phase-03.md` §9](../phases/phase-03.md); §9 dan §12 phase ini serta `M0` di PRD 29.2 sudah dinyatakan ulang | Pemeriksaan didaftarkan bersama adapternya: `PR-02-27`, `PR-03-04`, `PR-03-05`, `PR-03-20` |
| **Blocking** — entrypoint worker belum menyala saat dijalankan sebagai proses; server kesehatannya lahir di `bootstrap()` yang belum dipanggil siapa pun. Server kesehatan worker tidak dinyatakan tervalidasi runtime sampai butir ini tertutup | `PR-00-18` (keputusan 24) — acceptance-nya sudah memuat butir ini | Keadaan ini ada sejak `PR-00-11` dan di luar scope `PR-00-14` |
| Sumbu **akun** kelas `login` (penghitung di PostgreSQL, `SDD-SESS-07`) — `PR-00-15` hanya memasang sumbu IP | `PR-02-03` (keputusan 29) — rujukan dan acceptance-nya sudah memuat butir ini | Tabel akun belum ada |
| Pemakaian mode hitung-gagal pada `POST /auth/login` | `PR-02-02` (keputusan 30) | Route login belum ada |
| CSP halaman web: `'self'` + hash sha256 build-time | `PR-08-04` (keputusan 28) | `apps/web` belum punya build Vite |
| Variabel `SDD-16 §4.7` yang belum masuk skema `shared/config` | Bersama PR pemakainya (keputusan 32): `PR-02-02` (JWT), `PR-02-07` (TOTP), `PR-02-27` (FCM), `PR-03-04` (S3), `PR-03-20` (Gemini, `CHAT_ENABLED`), `PR-03-01` (`APP_BASE_URL`, keputusan 37) | Skema bertahap |
| Topologi origin web/API dan perlu tidaknya `cors` | `PR-00-18` (keputusan 38) | Topologi Nginx/CDN belum diputuskan |
| Keputusan `409 FILE_NOT_SCANNED` terhadap Bab 17.3 | `PR-03-04` (keputusan 35) | Endpoint unduh belum ada |

---

*Log ini mencatat pelaksanaan. Requirement tetap milik [PRD](../../PRD/), keputusan desain tetap milik [SDD](../../SDD/).*
